# EduScribe AI — Software Low-Level Design (LLD) Document

**Document Type:** Low-Level Design (LLD)
**Source of Truth:** EduScribe AI Technical Design Documentation
**Scope:** This document expands the existing EduScribe AI design documentation into a detailed engineering-level specification. No new architecture, technology, workflow, or feature is introduced. Every section below is a deeper elaboration of a section that already exists in the source documentation, preserving the same terminology, the same component boundaries, and the same design decisions.

---

## Table of Contents

### Core Pipeline

1. [Requirements](#1-requirements)
2. [Features](#2-features)
3. [Workflow](#3-workflow)
   - [3.1 End-to-End Pipeline](#31-end-to-end-pipeline)
   - [3.2 Prompt Pipeline](#32-prompt-pipeline)
4. [Semantic Chunking](#4-semantic-chunking)
5. [Chunk Data Model](#5-chunk-data-model)

### Content Generation

6. [Topic Detection](#6-topic-detection)
7. [Knowledge Gap Analysis](#7-knowledge-gap-analysis)
8. [Detailed Explanation Generation](#8-detailed-explanation-generation)
9. [Example Generation](#9-example-generation)
10. [Image Integration](#10-image-integration)
11. [Quality Gates](#11-quality-gates)

### Export Pipeline

12. [Markdown Generation](#12-markdown-generation)
13. [HTML Generation](#13-html-generation)
14. [PDF Generation](#14-pdf-generation)

### LLM Architecture

15. [LLM Provider Architecture](#15-llm-provider-architecture)
    - [15.1 Four-Tier Waterfall](#151-the-four-tier-waterfall)
    - [15.2 Provider Reference (2025/2026)](#152-integrated-provider-reference-20252026)
    - [15.3 Folder Structure](#153-folder-structure)
16. [LiteLLM](#16-litellm)
    - [16.1 LiteLLM Features](#161-what-litellm-provides)
    - [16.2 LiteLLM Proxy (Self-Hosted Gateway)](#162-litellm-proxy-self-hosted-gateway)
    - [16.3 Usage in Application Code](#163-usage-in-application-code)
17. [PydanticAI](#17-pydanticai)
    - [17.2 Schema Definitions (All Pipeline Stages)](#172-schema-definitions-for-all-pipeline-stages)
    - [17.3 Validation Flow and Auto-Retry](#173-validation-flow-and-auto-retry)
18. [Model Routing](#18-model-routing)
    - [18.2 Task-to-Model Routing Table (with LiteLLM IDs)](#182-task-to-model-routing-table)
    - [18.4 model_selector.py Reference Implementation](#184-model_selectorpy--reference-implementation)
    - [18.7 LLM Quality Reference (21 task categories)](#187-model-quality-reference)
    - [18.9 Embedding Model Reference (12 categories)](#189-embedding-model-reference)
    - [18.10 Model Profiles](#1810-model-profiles)
      - [18.10.0 Common Embedding Configuration](#18100-common-embedding-model-configuration)
      - [18.10.1 DeepSeek V3](#18101-deepseek-v3)
      - [18.10.2 DeepSeek R1](#18102-deepseek-r1)
      - [18.10.3 Gemini 2.5 Flash](#18103-gemini-25-flash)
      - [18.10.4 Qwen3 235B A22B](#18104-qwen3-235b-a22b)
      - [18.10.5 Qwen3 Coder](#18105-qwen3-coder)
      - [18.10.6 Llama 3.3 70B](#18106-llama-33-70b)
      - [18.10.7 Mistral Small 3.2](#18107-mistral-small-32)
      - [18.10.8 BGE-M3](#18108-bge-m3)
      - [18.10.9 Qwen3-Embedding-8B](#18109-qwen3-embedding-8b)
      - [18.10.10 Qwen3-Embedding-4B](#181010-qwen3-embedding-4b)
      - [18.10.11 NV-Embed-v2](#181011-nv-embed-v2)
      - [18.10.12 Jina Embeddings v3](#181012-jina-embeddings-v3)
      - [18.10.13 e5-Mistral-7B-Instruct](#181013-e5-mistral-7b-instruct)
      - [18.10.14 bge-base-en-v1.5](#181014-bge-base-en-v15)
      - [18.10.15 bge-small-en-v1.5](#181015-bge-small-en-v15)
    - [18.11 Free Tier Quota Reference (2025/2026)](#1811-free-tier-quota-reference-20252026)
    - [18.12 Quota-Aware Routing Logic](#1812-quota-aware-routing-logic)

### Resilience and Operations

19. [Retry Strategy](#19-retry-strategy)
20. [Fallback Strategy](#20-fallback-strategy)
21. [API Key Rotation](#21-api-key-rotation)
22. [Backend Task Flow](#22-backend-task-flow)

### Reference

23. [Technology Stack](#23-technology-stack)
24. [Important Notes](#24-important-notes)
---

## 1. Requirements

The Requirements chapter defines the contractual boundary of the EduScribe AI system — what the system must do (functional requirements), how well it must do it (non-functional requirements), and under what environmental assumptions it is expected to operate. In a Low-Level Design document, requirements are not aspirational statements; they are the criteria against which every downstream module (chunking, topic detection, explanation generation, provider management, export pipeline) is evaluated. Every subsequent chapter in this document exists specifically to satisfy one or more of the requirements defined here.

**Objective:** The objective of this chapter is to formally separate **what** the system must achieve from **how** it achieves it. This separation allows the architecture (multi-stage LLM pipeline, multi-provider fallback, semantic chunking, quality gates) to be justified against a fixed, traceable set of requirements rather than against ad-hoc engineering preference. Each requirement stated here is intentionally phrased so that it can be mapped one-to-one to a concrete module described later in this document.

### 1.3 Detailed Explanation

**Functional Requirements** describe the observable behavior of the system from the perspective of a student or instructor using EduScribe AI:

- The system must convert a lecture video into structured, in-depth educational notes rather than a brief summary. This requirement directly drives the "Atomic Generation" and "Pedagogical Depth" principles that shape the Detailed Explanation Generation module (Chapter 8).
- The system must extract and keep synchronized four distinct data streams per lecture: the audio transcript, the OCR-derived text from slides and whiteboards, timestamps, and extracted video frames. This requirement is the direct justification for the Chunk Data Model (Chapter 5), which is the structure responsible for holding all four streams together as a single unit.
- The system must detect lecture structure automatically — decomposing a lecture into macro-topics and then into micro-subtopics — rather than depending on manual tagging by an instructor or student. This is fulfilled by the Topic Detection module (Chapter 6).
- The system must generate standalone, pedagogically complete explanations per subtopic, covering intuition, examples, applications, and pitfalls. This is fulfilled jointly by Detailed Explanation Generation (Chapter 8) and Example Generation (Chapter 9).
- The system must export the final notes into Markdown, HTML, and PDF formats. This requirement is fulfilled by the three-stage export pipeline described in Chapters 12–14.
- The system must support multiple LLM providers with automatic failover, so that generation does not stop entirely because a single provider's quota is exhausted or the provider experiences an outage. This is fulfilled by the LLM Provider Architecture (Chapter 15) together with the Fallback Strategy (Chapter 20).
- The system must support future features such as quizzes, flashcards, mind maps, and RAG-based tutoring without requiring the entire video to be reprocessed. This requirement is satisfied structurally — not by building those features now, but by ensuring the Chunk Data Model and Knowledge Tree are reusable, persistent structures rather than transient computation artifacts.

**Non-Functional Requirements** describe the quality attributes the system must exhibit regardless of which feature is being executed:

- **Reliability** — the LLM layer must have no single point of failure. This is why the architecture never calls a single provider directly; it always routes through the LLM Manager, which is capable of switching providers transparently.
- **Scalability** — long-running AI operations (transcription, OCR, note generation, PDF rendering) must not block the request/response cycle of the API. This requirement is fulfilled through the Backend Task Flow (Chapter 22), which offloads these operations to asynchronous workers.
- **Maintainability** — provider-specific logic, prompt logic, business logic, and presentation logic must remain clearly separated so that a change in one layer does not force a change in another.
- **Cost-efficiency** — free-tier APIs must be prioritized, and computationally cheap tasks must be routed to lightweight models rather than to the most capable (and most expensive) model available. This requirement directly produces the Model Routing strategy (Chapter 18).
- **Auditability** — every prompt, response, latency measurement, token count, and cost figure must be logged, so that prompt quality and provider performance can be evaluated over time.

**Environmental Assumption** — the transcription component of the system (MLX Whisper) is explicitly built and optimized for Apple Silicon hardware. This is stated as a design fact in the source documentation, not as an incidental detail, and it has downstream implications for every module that depends on transcript availability, since transcript generation is the very first step of the pipeline.

### 1.4 Internal Workflow
Requirements do not have an "internal workflow" in the runtime sense — they are static constraints. However, they do have a **traceability workflow**: each functional requirement is decomposed into one or more modules, each module produces an output artifact, and that artifact is validated (via Quality Gates, Chapter 11) against the requirement that produced it. This traceability chain — Requirement → Module → Artifact → Validation — is what allows the system to claim, at any point, that it is meeting its stated requirements.

**Rationale:** Requirements were deliberately split into functional and non-functional categories because the two categories are validated differently. Functional requirements are validated by inspecting the output (does the note contain a worked example, a complexity analysis, a recap?). Non-functional requirements are validated by inspecting system behavior over time (did the pipeline continue functioning when a provider's quota was exhausted? did the API remain responsive while a 500MB video was being transcribed?). Keeping both categories explicit prevents a common failure mode in AI-generation systems, where "the output looks fine" is mistaken for "the system is reliable."

### 1.6 Advantages
- Provides a single, unambiguous reference point against which every module in this document can be justified.
- Makes the cost-efficiency requirement explicit early, which shapes every subsequent architectural decision involving LLM usage (chunking granularity, model routing, retry limits).
- Separates "what must be true of the output" from "what must be true of the system," avoiding conflation between content quality and system robustness.

### 1.7 Limitations
The environmental assumption tying transcription to Apple Silicon is a structural limitation baked directly into the requirements: since MLX Whisper is the transcription engine specified in the source documentation, the Requirements chapter implicitly inherits a platform dependency that affects the very first stage of the End-to-End Pipeline (Chapter 3.1).

**Summary:** The Requirements chapter is the foundation from which every other chapter in this LLD is derived. Functional requirements define the shape of the output (structured, subtopic-level, multi-format notes); non-functional requirements define the shape of the system that produces that output (reliable, asynchronous, cost-aware, auditable). Every module described in the remaining chapters exists because one of these requirements demanded it.

---

## 2. Features

Where the Requirements chapter defines obligations, the Features chapter defines the concrete design principles and mechanisms EduScribe AI implements to satisfy those obligations. Each feature listed here corresponds to an architectural decision that recurs throughout the rest of the system — these are not surface-level capabilities but structural principles that govern how every module is built.

**Objective:** The objective of this chapter is to enumerate the core design principles that differentiate EduScribe AI's architecture from a naive "transcript-to-summary" pipeline, and to describe, at a conceptual level, the mechanism by which each principle is enforced.

### 2.3 Detailed Explanation

**Structural Discovery.** Before any explanatory content is generated, the system performs a diagnostic pass over the entire lecture — the Lecture Analysis stage. This diagnostic pass produces metadata (subject, difficulty, prerequisites, teaching style) that is consumed by every later stage. The principle underlying this feature is "understand before you generate": generation quality depends on the model already knowing the shape of the lecture before it begins writing about any single part of it.

**Atomic Generation.** Rather than producing one large document in a single LLM call, EduScribe AI generates notes on a per-subtopic basis. Each subtopic explanation is generated as an independent unit of work. This directly prevents the dilution problem inherent to global summarization, where detailed sub-concepts get compressed away because the model is trying to cover an entire lecture in one pass.

**Multimodal Integrity.** Every claim made in the generated notes must be traceable back to the source material — the audio transcript, the OCR text, the timestamp, and the visual frame. This is enforced structurally by the Chunk Data Model (Chapter 5), which binds all four data types together at the chunk level, so that no stage of the pipeline can process one modality (e.g., transcript) in isolation from the others (e.g., OCR, frames).

**Pedagogical Depth.** Output must read as an "explanation," not a "summary." This distinction is operationalized in the Detailed Explanation Generation prompt (Chapter 8), which explicitly instructs the model to explain the "why" behind a concept — including derivations, comparisons to alternative (less efficient) approaches, and internal reasoning — rather than restating what the instructor said.

**Holistic Framework.** Every major topic follows a fixed educational template: Intuition (conceptual hook) → Practical Examples → Real-World Applications → Common Pitfalls/Misconceptions → Synthesis Recap. This template is what gives every generated topic a consistent shape regardless of subject matter, whether the lecture is on Binary Search or on a different domain entirely.

**Token-Agnostic Quality.** The system places no artificial ceiling on the length of a generated explanation. Length is dictated exclusively by the complexity of the subtopic being explained. This principle is what necessitates multi-stage prompting in the first place — a single-pass summarization prompt could never produce token-agnostic depth, because it is inherently bounded by the practical limits of a single request.

**Multi-Provider LLM Layer.** Automatic provider failover, API key rotation, and task-based model routing together ensure that the token-agnostic, multi-stage generation strategy described above does not create a single point of failure. Because atomic, per-subtopic generation multiplies the number of LLM calls required per lecture, provider resilience is not optional — it is a structural requirement of the atomic generation feature itself.

**Semantic Chunking.** The lecture is divided according to conceptual boundaries rather than fixed token counts, with slight overlap between adjacent chunks to preserve continuity. This feature is the mechanism that makes Structural Discovery and Atomic Generation possible in practice — without semantically coherent chunks, topic detection and subtopic detection would operate on fragmented, boundary-broken input.

**Knowledge Tree.** All topic and subtopic mentions, even when a concept is revisited multiple times across the lecture, are deduplicated and merged into a single hierarchical structure. This feature directly prevents duplicate note generation for a concept the instructor returns to later in the lecture.

**Quality Gates.** Automated checks are applied to the generated content (e.g., "every algorithm must include a worked example"). Failed checks trigger regeneration of only the affected subtopic, not the entire document, keeping the correction process both targeted and cost-efficient.

**Image Integration.** OCR-derived diagrams, formulas, and handwritten notes are embedded directly next to the explanation section that discusses them, rather than being listed separately or ignored.

**Multi-Format Export.** The final notes are converted through a defined chain — Markdown → HTML → PDF — using Jinja2 templating, Markdown-it-py rendering, and WeasyPrint PDF generation, respectively.

**Observability.** Every prompt/response cycle is logged through Langfuse, capturing latency, token usage, and cost, so that prompt quality and provider performance remain measurable over time.

### 2.4 Internal Workflow
The features described above are not independent — they form a dependency chain. Semantic Chunking must occur before Structural Discovery can operate on coherent input. Structural Discovery (Lecture Analysis) must complete before Atomic Generation can begin, because atomic generation depends on the topic/subtopic hierarchy that Structural Discovery — via Topic Detection — produces. The Knowledge Tree depends on Atomic Generation having identified all topic occurrences across all chunks. Quality Gates operate after Atomic Generation, validating its output. Multi-Format Export is the terminal stage, operating only after Quality Gates have passed. Observability runs orthogonally throughout, capturing telemetry from every LLM-touching feature.

**Rationale:** Each feature exists to counteract a specific, named failure mode of naive summarization systems: Structural Discovery counteracts context-blind generation; Atomic Generation counteracts dilution; Multimodal Integrity counteracts unverifiable claims; Pedagogical Depth counteracts shallow restatement; the Holistic Framework counteracts inconsistent topic coverage; Token-Agnostic Quality counteracts premature truncation; the Multi-Provider LLM Layer counteracts single-vendor fragility; Semantic Chunking counteracts concept fragmentation; the Knowledge Tree counteracts duplication; Quality Gates counteract undetected omissions; Image Integration counteracts loss of visual context; and Multi-Format Export counteracts single-format lock-in for the end user.

### 2.6 Advantages
- Every feature is traceable to a specific requirement from Chapter 1, avoiding speculative or unjustified functionality.
- The features collectively form a pipeline where each stage's output is the next stage's precondition, which simplifies reasoning about failure (a defect can be localized to the stage whose precondition was violated).
- Because features like Quality Gates operate at subtopic granularity, correction cost stays proportional to the size of the defect rather than to the size of the document.

### 2.7 Limitations
Token-Agnostic Quality, while beneficial for depth, has a direct and unavoidable cost implication: an uncapped explanation length interacts with the free-tier, quota-limited nature of the LLM providers described in Chapter 15 — a feature already flagged in the source documentation as requiring active quota monitoring.

**Summary:** The Features chapter operationalizes the requirements into a coherent set of design principles. Each feature is not a standalone capability but a structural commitment that shapes multiple downstream modules simultaneously — which is why, in the chapters that follow, the same feature names (semantic boundaries, atomicity, quality gates, multi-provider resilience) recur repeatedly across otherwise distinct chapters.

---

## 3. Workflow

### 3.1 End-to-End Pipeline

#### 3.1.1 Introduction
The End-to-End Pipeline is the top-level orchestration sequence that transforms a raw lecture video into a fully exported set of educational notes. It is the single authoritative sequence diagram for the entire system: every other chapter in this document describes one stage of this pipeline in greater detail.

#### 3.1.2 Design Objective
The objective of the End-to-End Pipeline is to impose a strict, linear dependency order on otherwise heterogeneous processing stages (audio processing, computer vision, natural language understanding, and document rendering), ensuring that no stage begins before its required inputs are available.

#### 3.1.3 Detailed Explanation
The pipeline begins with raw video ingestion, from which two extraction processes run in parallel: transcript generation (Whisper) and OCR/frame processing (PaddleOCR + OpenCV). These outputs are not consumed independently; they are merged during Semantic Chunking, which is the first stage to require both streams simultaneously. From semantic chunks, the system performs Topic Detection to identify macro-topics, followed by Subtopic Detection to decompose each macro-topic into specific, addressable units of explanation. The results of subtopic detection are merged into a Knowledge Tree, which deduplicates repeated topic mentions across the lecture. Only after the Knowledge Tree is finalized does Detailed Explanation Generation begin, operating one subtopic at a time. Once an explanation exists, Example Generation and Visual Reference attachment occur as a distinct step, followed by the three-stage export chain: Markdown generation, HTML generation, and PDF generation.

#### 3.1.4 Internal Workflow

```
Video
  ↓
Transcript (Whisper) + OCR (PaddleOCR) + Frame Extraction (OpenCV)
  ↓
Semantic Chunking
  ↓
Topic Detection (Macro-Topics)
  ↓
Subtopic Detection (Micro-Subtopics)
  ↓
Knowledge Tree (deduplicated, hierarchical)
  ↓
Detailed Explanation Generation (per subtopic)
  ↓
Examples and Visual References
  ↓
Markdown Generation
  ↓
HTML Generation
  ↓
PDF Generation
```

```mermaid
flowchart TD
    A[Video] --> B[Transcript - Whisper]
    A --> C[OCR - PaddleOCR]
    A --> D[Frame Extraction - OpenCV]
    B --> E[Semantic Chunking]
    C --> E
    D --> E
    E --> F[Topic Detection - Macro-Topics]
    F --> G[Subtopic Detection - Micro-Subtopics]
    G --> H[Knowledge Tree]
    H --> I[Detailed Explanation Generation]
    I --> J[Examples and Visual References]
    J --> K[Markdown Generation]
    K --> L[HTML Generation]
    L --> M[PDF Generation]
```

#### 3.1.5 Design Rationale
The pipeline is deliberately linear rather than a graph of independent parallel tasks, because each stage's output is a structural precondition for the next. Topic detection cannot run correctly on unchunked transcript, since the chunk boundaries are what encode "conceptual completeness" in the first place. Explanation generation cannot begin before the Knowledge Tree exists, since generating an explanation for a topic that will later be merged with another occurrence of the same topic would produce duplicate, conflicting content.

#### 3.1.6 Advantages
- A single, unambiguous processing order simplifies debugging: a defect in the output can always be traced back to exactly one pipeline stage.
- Parallelism is preserved where it is safe (transcript vs. OCR/frame extraction) and removed where it is not (chunking must wait for both streams).

#### 3.1.7 Conclusion
The End-to-End Pipeline is the master orchestration sequence of EduScribe AI. Every chapter from Section 4 onward in this document (Semantic Chunking, Chunk Data Model, Topic Detection, and so on) corresponds to exactly one node in this pipeline, described here at the level of a system-wide sequence and expanded, chapter by chapter, at module level.

---

### 3.2 Prompt Pipeline

#### 3.2.1 Introduction
The Prompt Pipeline is the LLM-facing counterpart to the End-to-End Pipeline. While the End-to-End Pipeline describes the movement of data through the system (video → chunks → topics → notes → export files), the Prompt Pipeline describes the sequence of distinct, single-responsibility LLM calls that transform structured input into structured educational output.

#### 3.2.2 Design Objective
The objective of the Prompt Pipeline is to decompose what would otherwise be a single, overloaded "summarize this lecture" instruction into a sequence of narrowly scoped prompts, each with exactly one responsibility, so that each individual LLM call can be evaluated, debugged, and regenerated independently of the others.

#### 3.2.3 Detailed Explanation
A single summarization prompt applied to an entire transcript is described in the source documentation as producing shallow, incomplete output — it tends to mention an algorithm and its complexity while omitting prerequisites, worked examples, common implementation mistakes, and practical applications. The Prompt Pipeline avoids this by assigning each of these concerns to its own dedicated stage.

The pipeline begins with **Stage 1: Lecture Analysis**, which does not produce any explanatory content; instead, it identifies the lecture's subject, difficulty, prerequisites, teaching style, and primary learning objectives, outputting this as structured JSON metadata that every later stage consumes.

**Stage 2: Topic Detection** identifies the macro-topics of the lecture, each associated with a start and end timestamp, without attempting to explain any of them.

**Stage 3: Subtopic Detection** operates on each topic independently, decomposing it into its internal structure (for example, "Binary Search" decomposing into Definition, Prerequisites, Algorithm, Dry Run, Complexity, and Applications).

**Stage 4: Knowledge Gap Analysis** is a distinctive stage that identifies information a beginner would require but which the lecture only briefly mentions or assumes entirely — definitions the instructor never explicitly stated, missing prerequisite concepts, implicit reasoning, and potential misconceptions. Its output is not shown to the end user directly; it guides Stage 5 so that generated notes function as complete learning resources rather than as a reformatted transcript.

**Stage 5: Detailed Explanation Generation** is the core content-producing stage, processing exactly one subtopic per invocation and instructing the model to teach the concept — covering definition, intuition, motivation, step-by-step reasoning, visual explanation, mathematical derivation where applicable, worked examples, code examples for programming lectures, real-world applications, common mistakes, best practices, interview perspective, and key takeaways. No upper bound is placed on explanation length.

**Stage 6: Example Generation** operates as an independent stage from explanation generation, producing simple examples, intermediate examples, real-world applications, numerical examples, programming examples, and visual examples tied to lecture frames.

The source documentation's pipeline diagram additionally lists "Common Mistakes" and "Revision Notes" as intermediate stages positioned between example generation and formatting, and the stage numbering explicitly continues at **Stage 9: Markdown Generation** — the final AI-facing stage, which converts the structured educational content produced by the preceding stages into well-formatted Markdown as an independent task, deliberately separated from content generation so that formatting logic can evolve without altering the explanation or example prompts.

#### 3.2.4 Internal Workflow

```
Video
  ↓
Transcript + OCR + Frames
  ↓
Stage 1: Lecture Analysis        (subject, difficulty, prerequisites, teaching style)
  ↓
Stage 2: Topic Detection         (macro-topics + timestamps)
  ↓
Stage 3: Subtopic Detection      (micro-subtopics per topic)
  ↓
Stage 4: Knowledge Gap Analysis  (implicit/missing prerequisite info)
  ↓
Stage 5: Detailed Explanation Generation (per subtopic, no length ceiling)
  ↓
Stage 6: Example Generation      (simple / intermediate / real-world / numerical / code / visual)
  ↓
Stage 9: Markdown Generation
  ↓
HTML Generation
  ↓
PDF Generation
```

```mermaid
sequenceDiagram
    participant V as Video
    participant O as Orchestrator LLM
    participant S as Scribe Agent(s)
    V->>O: Transcript + OCR + Frames
    O->>O: Stage 1 - Lecture Analysis
    O->>O: Stage 2 - Topic Detection
    O->>O: Stage 3 - Subtopic Detection
    O->>O: Stage 4 - Knowledge Gap Analysis
    O->>S: Dispatch subtopic list
    S->>S: Stage 5 - Detailed Explanation Generation
    S->>S: Stage 6 - Example Generation
    S->>O: Structured subtopic notes
    O->>O: Stage 9 - Markdown Generation
    O->>O: HTML Generation
    O->>O: PDF Generation
```

#### 3.2.5 Design Rationale
Each stage is deliberately restricted to a single responsibility so that a single LLM call is never asked to simultaneously understand, structure, and explain a lecture. This mirrors the "Atomic Generation" feature from Chapter 2: just as notes are generated per-subtopic, prompts are scoped per-responsibility. This design also means that improving the quality of one stage (for example, refining the Knowledge Gap Analysis prompt) never requires modifying any other stage's prompt.

#### 3.2.6 Advantages
- Each stage's output is independently structured (JSON where applicable), simplifying validation via PydanticAI (Chapter 17).
- Debugging is localized: if generated notes are missing prerequisite context, the fault can be isolated to Stage 4 rather than requiring a full pipeline re-audit.
- Later stages (Stage 5 onward) always operate on the structured output of earlier stages rather than on the raw transcript, reducing redundant re-processing.

#### 3.2.7 Limitations
The stage sequence, as documented, defines Stages 1 through 6 explicitly and then continues at Stage 9, with the pipeline diagram additionally referencing "Common Mistakes" and "Revision Notes" as intermediate steps. This numbering gap is inherited directly from the source documentation and is preserved here rather than resolved, per the instruction to introduce no new assumptions.

#### 3.2.8 Conclusion
The Prompt Pipeline is the LLM-execution backbone of EduScribe AI's content generation process. It converts the single-responsibility principle from general software engineering into a concrete prompting strategy, ensuring that lecture understanding, structural organization, and pedagogical explanation are always handled as separate, independently verifiable operations.

---

## 4. Semantic Chunking

Semantic Chunking is the component responsible for dividing merged transcript and OCR content into coherent, self-contained units before any topic detection or explanation generation occurs. It is positioned immediately after data ingestion in the End-to-End Pipeline and is a precondition for every subsequent AI-facing stage.

**Objective:** The objective of Semantic Chunking is to eliminate the "mid-sentence break" and "mixed-concept" problems that arise from fixed-size, token-based chunking, ensuring that every unit of text handed to the LLM pipeline represents one complete educational idea.

### 4.3 Detailed Explanation
Token-based chunking divides a transcript at fixed intervals (for example, every 1,000 tokens) irrespective of where a concept naturally begins or ends. The source documentation illustrates the resulting failure directly: a single explanation of Binary Search could be split across two chunks, with the algorithm description appearing in one chunk and the complexity analysis appearing in a separate chunk; alternatively, one chunk might contain the tail end of Binary Search together with the beginning of Merge Sort, forcing the LLM to reason about two unrelated concepts as if they were one.

Semantic Chunking replaces this fixed-size strategy with **boundary detection** — chunk edges are placed at points where the lecture's content actually transitions from one concept to another. The system combines multiple signals to detect these transitions: changes in lecture topic, significant pauses in speech, slide transitions, changes in OCR-extracted text, the appearance of new headings on slides, explicit instructor transition phrases ("Now let's discuss…", "Next…", "Moving on…"), and major shifts in vocabulary. No single signal is treated as authoritative; the boundary decision is a combination of these indicators.

Two additional structural rules govern chunk creation. First, **chunk overlap**: because educational concepts rarely end abruptly at a single sentence, adjacent chunks share a small semantic overlap — for example, if Chunk A ends with "Definition" and "Algorithm," Chunk B may begin with "Algorithm," "Example," and "Complexity," so that the "Algorithm" section is not lost at the boundary. This overlap is determined by concept continuity, not by a fixed token count, distinguishing it from traditional token-overlap strategies. Second, **chunk linking**: if the lecturer returns to an earlier concept later in the lecture, the system links the new chunk to the earlier chunk covering the same concept instead of duplicating the content — this is the mechanism that feeds directly into the deduplication performed later by the Knowledge Tree (Chapter 6).

### 4.4 Internal Workflow

```
Video
  ↓
Whisper Transcript
  ↓
OCR Text
  ↓
Merge Transcript + OCR
  ↓
Semantic Boundary Detection
  ↓
Chunk Creation
  ↓
Chunk Validation
  ↓
Chunk Storage
```

```mermaid
flowchart TD
    A[Whisper Transcript] --> C[Merge Transcript + OCR]
    B[OCR Text] --> C
    C --> D[Semantic Boundary Detection]
    D --> E[Chunk Creation]
    E --> F[Chunk Validation]
    F --> G[Chunk Storage]
    F -->|fails validation| H[Regenerate / Merge with Neighboring Chunk]
    H --> F
```

Only chunks that pass validation are forwarded to the LLM pipeline. Chunk validation checks include: transcript availability, OCR consistency with the transcript, timestamp correctness, minimum information density, semantic completeness (the chunk is not cut mid-concept), and duplicate detection. Chunks that fail validation are either regenerated or merged with a neighboring chunk rather than discarded outright.

Chunks are not stored as isolated units — they form a connected relationship graph:

```
Chunk 1 → Chunk 2 → Chunk 3 → Chunk 4
```

Each chunk references its previous and next neighbor, which allows later stages to recover context. For example, if Chunk 5 begins with the phrase "As discussed previously…", the system can automatically retrieve Chunk 4 to restore the missing context rather than treating Chunk 5 as an isolated, context-free unit.

**Rationale:** Semantic Chunking exists because every downstream AI stage — Topic Detection, Subtopic Detection, Detailed Explanation Generation — depends on receiving conceptually complete input. If chunk boundaries were arbitrary, Topic Detection would be forced to reconstruct concept boundaries that chunking should have already established, effectively duplicating work at a later, more expensive stage of the pipeline. Placing boundary detection as early as possible in the pipeline is therefore a direct consequence of the "Structural Discovery" feature principle described in Chapter 2.

### 4.6 Advantages
- Preserves the logical flow of the lecture rather than fragmenting it at arbitrary token boundaries.
- Produces more accurate topic detection, since Topic Detection operates on chunks that already respect concept boundaries.
- Reduces hallucination risk, since the model is never asked to reason across two unrelated concepts crammed into the same chunk.
- Establishes a reusable structural foundation for chunk-dependent features described elsewhere in the source documentation (flashcards, quiz generation, mind maps, RAG), since each of these consumes the same chunk objects that Semantic Chunking produces.

### 4.7 Limitations
Because boundary detection relies on multiple heuristic signals (pauses, transition phrases, OCR changes, vocabulary shifts) rather than a single deterministic rule, chunk quality is directly dependent on the reliability of the underlying transcript and OCR extraction — a lecture with poor audio quality or visually noisy slides would produce weaker boundary signals, which is precisely why Chunk Validation exists as a mandatory gate before storage.

**Summary:** Semantic Chunking is the foundational data-preparation stage of EduScribe AI. It converts an undifferentiated stream of transcript and OCR text into a validated, relationship-aware set of conceptually complete units, and its output — the chunk — is the atomic unit that every subsequent stage of the pipeline, from Topic Detection through to future RAG-based features, is built to consume.

---

## 5. Chunk Data Model

The Chunk Data Model defines the structured representation of a single semantic chunk once it has passed validation. It is the concrete schema that operationalizes the "Multimodal Integrity" feature described in Chapter 2 — the binding of transcript, OCR text, timestamps, and frame references into one addressable unit.

**Objective:** The objective of the Chunk Data Model is to ensure that every chunk carries not just its transcript content but the complete set of metadata required by every downstream consumer of that chunk — Topic Detection, Subtopic Detection, Notes Generation, and future chunk-dependent features — without requiring those consumers to re-derive metadata that chunking has already computed.

### 5.3 Detailed Explanation
A chunk is not merely a slice of transcript text. Every chunk stores structured metadata across four categories:

- **Temporal Metadata** — start timestamp, end timestamp, lecture duration, estimated reading time.
- **Visual Metadata** — frame references, OCR confidence, number of associated images.
- **Content Metadata** — keywords, topic hints, difficulty estimate, chunk summary.
- **Relationship Metadata** — previous chunk, next chunk, parent topic, child subtopics.

This structured representation is what allows downstream components to access transcript content, visual content, and contextual/relationship content through a single, uniform object, rather than requiring each downstream component to separately query multiple independent data sources.

### 5.4 Internal Workflow

The canonical chunk record, as defined in the source documentation, is represented as JSON:

```json
{
  "chunk_id": 15,
  "lecture_id": 2,
  "start_time": "00:12:20",
  "end_time": "00:18:45",
  "topic_hint": "Binary Search",
  "transcript": "...",
  "ocr_text": ["Binary Search", "O(log n)", "Sorted Array"],
  "frame_ids": ["frame_24.jpg", "frame_25.jpg"],
  "previous_chunk": 14,
  "next_chunk": 16,
  "confidence": 0.97
}
```

An earlier, simpler variant of the same structure is also defined in the source documentation at the chunking-strategy level, confirming that the fields `chunk_id`, `start_time`, `end_time`, `transcript`, `ocr_text`, and `frames` are the minimum required fields, with `lecture_id`, `topic_hint`, `previous_chunk`, `next_chunk`, and `confidence` representing the fuller, relationship-aware form of the same object.

Chunk storage is organized hierarchically at the database level:

```
Lectures
  ↓
Chunks
  ↓
Topics
  ↓
Subtopics
  ↓
Generated Notes
```

This organization is what simplifies future retrieval: a query for "all notes belonging to a lecture" can traverse this hierarchy top-down, and a query for "which chunk supports this piece of generated text" can traverse it bottom-up.

**Rationale:** The Chunk Data Model is intentionally over-specified relative to what a single downstream consumer (say, Topic Detection) strictly needs, because the same chunk object is reused across multiple, otherwise-unrelated features: Topic Detection, Subtopic Detection, Notes Generation, Flashcards, Quiz Generation, Mind Maps, and RAG all consume the same chunk structure, described in the source documentation as chunking being "the foundation for almost every AI feature." Under-specifying the schema now would force schema migrations later, once those additional features are implemented.

### 5.6 Advantages
- A single schema serves every chunk-consuming feature, avoiding duplicated or inconsistent chunk representations across modules.
- Relationship metadata (`previous_chunk`, `next_chunk`) enables context recovery without requiring a full-lecture re-scan.
- Confidence scoring is captured directly on the chunk, allowing downstream consumers to make informed decisions about how much to trust a given chunk's content.

**Summary:** The Chunk Data Model is the structural contract that makes every other module in this document interoperable. By encoding temporal, visual, content, and relationship metadata directly onto the chunk object, it ensures that no downstream stage of the pipeline needs to reconstruct information that Semantic Chunking has already computed.

---

## 6. Topic Detection

Topic Detection is the stage responsible for imposing hierarchical structure onto the set of validated chunks produced by Semantic Chunking. It operates in two tiers, converting an undifferentiated sequence of chunks into a navigable macro/micro topic hierarchy.

**Objective:** The objective of Topic Detection is to produce a strict, machine-readable roadmap of the lecture's content — identifying both the major pillars of the lecture and the specific concepts within each pillar — so that subsequent generation stages know exactly what units of content need to be explained, without omitting or misrepresenting any technical nuance.

### 6.3 Detailed Explanation
Topic Detection uses a **two-tiered detection strategy**. In **Tier 1**, the Orchestrator identifies "Macro-Topics" — the main pillars of the lecture, such as "Binary Search" within a lecture on searching algorithms. In **Tier 2**, each macro-topic is decomposed into "Micro-Subtopics" — the specific algorithms, definitions, or proofs that fall under that macro-topic, such as Definition, Prerequisites, Algorithm, Dry Run, Complexity, and Applications for the "Binary Search" macro-topic.

This hierarchy is output as strict JSON, which serves as the roadmap consumed by all later generation stages. After all chunks have been processed, repeated topic mentions are merged: if "Binary Search" appears across multiple chunks (for example, because the instructor first introduces it, then later revisits it during a Q&A segment), the system collects all associated information beneath a single topic node in the Knowledge Tree rather than generating duplicate notes for each occurrence.

### 6.4 Internal Workflow

**Tier 1 output (macro-topics, with timestamps):**
```json
{
  "topics": [
    { "title": "Binary Search", "start": "00:05:20", "end": "00:18:30" }
  ]
}
```

**Tier 2 output (micro-subtopics per topic):**
```json
{
  "topic": "Binary Search",
  "subtopics": ["Definition", "Prerequisites", "Algorithm", "Dry Run", "Complexity", "Applications"]
}
```

```mermaid
flowchart TD
    A[Validated Chunks] --> B[Tier 1: Macro-Topic Detection]
    B --> C["Macro-Topic + Timestamps (JSON)"]
    C --> D[Tier 2: Micro-Subtopic Detection]
    D --> E["Subtopics per Topic (JSON)"]
    E --> F[Merge Repeated Topic Mentions]
    F --> G[Knowledge Tree]
```

**Rationale:** The two-tier split exists because macro-topic identification and micro-subtopic identification are fundamentally different tasks: the former requires understanding the overall shape of the lecture (which is why it depends on Structural Discovery / Lecture Analysis output), while the latter requires deep, topic-specific decomposition. Separating them into two tiers allows each to be validated independently — a Tier 1 error (missing an entire macro-topic) is a structurally different failure from a Tier 2 error (missing a subtopic within an otherwise correctly identified macro-topic), and each is easier to detect and correct in isolation.

### 6.6 Advantages
- Strict JSON output makes the topic hierarchy machine-verifiable via PydanticAI (Chapter 17), rather than relying on free-text parsing.
- Timestamps attached at the macro-topic level allow later explanation stages to reference the corresponding video segment.
- Merging repeated topic mentions at this stage prevents duplicate note generation downstream, directly feeding the Knowledge Tree's deduplication behavior.

**Summary:** Topic Detection transforms a flat sequence of validated chunks into a structured, two-tier hierarchy that becomes the authoritative table of contents for the entire generated document. Every subsequent content-generation stage — Knowledge Gap Analysis, Detailed Explanation Generation, Example Generation — operates against the topic/subtopic units this stage produces, not against raw chunk text.

---

## 7. Knowledge Gap Analysis

Knowledge Gap Analysis is a distinctive stage in the Prompt Pipeline (Stage 4) that identifies information necessary for a beginner's understanding of a topic but which is missing, implicit, or only briefly mentioned in the lecture itself.

**Objective:** The objective of Knowledge Gap Analysis is to prevent generated notes from being a faithful but incomplete reformatting of the transcript — ensuring instead that the notes function as a complete learning resource, even when the instructor omitted or assumed certain foundational information during the live lecture.

### 7.3 Detailed Explanation
Lecture transcripts frequently omit information that instructors explain only verbally, gesture toward without stating explicitly, or assume the audience already possesses. If EduScribe AI simply converted the transcript into notes verbatim, these implicit gaps would be inherited directly into the output, leaving the student with the same conceptual blind spots that existed in the live lecture. Knowledge Gap Analysis addresses this by explicitly asking the LLM to identify:

- Definitions that were assumed but never explicitly stated by the instructor.
- Missing prerequisite concepts that a beginner would need before the current topic makes sense.
- Implicit reasoning — logical steps the instructor skipped because they were "obvious" to an expert but not to a novice.
- Additional context required for full understanding.
- Potential misconceptions students might form from the way the material was presented.

Critically, this stage's output is not surfaced to the end user as a standalone artifact. It is consumed internally, guiding Stage 5 (Detailed Explanation Generation) so that the resulting explanation proactively fills these gaps rather than leaving them for the student to discover independently.

### 7.4 Internal Workflow

```mermaid
flowchart LR
    A[Topic/Subtopic + Transcript Context] --> B[Knowledge Gap Analysis]
    B --> C[Identified Gaps: definitions, prerequisites, implicit reasoning, missing context, misconceptions]
    C --> D["Stage 5: Detailed Explanation Generation (gap-aware)"]
```

**Rationale:** Positioning Knowledge Gap Analysis as its own dedicated stage — rather than folding gap-detection instructions directly into the explanation-generation prompt — follows the same single-responsibility principle that governs the entire Prompt Pipeline (Chapter 3.2). Asking one prompt to simultaneously detect gaps and write a polished explanation risks the model prioritizing fluent prose over rigorous completeness checking; separating the two tasks ensures gap detection receives dedicated reasoning attention before any explanatory text is drafted.

### 7.6 Advantages
- Directly operationalizes the "Pedagogical Depth" feature principle from Chapter 2 by targeting the specific failure mode — implicit assumptions — that separates a transcript reformatting from a genuine explanation.
- Produces reusable diagnostic output that could, in principle, also inform Quality Gates (Chapter 11), since a subtopic whose known gaps were never addressed in the final explanation would represent an incomplete output.

**Summary:** Knowledge Gap Analysis is the mechanism by which EduScribe AI distinguishes itself from a transcript-to-notes converter. By explicitly surfacing what the lecture assumed but never stated, it ensures the downstream explanation stage produces content suitable for a student encountering the topic for the first time — directly fulfilling the system's stated vision of simulating the pedagogical depth of an experienced instructor.

---

## 8. Detailed Explanation Generation

Detailed Explanation Generation (Stage 5 of the Prompt Pipeline) is the core content-producing module of EduScribe AI. It is the stage where the system's central design philosophy — "explanation, not summary" — is directly enacted.

**Objective:** The objective of this module is to treat every subtopic as an isolated, standalone lesson, generating explanatory depth that would be structurally impossible to achieve within a single-pass, whole-lecture summarization prompt.

### 8.3 Detailed Explanation
Each subtopic is generated using what the source documentation calls an "Expanded Pedagogical Prompt." Rather than stating a fact or formula directly, the model is instructed to explain the derivation, the variables involved, and the reasoning behind why particular constants or conditions exist. This produces a document intended to function as a standalone educational resource, not a collection of disconnected bullet points.

For every subtopic, the generation prompt requires the following components, in order: definition in simple language; why the concept exists (motivation); prerequisites; a step-by-step explanation; a worked example; a visual explanation using the associated lecture frames; common mistakes; edge cases; complexity analysis where applicable; real-world applications; an interview or examination perspective; and key takeaways.

The source documentation's illustrative example makes the distinction between summary and explanation concrete: rather than writing "Binary Search repeatedly divides the search space in half," the model is expected to explain why linear search becomes inefficient at scale, introduce the requirement that the array must be sorted, describe how the middle element is chosen, walk through every comparison step, provide a complete worked example with actual values, analyze time complexity, discuss common implementation mistakes such as integer overflow when calculating the midpoint, and conclude with practical applications such as searching databases or dictionaries. Relevant OCR text and lecture screenshots are embedded wherever they reinforce a specific part of the explanation, directly implementing the Image Integration feature (Chapter 10).

Critically, **no upper limit is placed on explanation length**. If a concept genuinely requires several pages to be properly explained, the model is expected to produce that length of output rather than truncating for brevity. This is the direct implementation of the "Token-Agnostic Quality" principle from Chapter 2.

### 8.4 Internal Workflow

```mermaid
flowchart TD
    A[Subtopic identified by Topic Detection] --> B[Knowledge Gap Analysis output]
    B --> C[Expanded Pedagogical Prompt]
    C --> D[Definition]
    C --> E[Motivation / Why it exists]
    C --> F[Prerequisites]
    C --> G[Step-by-step explanation]
    C --> H[Worked example]
    C --> I[Visual explanation using lecture frames]
    C --> J[Common mistakes / Edge cases]
    C --> K[Complexity analysis]
    C --> L[Real-world applications]
    C --> M[Interview/examination perspective]
    C --> N[Key takeaways]
    D & E & F & G & H & I & J & K & L & M & N --> O[Complete Subtopic Explanation]
    O --> P[Quality Gates]
```

Input to this module is a single subtopic (identified by Topic Detection) together with its associated chunk data (transcript, OCR text, frame references) and the Knowledge Gap Analysis output for that subtopic. Processing consists of the single Expanded Pedagogical Prompt invocation described above. Output is the fully composed subtopic explanation, which is passed forward to Example Generation (Chapter 9) and subsequently to Quality Gates (Chapter 11) for validation.

**Rationale:** Generating explanations one subtopic at a time — rather than one lecture at a time — is what allows this stage to honor the "no length ceiling" requirement without running into the practical limits that a single, whole-lecture request would encounter. It also means that if Quality Gates later reject a specific subtopic's explanation, only that subtopic needs to be regenerated, not the entire lecture's worth of notes.

### 8.6 Advantages
- Produces genuinely standalone educational content, since each subtopic explanation is generated with the explicit instruction to teach the concept as an isolated lesson.
- Embeds visual references directly at the point of relevance, rather than as a disconnected appendix.
- Because generation is atomic per subtopic, regeneration triggered by Quality Gates is inherently cheap and localized.

### 8.7 Limitations
Because explanation length is uncapped, this stage is the primary driver of LLM token consumption in the entire pipeline — a direct consequence acknowledged in the source documentation's Technology Stack chapter, which notes that this design choice has "direct cost implications given the free-tier/quota constraints" of the LLM providers described in Chapter 15.

**Summary:** Detailed Explanation Generation is the module in which EduScribe AI's core value proposition is realized: a subtopic-level, first-principles explanation that mirrors the depth an experienced instructor would provide in office hours, rather than the compressed depth of a lecture summary.

---

## 9. Example Generation

Example Generation (Stage 6 of the Prompt Pipeline) is a dedicated stage that produces concrete examples for each concept, deliberately separated from the explanation-generation prompt described in Chapter 8.

**Objective:** The objective of this module is to ensure that every concept is reinforced through multiple types of concrete examples, based on the premise — stated directly in the source documentation — that students learn concepts more effectively through examples than through explanation text alone.

### 9.3 Detailed Explanation
For each concept, the system generates six categories of examples: simple examples, intermediate examples, real-world applications, numerical examples (when applicable to the subject matter), programming examples (when applicable), and visual examples that draw on the lecture's extracted frames. Separating example generation into its own stage — rather than embedding example instructions inside the Stage 5 explanation prompt — is explicitly justified in the source documentation as allowing future improvements to example quality without requiring any change to the explanation prompt itself.

### 9.4 Internal Workflow

```mermaid
flowchart LR
    A[Subtopic Explanation - Stage 5 output] --> B[Example Generation - Stage 6]
    B --> C[Simple Examples]
    B --> D[Intermediate Examples]
    B --> E[Real-world Applications]
    B --> F[Numerical Examples]
    B --> G[Programming Examples]
    B --> H[Visual Examples - lecture frames]
```

**Rationale:** By decoupling example generation from explanation generation, the two prompts can be independently versioned, independently improved, and independently regenerated if a Quality Gate check specifically flags a missing worked example — without risking any unintended alteration to the surrounding explanatory prose.

### 9.6 Advantages
- Guarantees consistent example coverage across every concept, since example categories are fixed and explicitly enumerated rather than left to the discretion of a single combined prompt.
- Enables independent iteration on example quality, isolated from explanation quality.
- Directly supports the Quality Gate requirement that "every algorithm should include at least one worked example" (Chapter 11), since worked-example production is this stage's explicit responsibility.

**Summary:** Example Generation operationalizes the pedagogical principle that concrete examples reinforce abstract explanation. As a stage kept structurally distinct from Detailed Explanation Generation, it allows EduScribe AI to guarantee example coverage as a first-class, independently verifiable output rather than as an incidental byproduct of the explanation prompt.

---

## 10. Image Integration

Image Integration governs how OCR-derived and frame-derived visual material — diagrams, formulas, tables, and handwritten notes — is incorporated into the generated notes.

**Objective:** The objective of this module is to ensure that visual material captured during ingestion is never discarded or listed as a disconnected appendix, but is instead referenced directly within the explanation section that discusses it.

### 10.3 Detailed Explanation
Whenever OCR extraction or frame extraction identifies a diagram, formula, table, or handwritten annotation, that visual element is referenced in the explanation text rather than being ignored. The placement rule is explicit: images are positioned beside the section of the explanation that discusses them, rather than being grouped separately at the end of the document. This directly reflects the "Multimodal Integrity" feature principle from Chapter 2 — a claim made in the generated text must be anchored to its visual source wherever a visual source exists.

### 10.4 Internal Workflow

```mermaid
flowchart TD
    A[OCR / Frame Extraction] --> B{Diagram, formula, table, or handwriting detected?}
    B -->|Yes| C[Reference frame_id in relevant explanation section]
    B -->|No| D[No visual reference required]
    C --> E["Explanation output (image placed beside discussing section)"]
```

**Rationale:** Image placement is tied to explanation relevance rather than document position (e.g., "all images at the end") because separating visual material from its discussing text would break the anchoring guarantee that every claim be traceable to source material — a requirement that governs the entire Multimodal Integrity principle.

### 10.6 Advantages
- Preserves visual context exactly where it is pedagogically useful, rather than forcing the reader to cross-reference a disconnected image appendix.
- Directly supports the Quality Gate requirement that "every important figure should reference a lecture frame" (Chapter 11).

**Summary:** Image Integration ensures that the multimodal nature of a lecture — text, speech, and visuals — is preserved through to the final generated notes, rather than being reduced to text-only output during synthesis.

---

## 11. Quality Gates

Quality Gates are automated validation checks applied to generated content before it is considered complete, ensuring that every subtopic explanation meets a fixed set of completeness criteria.

**Objective:** The objective of Quality Gates is to catch incomplete or inconsistent output automatically, and — critically — to correct it at the smallest possible granularity, avoiding the cost and risk of regenerating an entire document because a single subtopic failed a single check.

### 11.3 Detailed Explanation
Five checks are explicitly defined:

1. Every subtopic must have a complete explanation.
2. Every algorithm must include at least one worked example.
3. Every mathematical concept must include a derivation, if the lecture itself presented one.
4. Every important figure must reference a lecture frame.
5. Every generated section must be factually consistent with the transcript.

If any of these checks fails, the system regenerates **only the affected subtopic**, rather than regenerating the entire document. This is stated explicitly in the source documentation as a deliberate design choice.

### 11.4 Internal Workflow

```mermaid
flowchart TD
    A[Generated Subtopic Explanation + Examples] --> B{Complete explanation?}
    B -->|No| G[Regenerate this subtopic only]
    B -->|Yes| C{Worked example present for algorithms?}
    C -->|No| G
    C -->|Yes| D{Derivation present for math concepts?}
    D -->|No| G
    D -->|Yes| E{Figures reference a lecture frame?}
    E -->|No| G
    E -->|Yes| F{Factually consistent with transcript?}
    F -->|No| G
    F -->|Yes| H[Subtopic passes - forwarded to Markdown Generation]
    G --> A
```

**Rationale:** Quality Gates operate at subtopic granularity because Detailed Explanation Generation (Chapter 8) itself operates at subtopic granularity — the atomicity of the generation stage is what makes atomic, targeted regeneration possible at the validation stage. If explanation generation instead produced one document per lecture, Quality Gates would have no choice but to regenerate the entire document on any single failure.

### 11.6 Advantages
- Keeps correction cost proportional to the size of the defect rather than the size of the document, directly supporting the cost-efficiency non-functional requirement from Chapter 1.
- Provides a concrete, checkable definition of "complete" for a subtopic explanation, rather than relying on subjective judgment.
- The factual-consistency check anchors generated content back to the transcript, reducing the risk of hallucinated claims persisting into the final output.

**Summary:** Quality Gates are the enforcement mechanism that converts the qualitative goals of Chapters 2 and 8 (pedagogical depth, completeness, factual grounding) into a concrete, automatable, and economically efficient validation step, ensuring defects are caught and corrected at the smallest unit of work possible.

---

## 12. Markdown Generation

Markdown Generation is the final AI-facing stage of the Prompt Pipeline (Stage 9) and the first stage of the three-part export chain (Markdown → HTML → PDF).

**Objective:** The objective of this module is to convert fully validated, structured educational content into well-formatted Markdown, treating formatting as a task entirely independent of content generation.

### 12.3 Detailed Explanation
Instead of asking the LLM to generate content and formatting simultaneously, EduScribe AI treats formatting as an independent task performed after all content-generation and Quality Gate stages have completed. This separation allows the same structured content to be exported into Markdown, HTML, PDF, or other formats without requiring any change to the underlying explanation, example, or gap-analysis prompts. The final note structure that Markdown Generation assembles follows a fixed template:

```
# Lecture Title
    ## Topic
    ### Subtopic
  - Explanation
  - Illustration
  - Worked Example
  - Screenshot
  - Common Mistakes
  - Applications
  - Revision Summary
  - Key Takeaways
  - References
```

At the library level, Markdown-it-py is used as the standards-compliant Markdown rendering engine that later consumes this generated Markdown during the HTML Generation stage (Chapter 13).

### 12.4 Internal Workflow

```mermaid
flowchart LR
    A[Validated Subtopic Content - post Quality Gates] --> B[Stage 9: Markdown Generation]
    B --> C["Final Note Structure (# Lecture / ## Topic / ### Subtopic)"]
    C --> D[Markdown-it-py rendering]
    D --> E[HTML Generation]
```

**Rationale:** Treating Markdown Generation as an independently executed final AI stage — rather than instructing the explanation prompt in Chapter 8 to directly output Markdown — means that changes to document formatting conventions (heading levels, section ordering) never require regenerating or reprompting the underlying educational content.

### 12.6 Advantages
- Establishes a single, fixed note structure applied consistently across every lecture, regardless of subject matter.
- Decouples formatting logic from content-generation logic, improving maintainability, as stated directly in the source documentation for this design choice.
- Produces a format (Markdown) that is itself the direct input to the subsequent HTML Generation stage, keeping the export chain linear and dependency-ordered.

**Summary:** Markdown Generation is the bridge between EduScribe AI's content-generation pipeline and its multi-format export pipeline. By fixing the final note structure and treating formatting as a distinct concern, it ensures that Explanation, Illustration, Worked Example, Screenshot, Common Mistakes, Applications, Revision Summary, Key Takeaways, and References are assembled consistently for every subtopic, in every lecture.

---

## 13. HTML Generation

HTML Generation is the second stage of the export chain, transforming the Markdown output of Chapter 12 into styled HTML.

**Objective:** The objective of this module is to render structured Markdown notes into presentable HTML using reusable, templated layouts, rather than constructing HTML output manually for each lecture.

### 13.3 Detailed Explanation
HTML Generation relies on **Jinja2** for templating. Rather than manually constructing HTML for every generated document, Jinja2 templates define reusable layouts that the Markdown content is rendered into. This achieves a clean separation between presentation (the template) and logic/content (the generated Markdown), meaning that a visual redesign of the exported notes would only require modifying the Jinja2 templates, not the underlying content-generation pipeline.

### 13.4 Internal Workflow

```mermaid
flowchart LR
    A[Markdown Output - Chapter 12] --> B[Markdown-it-py parses Markdown]
    B --> C[Jinja2 Templates apply layout]
    C --> D[Styled HTML]
    D --> E[PDF Generation]
```

**Rationale:** Using a templating engine rather than direct string construction for HTML generation follows the same "separation of presentation and logic" principle explicitly stated in the source documentation for Jinja2's inclusion in the technology stack.

### 13.6 Advantages
- Produces clean, maintainable output by isolating layout definitions from generated content.
- Enables easy customization of the presentation layer without touching content-generation logic.
- Serves as the direct precursor input required by the PDF Generation stage (Chapter 14), which renders HTML and CSS into the final PDF.

**Summary:** HTML Generation converts the structurally fixed Markdown notes produced in Chapter 12 into a styled, presentable format, using Jinja2's templating capability to keep layout concerns fully separated from the educational content itself.

---

## 14. PDF Generation

PDF Generation is the final stage of the export chain, converting the styled HTML output of Chapter 13 into a professional, distributable PDF document.

**Objective:** The objective of this module is to produce a professional-quality PDF directly from HTML and CSS, without manually drawing PDF pages, while supporting the visual and structural elements required by educational content — images, tables, and page numbering.

### 14.3 Detailed Explanation
PDF Generation uses **WeasyPrint**, which renders HTML and CSS directly into a PDF document. The source documentation specifically credits WeasyPrint with excellent typography support, CSS support, and the ability to render images, tables, page numbering, and a table of contents — all of which are directly relevant to the structure of an educational document containing worked examples, screenshots, and multi-level headings (Lecture → Topic → Subtopic).

### 14.4 Internal Workflow

```mermaid
flowchart LR
    A[Styled HTML - Chapter 13] --> B[WeasyPrint: HTML + CSS rendering]
    B --> C["Professional PDF (typography, images, tables, page numbers, TOC)"]
```

**Rationale:** Rendering directly from HTML/CSS rather than constructing the PDF programmatically (page by page) allows the same styling investment made for the HTML export (Chapter 13) to be directly reused for the PDF export, rather than requiring a second, independent layout implementation.

### 14.6 Advantages
- Produces a polished, professional final artifact suitable for distribution or offline study.
- Reuses the HTML/CSS styling layer already established for the HTML export target, avoiding duplicated layout work.
- Supports the structural elements (tables, page numbering, table of contents) that a multi-topic, multi-subtopic educational document requires.

**Summary:** PDF Generation is the terminal stage of EduScribe AI's End-to-End Pipeline, converting the fully validated, formatted, and styled educational content into the polished, final deliverable that the student ultimately receives.

---

## 15. LLM Provider Architecture

EduScribe AI never allows any application code to call an LLM vendor directly. Every request passes through a centralized **LLM Manager** that selects the correct provider and model for the task, manages API keys, enforces rate limits, and executes automatic failover — transparently, without any change required to the business logic that triggered the request.

**Objective:** Guarantee high availability, vendor independence, and zero-cost operation across the entire note-generation pipeline by routing all LLM calls through an abstraction layer that understands the free-tier constraints of every integrated provider.

---

### 15.1 The Four-Tier Waterfall

EduScribe AI organizes all integrated providers into a strict priority waterfall. The system always starts at Tier 1 and only descends to the next tier when the current tier's available quota is exhausted or returns a non-retriable error.

```
┌─────────────────────────────────────────────────────────┐
│  TIER 1 — PREMIUM FREE                                  │
│  Best quality · Large context · Primary default         │
│  Gemini 2.5 Flash → Gemini 2.5 Flash Lite              │
│  DeepSeek V3 (OpenRouter :free) → Qwen3 235B (OR :free) │
└──────────────────────┬──────────────────────────────────┘
                       │ quota exhausted / 429
┌──────────────────────▼──────────────────────────────────┐
│  TIER 2 — HIGH-SPEED FREE                               │
│  Very low latency · Ultra-fast token generation          │
│  Cerebras/Llama-4-Scout → Cerebras/Qwen3-32B            │
│  Groq/Llama-3.3-70B → Groq/Qwen3-32B                   │
└──────────────────────┬──────────────────────────────────┘
                       │ quota exhausted / 429
┌──────────────────────▼──────────────────────────────────┐
│  TIER 3 — OPEN-SOURCE BACKUP                            │
│  Reliable · Stable · Broad model selection               │
│  Together AI (Llama-3.3-70B-Free) → SambaNova           │
│  Mistral Small 3.2 (Free tier)                          │
└──────────────────────┬──────────────────────────────────┘
                       │ quota exhausted / 429
┌──────────────────────▼──────────────────────────────────┐
│  TIER 4 — EMERGENCY FALLBACK                            │
│  Last resort · Smallest free models still capable        │
│  OpenRouter/free router → Groq/Llama-3.1-8B             │
│  Mistral/open-mistral-nemo                              │
└─────────────────────────────────────────────────────────┘
```

Within each tier, providers are tried left-to-right. Within each provider, all available API keys are rotated before the provider is declared exhausted (see Chapter 21).

---

### 15.2 Integrated Provider Reference (2025/2026)

> [!IMPORTANT]
> All providers below are used on their **free tiers only**. Rate limits change periodically — always verify against the official provider dashboard. Limits marked *(verify)* are approximate and should be checked at sign-up.

| Provider | Free Tier | Models (LiteLLM ID) | RPM | RPD | Context | Latency | Tier |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **Google AI Studio** | Yes — no card needed | `gemini/gemini-2.5-flash`, `gemini/gemini-2.5-flash-lite-preview-06-17` | ~15 *(verify)* | ~1,500 *(verify)* | 1M tokens | Medium | 1 |
| **OpenRouter** | Yes — 50 RPD free; 1,000 RPD after $10 lifetime purchase | `openrouter/deepseek/deepseek-chat:free`, `openrouter/deepseek/deepseek-r1:free`, `openrouter/qwen/qwen3-235b-a22b:free`, `openrouter/meta-llama/llama-3.3-70b-instruct:free`, `openrouter/qwen/qwen3-coder-480b-a35b:free` | 20 | 50 / 1,000† | Model-dependent (up to 128K) | Medium | 1 / 3 |
| **Cerebras** | Yes — no card needed | `cerebras/llama-4-scout-17b-16e-instruct`, `cerebras/qwen3-32b`, `cerebras/qwen3-235b-a22b` | 30 | Unlimited‡ | 64K–128K | **Ultra-fast** (2,600+ tok/s) | 2 |
| **Groq** | Yes — no card needed | `groq/llama-3.3-70b-versatile`, `groq/qwen3-32b`, `groq/llama-3.1-8b-instant`, `groq/deepseek-r1-distill-llama-70b` | 30 | 14,400 | 128K | **Very fast** | 2 |
| **Together AI** | Trial credits on signup; then free models perpetually | `together_ai/meta-llama/Llama-3.3-70B-Instruct-Turbo-Free`, `together_ai/deepseek-ai/DeepSeek-R1-Distill-Llama-70B-Free` | Dynamic | Dynamic | 128K | Medium | 3 |
| **SambaNova** | $5 credits on signup; Dev tier with high limits | `sambanova/Meta-Llama-3.3-70B-Instruct`, `sambanova/DeepSeek-R1` | *(verify)* | 20M tokens/day | 128K | **Fast** | 3 |
| **Mistral (la Plateforme)** | Yes — Experiment plan free | `mistral/mistral-small-latest`, `mistral/open-mistral-nemo`, `mistral/codestral-latest` | ~30 (1 RPS) | Unspecified | 128K | Medium | 3 / 4 |

**†** OpenRouter free-model RPD: 50/day without any purchase; rises to 1,000/day after a one-time $10 lifetime credit purchase — strongly recommended.

**‡** Cerebras enforces a 1M token/day soft limit on the free tier; RPD is not separately capped.

> [!NOTE]
> **Removed from original doc:** Zhipu AI (GLM) — no longer offers a meaningful free public API tier as of 2025. **Not included:** Fireworks AI, Novita AI — free credits exhaust quickly and are not suitable as reliable fallback tiers for a production platform.

---

### 15.3 Folder Structure

```
backend/
└── app/
    └── services/
        └── llm/
            ├── base_provider.py          # Abstract BaseProvider interface
            ├── providers/
            │   ├── google_provider.py    # Gemini 2.5 Flash / Flash Lite
            │   ├── openrouter_provider.py
            │   ├── cerebras_provider.py
            │   ├── groq_provider.py
            │   ├── together_provider.py
            │   ├── sambanova_provider.py
            │   └── mistral_provider.py
            ├── llm_manager.py            # Tier waterfall orchestrator
            ├── model_selector.py         # Task → model ID routing
            ├── fallback_manager.py       # Inter-provider fallback logic
            ├── key_manager.py            # Per-provider API key rotation
            ├── retry_manager.py          # Intra-provider retry (exponential backoff)
            ├── quota_tracker.py          # Redis-backed quota state per provider
            └── response_parser.py        # Normalise + validate raw LLM responses
```

Every external call flows through this stack in one direction: `llm_manager` → `model_selector` → `key_manager` → `LiteLLM` → provider API. No component outside this directory ever calls an LLM directly.

---

### 15.4 Internal Workflow

```mermaid
flowchart TD
    A[Notes Service / RAG Service / Quiz Service] --> B[LLM Manager]
    B --> C{Quota Tracker: which tier has quota?}
    C -->|Tier 1 available| D[Tier 1: Gemini / OpenRouter]
    C -->|Tier 1 exhausted| E[Tier 2: Cerebras / Groq]
    C -->|Tier 2 exhausted| F[Tier 3: Together / SambaNova / Mistral]
    C -->|Tier 3 exhausted| G[Tier 4: Emergency models]
    C -->|All exhausted| H[Queue request — retry after quota reset]
    D & E & F & G --> I[Key Manager: rotate API key if 429]
    I --> J[LiteLLM Proxy: unified call]
    J --> K[Provider API]
    K -->|200 OK| L[Response Parser + PydanticAI Validation]
    K -->|429 / 5xx| M[Retry Manager: exponential backoff × 3]
    M -->|still failing| N[Fallback Manager: next provider in tier]
    L --> O[Structured Response → calling service]
```

**Rationale:** The waterfall descends only when necessary, so the highest-quality provider (Gemini 2.5 Flash, 1M context) handles the majority of requests. Tier 2 (Cerebras/Groq) handles overflow efficiently with ultra-low latency. Tiers 3–4 ensure the pipeline never halts entirely — even if Tiers 1 and 2 are fully exhausted.

### 15.5 Advantages

- **Zero spend:** The entire stack operates within free-tier limits. No credit card processing ever hits the application's budget.
- **5-tier resilience:** Even simultaneous exhaustion of Tiers 1–3 still leaves emergency fallback models available.
- **Vendor independence:** All providers are swappable by changing a configuration value, not application code.
- **Quota-aware scheduling:** The Quota Tracker (backed by Redis) prevents unnecessary calls to exhausted providers, conserving rate-limit headroom for higher-priority requests.
- **Per-key rotation:** Multiple API keys per provider extend effective daily quota (see Chapter 21).

### 15.6 Limitations

The free-tier-only constraint means the system is subject to:
- **Daily quota ceilings** — once all providers exhaust their daily allowance, the pipeline queues requests until midnight UTC when most quotas reset.
- **Rate limit collisions** — at high concurrency (multiple students uploading lectures simultaneously), Tier 1 RPM limits (15–30 RPM) can be hit within minutes, requiring rapid descent to Tier 2/3.
- **Dynamic limit changes** — providers adjust free-tier limits without notice; the `quota_tracker.py` must be updated accordingly.

**Summary:** The LLM Provider Architecture converts EduScribe AI from a fragile single-vendor dependency into a resilient 4-tier free-tier waterfall, where no single provider's quota exhaustion can halt the note-generation pipeline.

---

## 16. LiteLLM

LiteLLM is the unified communication layer that translates every LLM call — regardless of which provider is active — into a single consistent format, eliminating provider-specific code from the application entirely.

**Objective:** Provide a single Python call that works for Gemini, OpenRouter, Cerebras, Groq, Together AI, SambaNova, and Mistral without any branching logic in application code, while also providing built-in retry, fallback, cost tracking, and context-window management.

---

### 16.1 What LiteLLM Provides

| Feature | LiteLLM Capability |
| --- | --- |
| Unified API | One function call works for 100+ providers |
| Built-in retries | `num_retries=3` with automatic exponential backoff |
| Built-in fallbacks | `fallbacks=[...]` — switches model on failure |
| Context window fallback | Routes to larger model if prompt exceeds context limit |
| Cost tracking | Records token usage + estimated cost per call |
| Streaming | `stream=True` works identically across all providers |
| Async support | `await litellm.acompletion(...)` for FastAPI/Celery |
| Langfuse integration | `success_callback=["langfuse"]` — zero extra code |

---

### 16.2 LiteLLM Proxy (Self-Hosted Gateway)

Beyond the Python SDK, LiteLLM ships a **Proxy** — a self-hosted HTTP gateway (Docker container, open-source, free) that acts as a drop-in replacement for the OpenAI API endpoint. The entire EduScribe AI backend routes all LLM calls through this proxy rather than calling provider APIs directly.

**Why use the Proxy instead of the SDK directly:**

| Concern | SDK only | LiteLLM Proxy |
| --- | --- | --- |
| API key management | In application code | Centralised in proxy config |
| Key rotation | Manual | Automatic per-provider |
| Rate limiting | Application-level | Proxy-level (before request leaves server) |
| Cost enforcement | None | Hard budget limits per key/user |
| Logging | Scattered | Centralised to Langfuse / Prometheus |
| Fallback chain | Per-call config | Single `config.yaml` governs all calls |
| Multi-worker support | Each Celery worker needs keys | All workers share one proxy endpoint |

**Proxy architecture for EduScribe AI:**

```
Celery Workers (N instances)
        │
        ▼  POST http://litellm-proxy:4000/chat/completions
  LiteLLM Proxy (Docker, localhost or sidecar)
        │
        ├── Key Manager (rotates per-provider keys)
        ├── Budget Tracker (blocks calls if daily limit hit)
        ├── Fallback Router (waterfall per config.yaml)
        ├── Langfuse Logger (token + cost + latency)
        └── Provider APIs (Gemini, OpenRouter, Groq, etc.)
```

**Minimal `config.yaml` for EduScribe AI:**

```yaml
model_list:
  # Tier 1 — Primary
  - model_name: gemini-flash
    litellm_params:
      model: gemini/gemini-2.5-flash
      api_key: os.environ/GOOGLE_API_KEY_1

  - model_name: deepseek-v3
    litellm_params:
      model: openrouter/deepseek/deepseek-chat:free
      api_key: os.environ/OPENROUTER_API_KEY

  - model_name: qwen3-235b
    litellm_params:
      model: openrouter/qwen/qwen3-235b-a22b:free
      api_key: os.environ/OPENROUTER_API_KEY

  # Tier 2 — High-speed
  - model_name: cerebras-llama4
    litellm_params:
      model: cerebras/llama-4-scout-17b-16e-instruct
      api_key: os.environ/CEREBRAS_API_KEY

  - model_name: groq-llama33
    litellm_params:
      model: groq/llama-3.3-70b-versatile
      api_key: os.environ/GROQ_API_KEY_1

  # Tier 3 — Backup
  - model_name: together-llama33
    litellm_params:
      model: together_ai/meta-llama/Llama-3.3-70B-Instruct-Turbo-Free
      api_key: os.environ/TOGETHER_API_KEY

  - model_name: mistral-small
    litellm_params:
      model: mistral/mistral-small-latest
      api_key: os.environ/MISTRAL_API_KEY

  # Tier 4 — Emergency
  - model_name: groq-llama8b
    litellm_params:
      model: groq/llama-3.1-8b-instant
      api_key: os.environ/GROQ_API_KEY_1

router_settings:
  routing_strategy: usage-based-routing
  num_retries: 3
  retry_after: 5
  fallbacks:
    - gemini-flash: [deepseek-v3, qwen3-235b, cerebras-llama4, groq-llama33, together-llama33, mistral-small, groq-llama8b]
    - deepseek-v3: [qwen3-235b, cerebras-llama4, groq-llama33, mistral-small, groq-llama8b]

litellm_settings:
  success_callback: ["langfuse"]
  failure_callback: ["langfuse"]
  request_timeout: 120
  drop_params: true

general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY
  database_url: os.environ/DATABASE_URL
```

---

### 16.3 Usage in Application Code

With the proxy running, application code never imports a provider SDK. Every call is a standard HTTP request or a single LiteLLM Python call:

```python
import litellm

# Async call (used in FastAPI endpoints + Celery tasks)
response = await litellm.acompletion(
    model="gemini-flash",          # proxy model alias
    messages=[
        {"role": "system", "content": system_prompt},
        {"role": "user",   "content": user_prompt},
    ],
    temperature=0.3,
    max_tokens=4096,
    fallbacks=["deepseek-v3", "groq-llama33"],   # override per-call if needed
    num_retries=3,
    timeout=90,
    metadata={"task": "topic_detection", "lecture_id": lecture_id},
)

content = response.choices[0].message.content
tokens_used = response.usage.total_tokens
```

Changing from Gemini to DeepSeek as the default requires only changing the `model=` string — no other code changes anywhere in the application.

---

### 16.4 Internal Workflow

```mermaid
sequenceDiagram
    participant CW as Celery Worker
    participant LP as LiteLLM Proxy
    participant KM as Key Manager
    participant BT as Budget Tracker
    participant PR as Provider API
    participant LF as Langfuse

    CW->>LP: POST /chat/completions {model: "gemini-flash", messages: [...]}
    LP->>BT: Check: daily budget remaining for gemini-flash?
    BT-->>LP: OK — budget available
    LP->>KM: Get next active API key for provider
    KM-->>LP: GOOGLE_API_KEY_2
    LP->>PR: Forward request to Gemini API
    PR-->>LP: 429 Too Many Requests
    LP->>LP: Retry × 3 with exponential backoff (5s, 10s, 20s)
    LP->>LP: All retries exhausted → activate fallback chain
    LP->>PR: Forward to deepseek-v3 (OpenRouter)
    PR-->>LP: 200 OK {content: "..."}
    LP->>LF: Log: model, tokens, latency, cost, task metadata
    LP-->>CW: Return normalised response
```

**Rationale:** By centralising key rotation, budget tracking, and fallback logic in the proxy rather than in application code, every Celery worker benefits from the same routing intelligence without duplicating it. A new Celery worker starting up automatically inherits the correct routing rules from the shared proxy.

### 16.5 Advantages

- Single proxy config file governs the entire fallback waterfall — no per-task fallback logic scattered across service files.
- Built-in Langfuse callback provides zero-code observability for every LLM call.
- Context window fallback (`context_window_fallback_dict`) automatically upgrades to Gemini 2.5 Flash (1M context) if a smaller model's context limit is exceeded.
- All Celery workers share one proxy — API keys are never duplicated across worker processes.

**Summary:** LiteLLM Proxy converts the 4-tier provider waterfall from a design principle into a single, declarative `config.yaml` that governs every LLM call made by every Celery worker in EduScribe AI.

---

## 17. PydanticAI

PydanticAI is the schema enforcement layer that sits between LiteLLM's raw response and the application's business logic, guaranteeing that every LLM output conforms to a predefined schema before it propagates further.

**Objective:** Convert LLM output from "probably structured JSON" to "guaranteed schema-compliant typed objects," preventing malformed responses from silently corrupting the Notes Generator, Markdown Generation, or Quality Gate stages downstream.

---

### 17.1 The Gap LiteLLM Leaves Open

LiteLLM guarantees a **uniform call format** across providers — but it does not validate the **content** of the response. A model might return:

- Valid JSON with a missing required field (`subtopics` omitted)
- Correct JSON schema but wrong data type (`"difficulty": "hard"` instead of `"difficulty": 3`)
- A markdown explanation block instead of JSON (common when temperature is too high)
- Truncated JSON when the model hits its `max_tokens` ceiling

PydanticAI catches all four failure modes before the response reaches any downstream stage.

---

### 17.2 Schema Definitions for All Pipeline Stages

Every stage of the Prompt Pipeline (Chapter 3.2) has a corresponding Pydantic model. These models are the contracts that every LLM call in EduScribe AI must satisfy:

```python
from pydantic import BaseModel, Field
from typing import Optional

# Stage 1 — Lecture Analysis
class LectureAnalysis(BaseModel):
    subject: str
    difficulty: int = Field(ge=1, le=5)          # 1 = beginner, 5 = expert
    prerequisites: list[str]
    teaching_style: str
    primary_objectives: list[str]
    estimated_topics: int = Field(ge=1)

# Stage 2 — Topic Detection
class Topic(BaseModel):
    title: str
    start_time: str                              # "HH:MM:SS"
    end_time: str
    confidence: float = Field(ge=0.0, le=1.0)

class TopicList(BaseModel):
    topics: list[Topic]

# Stage 3 — Subtopic Detection
class SubtopicList(BaseModel):
    topic: str
    subtopics: list[str] = Field(min_length=1)

# Stage 4 — Knowledge Gap Analysis
class KnowledgeGap(BaseModel):
    missing_definitions: list[str]
    missing_prerequisites: list[str]
    implicit_reasoning: list[str]
    potential_misconceptions: list[str]

# Stage 5 — Explanation Generation (free text — validated for completeness)
class SubtopicExplanation(BaseModel):
    subtopic: str
    definition: str
    motivation: str
    step_by_step: str
    worked_example: str
    common_mistakes: list[str]
    key_takeaways: list[str]
    frame_references: list[str]                  # OCR frame IDs
    word_count: Optional[int] = None

# Stage 6 — Example Generation
class ExampleSet(BaseModel):
    subtopic: str
    simple_examples: list[str]
    intermediate_examples: list[str]
    real_world_applications: list[str]
    numerical_examples: Optional[list[str]] = None
    programming_examples: Optional[list[str]] = None
    visual_examples: Optional[list[str]] = None
```

---

### 17.3 Validation Flow and Auto-Retry

PydanticAI does not just validate — it **retries automatically** when validation fails, sending the error back to the LLM with a correction instruction before escalating to the fallback chain:

```python
from pydantic_ai import Agent
from pydantic_ai.models.litellm import LiteLLMModel

model = LiteLLMModel(model_id="gemini-flash")   # uses the proxy alias

agent = Agent(
    model=model,
    result_type=SubtopicList,     # the expected schema
    system_prompt=SUBTOPIC_DETECTION_SYSTEM_PROMPT,
    result_retries=3,             # retry LLM call up to 3× on schema failure
)

result = await agent.run(topic_prompt)
subtopics: SubtopicList = result.data   # guaranteed schema-compliant
```

**Retry sequence on validation failure:**
1. Model returns malformed JSON → PydanticAI catches `ValidationError`
2. PydanticAI sends a second request: original prompt + validation error description
3. Model corrects and returns valid JSON → PydanticAI validates again
4. If still failing after `result_retries=3` → raises `UnexpectedModelBehavior`
5. `UnexpectedModelBehavior` → LiteLLM activates fallback chain (next provider)

---

### 17.4 Internal Workflow

```mermaid
flowchart TD
    A[LLM Response via LiteLLM] --> B{PydanticAI: schema validate}
    B -->|Valid| C[Typed result object → downstream stage]
    B -->|Invalid: missing field / wrong type / malformed JSON| D[Auto-retry with error context]
    D --> E{Retry attempt ≤ 3?}
    E -->|Yes| F[Re-send prompt + validation error to same model]
    F --> B
    E -->|No: retries exhausted| G[Raise UnexpectedModelBehavior]
    G --> H[LiteLLM activates fallback → next provider]
    H --> B
```

**Rationale:** Positioning PydanticAI between LiteLLM and business logic enforces a clean contract: upstream code handles provider complexity, downstream code handles domain logic, and PydanticAI guarantees the handoff between them is always type-safe.

### 17.5 Advantages

- **Fail-fast:** Malformed responses are caught immediately at the LLM boundary, not silently propagated into generated notes.
- **Self-correcting:** `result_retries` gives the model a second chance with explicit error feedback before escalating — avoiding unnecessary provider switches for simple formatting errors.
- **Typed throughout:** Every downstream function receives a strongly typed object, not a raw string. IDEs catch schema misuse at development time, not at runtime.
- **Provider-agnostic:** PydanticAI's `LiteLLMModel` adapter works with any model behind the proxy, so the validation layer never needs to change when the underlying provider changes.

**Summary:** PydanticAI is the last line of defence between the unpredictable nature of LLM text output and the schema-dependent stages (Markdown Generation, Quality Gates, Knowledge Tree assembly) that depend on structured data being correct.

---

## 18. Model Routing

Model Routing is the mechanism by which EduScribe AI assigns a specific model — identified by its exact LiteLLM model ID — to each of the nine task types it executes, rather than routing everything through a single default model.

**Objective:** Match task complexity to model capability so that free-tier quota is spent efficiently — lightweight formatting tasks never consume the same quota as deep pedagogical explanation generation.

---

### 18.1 Core Routing Principles

Three rules govern all routing decisions:

1. **Task complexity determines tier.** Structural tasks (JSON extraction, formatting) route to fast, light models. Deep content generation routes to the most capable available model.
2. **Quality first, quota second.** The system always attempts the highest-quality option first. Quota-awareness is handled by the LiteLLM Proxy and Quota Tracker — the routing table specifies intent, not constraint.
3. **Every task has three options.** Every task type has a `primary`, `secondary`, and `emergency` model. This gives `model_selector.py` concrete fallback targets without requiring runtime decisions.

---

### 18.2 Task-to-Model Routing Table

The values in the **Primary**, **Secondary**, and **Emergency** columns are exact LiteLLM model IDs (or proxy aliases) usable directly in `model_selector.py`.

| Task | Primary | Secondary | Emergency | Temp | Max Tokens |
| --- | --- | --- | --- | --- | --- |
| **Lecture Analysis** | `gemini-flash` | `deepseek-v3` | `groq-llama33` | 0.1 | 2,048 |
| **Topic Detection** | `gemini-flash` | `deepseek-v3` | `groq-llama33` | 0.1 | 1,024 |
| **Subtopic Detection** | `gemini-flash` | `deepseek-v3` | `groq-llama33` | 0.1 | 1,024 |
| **Knowledge Gap Analysis** | `gemini-flash` | `deepseek-v3` | `groq-llama33` | 0.2 | 2,048 |
| **Detailed Notes (General)** | `gemini-flash` | `deepseek-v3` | `groq-llama33` | 0.4 | 8,192 |
| **Programming Explanation** | `deepseek-v3` | `gemini-flash` | `groq-llama33` | 0.3 | 8,192 |
| **Mathematics / Proofs** | `qwen3-235b` | `deepseek-r1` | `groq/deepseek-r1-distill-llama-70b` | 0.1 | 8,192 |
| **Example Generation** | `gemini-flash` | `deepseek-v3` | `groq-llama33` | 0.5 | 4,096 |
| **Markdown Formatting** | `groq-llama33` | `gemini/gemini-2.5-flash-lite-preview-06-17` | `groq-llama8b` | 0.1 | 4,096 |
| **Quiz Generation** | `gemini-flash` | `deepseek-v3` | `groq-llama33` | 0.4 | 4,096 |
| **Flashcard Generation** | `gemini-flash` | `deepseek-v3` | `cerebras-llama4` | 0.3 | 2,048 |
| **Mind Map Generation** | `gemini-flash` | `deepseek-v3` | `cerebras-llama4` | 0.2 | 2,048 |
| **RAG Answering** | `gemini-flash` | `deepseek-v3` | `groq-llama33` | 0.3 | 4,096 |
| **OCR Correction** | `gemini-flash` | `deepseek-v3` | `groq-llama33` | 0.1 | 2,048 |
| **Code Generation (standalone)** | `deepseek-v3` | `openrouter/qwen/qwen3-coder-480b-a35b:free` | `groq-llama33` | 0.2 | 8,192 |
| **JSON Extraction** | `gemini-flash` | `groq-llama33` | `cerebras-llama4` | 0.0 | 1,024 |

**Key routing rationale:**
- **Gemini 2.5 Flash** is primary for most tasks: best structured output, 1M context, generous free tier.
- **DeepSeek V3** is primary for programming: consistently top-rated for code and Markdown formatting in MTEB/BigCodeBench evaluations.
- **Qwen3 235B** is primary for mathematics: highest mathematical reasoning scores (MATH-500, AIME) among open-weight free models.
- **DeepSeek R1** is secondary for mathematics: native chain-of-thought reasoning for proof derivation.
- **Groq/Llama-3.3-70B** handles Markdown formatting and JSON extraction — tasks that require no deep reasoning, just fast, reliable text transformation at 14,400 RPD.
- **Cerebras** handles flashcards and mind maps where response speed matters more than depth.

---

### 18.3 Routing Decision Workflow

```mermaid
flowchart TD
    A[Incoming task] --> B{model_selector.py: determine task type}
    B --> C[Look up primary model in routing table]
    C --> D{Quota Tracker: primary model has quota?}
    D -->|Yes| E[Call primary via LiteLLM Proxy]
    D -->|No| F[Look up secondary model]
    F --> G{Secondary has quota?}
    G -->|Yes| H[Call secondary via LiteLLM Proxy]
    G -->|No| I[Call emergency model]
    E & H & I --> J{Response valid?}
    J -->|Yes| K[Return to pipeline]
    J -->|No: 429 / schema fail| L[LiteLLM Proxy: activate fallback chain]
    L --> J
```

---

### 18.4 `model_selector.py` — Reference Implementation

```python
from enum import Enum
from dataclasses import dataclass

class TaskType(Enum):
    LECTURE_ANALYSIS     = "lecture_analysis"
    TOPIC_DETECTION      = "topic_detection"
    SUBTOPIC_DETECTION   = "subtopic_detection"
    KNOWLEDGE_GAP        = "knowledge_gap"
    DETAILED_NOTES       = "detailed_notes"
    PROGRAMMING_EXPLAIN  = "programming_explanation"
    MATHEMATICS          = "mathematics"
    EXAMPLE_GENERATION   = "example_generation"
    MARKDOWN_FORMAT      = "markdown_format"
    QUIZ_GENERATION      = "quiz_generation"
    FLASHCARD_GENERATION = "flashcard_generation"
    MIND_MAP_GENERATION  = "mind_map"
    RAG_ANSWERING        = "rag_answering"
    OCR_CORRECTION       = "ocr_correction"
    CODE_GENERATION      = "code_generation"
    JSON_EXTRACTION      = "json_extraction"

@dataclass
class ModelConfig:
    primary:   str
    secondary: str
    emergency: str
    temperature: float
    max_tokens: int

ROUTING_TABLE: dict[TaskType, ModelConfig] = {
    TaskType.LECTURE_ANALYSIS:     ModelConfig("gemini-flash",  "deepseek-v3",  "groq-llama33",                       0.1, 2048),
    TaskType.TOPIC_DETECTION:      ModelConfig("gemini-flash",  "deepseek-v3",  "groq-llama33",                       0.1, 1024),
    TaskType.SUBTOPIC_DETECTION:   ModelConfig("gemini-flash",  "deepseek-v3",  "groq-llama33",                       0.1, 1024),
    TaskType.KNOWLEDGE_GAP:        ModelConfig("gemini-flash",  "deepseek-v3",  "groq-llama33",                       0.2, 2048),
    TaskType.DETAILED_NOTES:       ModelConfig("gemini-flash",  "deepseek-v3",  "groq-llama33",                       0.4, 8192),
    TaskType.PROGRAMMING_EXPLAIN:  ModelConfig("deepseek-v3",   "gemini-flash", "groq-llama33",                       0.3, 8192),
    TaskType.MATHEMATICS:          ModelConfig("qwen3-235b",    "deepseek-r1",  "groq/deepseek-r1-distill-llama-70b", 0.1, 8192),
    TaskType.EXAMPLE_GENERATION:   ModelConfig("gemini-flash",  "deepseek-v3",  "groq-llama33",                       0.5, 4096),
    TaskType.MARKDOWN_FORMAT:      ModelConfig("groq-llama33",  "gemini/gemini-2.5-flash-lite-preview-06-17", "groq-llama8b", 0.1, 4096),
    TaskType.QUIZ_GENERATION:      ModelConfig("gemini-flash",  "deepseek-v3",  "groq-llama33",                       0.4, 4096),
    TaskType.FLASHCARD_GENERATION: ModelConfig("gemini-flash",  "deepseek-v3",  "cerebras-llama4",                    0.3, 2048),
    TaskType.MIND_MAP_GENERATION:  ModelConfig("gemini-flash",  "deepseek-v3",  "cerebras-llama4",                    0.2, 2048),
    TaskType.RAG_ANSWERING:        ModelConfig("gemini-flash",  "deepseek-v3",  "groq-llama33",                       0.3, 4096),
    TaskType.OCR_CORRECTION:       ModelConfig("gemini-flash",  "deepseek-v3",  "groq-llama33",                       0.1, 2048),
    TaskType.CODE_GENERATION:      ModelConfig("deepseek-v3",   "openrouter/qwen/qwen3-coder-480b-a35b:free", "groq-llama33", 0.2, 8192),
    TaskType.JSON_EXTRACTION:      ModelConfig("gemini-flash",  "groq-llama33", "cerebras-llama4",                    0.0, 1024),
}

def get_model_config(task: TaskType) -> ModelConfig:
    return ROUTING_TABLE[task]
```

---

### 18.5 Internal Workflow (Routing)

```mermaid
flowchart TD
    A[Task: Subtopic Detection] --> B[get_model_config TaskType.SUBTOPIC_DETECTION]
    B --> C["ModelConfig: primary=gemini-flash, temp=0.1, max_tokens=1024"]
    C --> D[quota_tracker.has_quota gemini-flash?]
    D -->|Yes| E["litellm.acompletion model=gemini-flash, temperature=0.1, max_tokens=1024"]
    D -->|No| F["litellm.acompletion model=deepseek-v3 (secondary)"]
    E & F --> G[PydanticAI: validate SubtopicList schema]
    G -->|Valid| H[Return SubtopicList to pipeline]
    G -->|Invalid| I[PydanticAI retry × 3 → fallback chain]
```

**Rationale:** Separating the routing table from the call logic means changes to model selection never require changes to prompt engineering or validation code. A new model can be added by updating `ROUTING_TABLE` alone.

### 18.6 Advantages

- **Concrete and executable:** The routing table uses real LiteLLM model IDs, not vague model family names. `model_selector.py` can be used directly without any additional translation step.
- **Cost-optimised by design:** Formatting and JSON extraction tasks use fast Groq/Cerebras models (14,400+ RPD) instead of consuming Gemini's more limited quota.
- **Domain-aware:** Mathematics routes to Qwen3 235B and DeepSeek R1 — the two highest-scoring models on MATH-500 and AIME benchmarks available on free tiers.

---

### 18.7 Model Quality Reference

The routing table in Section 18.2 defines *which* model to use by task type. This section provides the evidence base — ratings across 21 educational task categories — that justifies every routing decision. All models below are accessible on free tiers via the integrated providers.

#### 18.7.1 Structured Note Generation

> Best for: lecture notes, chapter notes, deep explanations, summaries.

| Model | Quality | Free Tier | Provider |
| --- | --- | --- | --- |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ | ✅ | Google AI Studio |
| DeepSeek V3 | ⭐⭐⭐⭐⭐ | ✅ | OpenRouter :free |
| Qwen3 235B A22B | ⭐⭐⭐⭐⭐ | ✅ | OpenRouter :free |
| Llama 3.3 70B | ⭐⭐⭐⭐☆ | ✅ | Groq / Together AI |
| Mistral Small 3.2 | ⭐⭐⭐⭐☆ | ✅ | Mistral la Plateforme |

**Primary:** Gemini 2.5 Flash · DeepSeek V3 · Qwen3 235B

---

#### 18.7.2 Mathematics (Step-by-Step)

| Model | Proof Quality | Reasoning | Free Tier |
| --- | --- | --- | --- |
| Qwen3 235B | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ |
| DeepSeek R1 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ✅ |
| Llama 3.3 70B | ⭐⭐⭐⭐☆ | ⭐⭐⭐⭐☆ | ✅ |

**Primary:** Qwen3 235B · DeepSeek R1

---

#### 18.7.3 Formula Derivation and Proof

> Good for: Calculus, Linear Algebra, Statistics, Physics derivations, Algorithm proofs.

| Model | Quality |
| --- | --- |
| DeepSeek R1 | ⭐⭐⭐⭐⭐ |
| Qwen3 235B | ⭐⭐⭐⭐⭐ |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ |
| Llama 3.3 70B | ⭐⭐⭐⭐☆ |

---

#### 18.7.4 Code Generation

| Model | Quality | Notes |
| --- | --- | --- |
| DeepSeek V3 | ⭐⭐⭐⭐⭐ | Consistently top on HumanEval, SWE-bench |
| Qwen3 Coder 480B | ⭐⭐⭐⭐⭐ | Largest open-weight code model; free on OpenRouter |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ | Strong across Python, JS, SQL |
| Llama 3.3 70B | ⭐⭐⭐⭐☆ | |
| Codestral (Mistral) | ⭐⭐⭐⭐☆ | Free on Mistral la Plateforme |

**Primary:** DeepSeek V3 · Qwen3 Coder 480B · Gemini 2.5 Flash

---

#### 18.7.5 Code Explanation

| Model | Quality |
| --- | --- |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ |
| DeepSeek V3 | ⭐⭐⭐⭐⭐ |
| Qwen3 235B | ⭐⭐⭐⭐⭐ |
| Llama 3.3 70B | ⭐⭐⭐⭐☆ |

---

#### 18.7.6 Example Generation (Real-World)

> Good for: OOP, Machine Learning, Networks, Databases, Operating Systems.

| Model | Quality |
| --- | --- |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ |
| DeepSeek V3 | ⭐⭐⭐⭐⭐ |
| Qwen3 235B | ⭐⭐⭐⭐⭐ |
| Mistral Small 3.2 | ⭐⭐⭐⭐☆ |

---

#### 18.7.7 Quiz Generation

> Can generate: MCQ, True/False, Fill-in-the-blank, HOTS questions, University-style exams.

| Model | Quality |
| --- | --- |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ |
| DeepSeek V3 | ⭐⭐⭐⭐⭐ |
| Qwen3 235B | ⭐⭐⭐⭐⭐ |
| Llama 3.3 70B | ⭐⭐⭐⭐☆ |

---

#### 18.7.8 Flashcard Generation

| Model | Quality |
| --- | --- |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ |
| DeepSeek V3 | ⭐⭐⭐⭐⭐ |
| Cerebras/Llama-4-Scout | ⭐⭐⭐⭐☆ | Ultra-fast for bulk generation |
| Qwen3 235B | ⭐⭐⭐⭐⭐ |

---

#### 18.7.9 Mind Map Generation

> Output formats: Mermaid, Markdown trees, Nested bullet structures.

| Model | Quality |
| --- | --- |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ |
| DeepSeek V3 | ⭐⭐⭐⭐⭐ |
| Qwen3 235B | ⭐⭐⭐⭐⭐ |

---

#### 18.7.10 Markdown Notes

| Model | Quality |
| --- | --- |
| DeepSeek V3 | ⭐⭐⭐⭐⭐ |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ |
| Qwen3 235B | ⭐⭐⭐⭐⭐ |
| Groq/Llama-3.3-70B | ⭐⭐⭐⭐☆ | Best for fast formatting-only tasks |

---

#### 18.7.11 HTML Notes

| Model | Quality |
| --- | --- |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ |
| DeepSeek V3 | ⭐⭐⭐⭐⭐ |
| Qwen3 235B | ⭐⭐⭐⭐☆ |

---

#### 18.7.12 LaTeX Generation

| Model | Quality |
| --- | --- |
| Qwen3 235B | ⭐⭐⭐⭐⭐ |
| DeepSeek R1 | ⭐⭐⭐⭐⭐ |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ |

---

#### 18.7.13 Educational Content (General)

| Model | Quality |
| --- | --- |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ |
| Qwen3 235B | ⭐⭐⭐⭐⭐ |
| DeepSeek V3 | ⭐⭐⭐⭐⭐ |

---

#### 18.7.14 Long Context Understanding

| Model | Context Limit | Quality |
| --- | --- | --- |
| Gemini 2.5 Flash | 1,000,000 tokens | ⭐⭐⭐⭐⭐ |
| Qwen3 235B | 128,000 tokens | ⭐⭐⭐⭐⭐ |
| DeepSeek V3 | 128,000 tokens | ⭐⭐⭐⭐☆ |
| Llama 3.3 70B | 128,000 tokens | ⭐⭐⭐⭐☆ |

> **Gemini 2.5 Flash's 1M token context is the primary reason it is the default for Lecture Analysis and Topic Detection** — entire lecture transcripts can fit in a single prompt without chunking.

---

#### 18.7.15 OCR Correction

| Model | Quality |
| --- | --- |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ |
| Qwen3 235B | ⭐⭐⭐⭐⭐ |
| DeepSeek V3 | ⭐⭐⭐⭐☆ |

---

#### 18.7.16 Table Generation

| Model | Quality |
| --- | --- |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ |
| DeepSeek V3 | ⭐⭐⭐⭐⭐ |
| Qwen3 235B | ⭐⭐⭐⭐⭐ |

---

#### 18.7.17 Educational Diagram (Text-Based)

> Can generate: Mermaid, Flowcharts, Sequence diagrams, Mind maps, Class diagrams.

| Model | Quality |
| --- | --- |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ |
| DeepSeek V3 | ⭐⭐⭐⭐⭐ |
| Qwen3 235B | ⭐⭐⭐⭐⭐ |

---

#### 18.7.18 RAG Answering

| Model | Quality |
| --- | --- |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ |
| DeepSeek V3 | ⭐⭐⭐⭐⭐ |
| Qwen3 235B | ⭐⭐⭐⭐⭐ |
| Llama 3.3 70B | ⭐⭐⭐⭐☆ |

---

#### 18.7.19 Summarization

| Model | Quality |
| --- | --- |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ |
| DeepSeek V3 | ⭐⭐⭐⭐⭐ |
| Qwen3 235B | ⭐⭐⭐⭐⭐ |

---

#### 18.7.20 Reasoning

| Model | Quality |
| --- | --- |
| DeepSeek R1 | ⭐⭐⭐⭐⭐ |
| Qwen3 235B | ⭐⭐⭐⭐⭐ |
| Gemini 2.5 Flash | ⭐⭐⭐⭐⭐ |
| Llama 3.3 70B | ⭐⭐⭐⭐☆ |

---

#### 18.7.21 Cross-Task Summary

| Task Category | Primary | Secondary |
| --- | --- | --- |
| Structured Note Generation | Gemini 2.5 Flash | DeepSeek V3 |
| Mathematics (Step-by-Step) | Qwen3 235B | DeepSeek R1 |
| Formula Derivation / Proof | DeepSeek R1 | Qwen3 235B |
| Code Generation | DeepSeek V3 | Qwen3 Coder 480B |
| Code Explanation | Gemini 2.5 Flash | DeepSeek V3 |
| Example Generation | Gemini 2.5 Flash | DeepSeek V3 |
| Quiz Generation | Gemini 2.5 Flash | DeepSeek V3 |
| Flashcard Generation | Gemini 2.5 Flash | Cerebras/Llama-4-Scout |
| Mind Map Generation | Gemini 2.5 Flash | DeepSeek V3 |
| Markdown Notes | DeepSeek V3 | Gemini 2.5 Flash |
| HTML Notes | Gemini 2.5 Flash | DeepSeek V3 |
| LaTeX Generation | Qwen3 235B | DeepSeek R1 |
| Educational Content (General) | Gemini 2.5 Flash | Qwen3 235B |
| Long Context Understanding | Gemini 2.5 Flash (1M ctx) | Qwen3 235B |
| OCR Correction | Gemini 2.5 Flash | Qwen3 235B |
| Table Generation | Gemini 2.5 Flash | DeepSeek V3 |
| Educational Diagram (Text) | Gemini 2.5 Flash | DeepSeek V3 |
| RAG Answering | Gemini 2.5 Flash | DeepSeek V3 |
| Summarization | Gemini 2.5 Flash | DeepSeek V3 |
| Reasoning | DeepSeek R1 | Qwen3 235B |
| JSON Extraction | Gemini 2.5 Flash | Groq/Llama-3.3-70B |

---

### 18.8 Conclusion

Model Routing ensures every LLM call in EduScribe AI is served by the most appropriate model, matched to both the cognitive demands of the task and the quota constraints of the provider. The routing table in Section 18.2 provides concrete, immediately usable configuration for `model_selector.py`, while the quality reference in Section 18.7 documents the evidence behind every routing decision.

---

### 18.9 Embedding Model Reference

While Sections 18.2 and 18.7 cover routing decisions for LLM generation tasks, EduScribe AI's RAG pipeline (Section 18.7.18) requires a complementary routing decision for the **retrieval layer** — specifically, which embedding model converts chunks, notes, and queries into vector representations. This section provides the same evidence-based reference for embedding model selection, organised by use-case category.

#### 18.9.1 General Purpose (Best Overall)

| Embedding Model | Dimensions | Multilingual | Notes | Rating |
| --- | --- | --- | --- | --- |
| Qwen3-Embedding-8B | 4,096 | ✅ | State-of-the-art open embedding | ⭐⭐⭐⭐⭐ |
| Qwen3-Embedding-4B | 2,560 | ✅ | Near SOTA, lower compute | ⭐⭐⭐⭐⭐ |
| BGE-M3 | 1,024 | ✅ | Dense + Sparse + Multi-vector | ⭐⭐⭐⭐⭐ |
| NV-Embed-v2 | 4,096 | ✅ | Excellent retrieval quality | ⭐⭐⭐⭐⭐ |
| e5-mistral-7b-instruct | 4,096 | ✅ | Strong semantic understanding | ⭐⭐⭐⭐☆ |

**Top recommendations:** Qwen3-Embedding-8B › BGE-M3 › NV-Embed-v2

---

#### 18.9.2 Educational Notes and Lecture Retrieval

| Model | Best For |
| --- | --- |
| Qwen3-Embedding-8B | Lecture notes |
| BGE-M3 | Books and PDFs |
| NV-Embed-v2 | Educational documents |
| e5-mistral | Long notes |

---

#### 18.9.3 Code Retrieval

| Model | Rating |
| --- | --- |
| CodeRankEmbed | ⭐⭐⭐⭐⭐ |
| Qwen3-Embedding-8B | ⭐⭐⭐⭐⭐ |
| BGE-M3 | ⭐⭐⭐⭐☆ |
| Jina Embeddings v3 | ⭐⭐⭐⭐☆ |

---

#### 18.9.4 Mathematics and Formula Retrieval

| Model | Rating |
| --- | --- |
| Qwen3-Embedding-8B | ⭐⭐⭐⭐⭐ |
| BGE-M3 | ⭐⭐⭐⭐⭐ |
| NV-Embed-v2 | ⭐⭐⭐⭐☆ |

---

#### 18.9.5 Scientific Papers

| Model | Rating |
| --- | --- |
| Qwen3-Embedding-8B | ⭐⭐⭐⭐⭐ |
| BGE-M3 | ⭐⭐⭐⭐⭐ |
| e5-mistral | ⭐⭐⭐⭐☆ |

---

#### 18.9.6 Long Document Retrieval

| Model | Rating |
| --- | --- |
| BGE-M3 | ⭐⭐⭐⭐⭐ |
| Qwen3-Embedding-8B | ⭐⭐⭐⭐⭐ |
| NV-Embed-v2 | ⭐⭐⭐⭐⭐ |

---

#### 18.9.7 Small and Fast Models

> Ideal for CPU deployments or prototype environments.

| Model | Dimensions | Rating |
| --- | --- | --- |
| BAAI/bge-small-en-v1.5 | 384 | ⭐⭐⭐⭐☆ |
| BAAI/bge-base-en-v1.5 | 768 | ⭐⭐⭐⭐☆ |
| all-MiniLM-L6-v2 | 384 | ⭐⭐⭐☆☆ |
| gte-small | 384 | ⭐⭐⭐⭐☆ |

---

#### 18.9.8 Multilingual

| Model | Languages | Rating |
| --- | --- | --- |
| BGE-M3 | 100+ | ⭐⭐⭐⭐⭐ |
| Qwen3-Embedding-8B | 100+ | ⭐⭐⭐⭐⭐ |
| Jina Embeddings v3 | 90+ | ⭐⭐⭐⭐☆ |
| multilingual-e5-large | 100+ | ⭐⭐⭐⭐☆ |

---

#### 18.9.9 Hybrid Retrieval

For production RAG, BGE-M3 is one of the best choices because it supports multiple retrieval strategies in a single model.

| Model | Dense | Sparse | Multi-vector | Rating |
| --- | --- | --- | --- | --- |
| BGE-M3 | ✅ | ✅ | ✅ | ⭐⭐⭐⭐⭐ |
| SPLADE | ❌ | ✅ | ❌ | ⭐⭐⭐⭐☆ |
| ColBERT v2 | ❌ | ❌ | ✅ | ⭐⭐⭐⭐☆ |

---

#### 18.9.10 Lightweight (CPU-Friendly)

| Model | RAM Usage |
| --- | --- |
| bge-small | ~0.2 GB |
| gte-small | ~0.2 GB |
| MiniLM | ~0.1 GB |
| bge-base | ~0.5 GB |

---

#### 18.9.11 High-End Production

| Model | Quality |
| --- | --- |
| Qwen3-Embedding-8B | ⭐⭐⭐⭐⭐ |
| NV-Embed-v2 | ⭐⭐⭐⭐⭐ |
| BGE-M3 | ⭐⭐⭐⭐⭐ |
| e5-mistral-7b | ⭐⭐⭐⭐☆ |

---

#### 18.9.12 Recommended by Scale

| Scale | Chunk Count | Recommended Models |
| --- | --- | --- |
| 🟢 Small / Prototype | ≤ 100K chunks | BAAI/bge-small-en-v1.5 · gte-small |
| 🟡 Medium Production | 100K – 5M chunks | BGE-M3 ⭐ · Qwen3-Embedding-4B · Jina Embeddings v3 |
| 🔴 Large Production | 5M+ chunks | Qwen3-Embedding-8B ⭐ · BGE-M3 ⭐ · NV-Embed-v2 |

### 18.10 Model Profiles

Model Profiles provide engineering-level specifications for each primary model used in EduScribe AI. These profiles complement the routing table (Section 18.3) and quality reference (Section 18.7) by documenting the concrete technical constraints — context windows, memory requirements, token budgets, throughput, and RAG configuration — that the `model_selector.py` and `provider_manager.py` components must respect at runtime.

---

#### 18.10.1 DeepSeek V3

##### General Information

| Property | Value |
| --- | --- |
| Model Name | DeepSeek V3 |
| Provider | DeepSeek AI |
| Model Type | Large Language Model (LLM) |
| Architecture | Mixture of Experts (MoE) |
| Release | December 2024 |
| License | Open Weights |
| API Available | Yes |
| Streaming | ✅ |
| Function Calling | ✅ |
| JSON Mode | ✅ |
| Tool Calling | ✅ |
| Vision | ❌ |

##### Architecture

| Property | Value |
| --- | --- |
| Architecture | Mixture of Experts |
| Total Parameters | 671 Billion |
| Active Parameters | 37 Billion |
| Experts | 256 |
| Activated Experts / Token | 8 |
| Dense Layers | Shared |
| KV Cache | Yes |
| Flash Attention | Yes |
| Rotary Embedding | RoPE |
| GQA | Supported |
| Quantization | FP16, INT8, INT4, GGUF |

##### Context Window

| Property | Value |
| --- | --- |
| Maximum Context Window | 128,000 tokens |
| Maximum Prompt Tokens | 128K (shared budget) |
| Maximum Completion | Provider dependent (commonly 8K–16K+) |
| Safe Prompt Size | 100K |
| Reserved Buffer | 2K–4K tokens |

##### Token Budget (Example — 128K Total Context)

| Component | Tokens |
| --- | --- |
| System Prompt | 1,500 |
| Developer Prompt | 3,000 |
| User Prompt | 1,800 |
| Retrieved Context | 70,000 |
| Conversation History | 25,000 |
| Tool Results | 5,000 |
| Reserved | 3,500 |
| **Output** | **18,000** |

##### Token Calculation (Example)

```
Transcript: 50,000 tokens
Chunk Size:      800 tokens
Overlap:         100 tokens
Effective Chunk: 700 tokens

Chunks = 50,000 / 700 ≈ 71 chunks

Retriever selects 18 chunks → 18 × 800 = 14,400 tokens

Prompt breakdown:
  System    1,500
  Developer 2,500
  User      1,500
  Retrieved 14,400
  History   12,000
  ─────────────────
  Total     31,900 tokens

Remaining = 128,000 − 31,900 = 96,100 tokens available for output
```

##### Memory Requirements

| Precision | Memory |
| --- | --- |
| FP16 | ~1.3 TB (full model) |
| Active MoE compute | ~37B active parameters per token |
| INT8 | ~650 GB |
| INT4 | ~330 GB |
| GGUF | Depends on quantization level |

##### Throughput

| Metric | Typical |
| --- | --- |
| Time to First Token | 300–800 ms |
| Generation Speed | 80–150 tokens/sec |
| Streaming | Yes |
| Parallel Requests | Yes |

##### API

```
POST /chat/completions

Required fields:
  system
  developer
  messages

Optional fields:
  temperature
  top_p
  stream
  max_tokens
```

##### Supported Features

| Feature | Support |
| --- | --- |
| Chat | ✅ |
| Code | ✅ |
| Mathematics | ⭐⭐⭐⭐⭐ |
| JSON | ✅ |
| Tool Calling | ✅ |
| Structured Output | ✅ |
| Markdown | Excellent |
| HTML | Excellent |
| Mermaid | Excellent |

##### Best Use Cases

| Use Case | Rating |
| --- | --- |
| Lecture Notes | ⭐⭐⭐⭐⭐ |
| Programming | ⭐⭐⭐⭐⭐ |
| Educational Content | ⭐⭐⭐⭐⭐ |
| Markdown | ⭐⭐⭐⭐⭐ |
| Long Documents | ⭐⭐⭐⭐⭐ |
| Summaries | ⭐⭐⭐⭐☆ |
| Reasoning | ⭐⭐⭐⭐☆ |

##### Limitations

- No native image input
- No OCR capability
- No speech recognition
- No video understanding
- Requires external retrieval pipeline for RAG

##### Cost Estimation (Example — 500 Daily Users)

```
Per user per request:
  Prompt completion:  15,000 tokens
  Output completion:   3,000 tokens
  Total per request:  18,000 tokens

Daily:   500 users × 18,000 tokens = 9,000,000 tokens
Monthly: 9,000,000 × 30           = 270,000,000 tokens
```

##### Recommended Prompt Budget

| Component | Tokens |
| --- | --- |
| System | 1,000 |
| Developer | 2,000 |
| User | 1,500 |
| Retrieved Context | 15,000 |
| History | 8,000 |
| Output | 6,000 |

##### RAG Configuration

| Parameter | Value |
| --- | --- |
| Chunk Size | 800 tokens |
| Overlap | 100 tokens |
| Top-K | 15 |
| Embedding Model | BGE-M3 |
| Re-ranker | BGE Reranker |
| Context Compression | Yes |

##### Engineering Notes

| Parameter | Value |
| --- | --- |
| Preferred Chunk Size | 800 tokens |
| Max Chunks in Context | 20 |
| Ideal Context Window Usage | 20K–40K tokens |
| Recommended Output Budget | 4K–8K tokens |
| Best Temperature | 0.2–0.5 |
| Best Top-P | 0.9 |

##### Evaluation Scores

| Category | Score (out of 10) |
| --- | --- |
| Coding | 9.8 |
| Mathematics | 9.7 |
| Long Context | 9.6 |
| Educational Notes | 9.8 |
| JSON Output | 9.5 |
| RAG | 9.6 |
| Markdown | 9.9 |
| Cost Efficiency | 9.7 |

---

#### 18.10.2 DeepSeek R1

##### General Information

| Property | Value |
| --- | --- |
| Model Name | DeepSeek R1 |
| Provider | DeepSeek AI |
| Model Type | Reasoning Large Language Model (LLM) |
| Architecture | Mixture of Experts (MoE) |
| Release | January 2025 |
| License | Open Weights |
| API Available | Yes |
| Streaming | ✅ |
| Function Calling | ✅ |
| JSON Mode | ✅ |
| Tool Calling | ✅ |
| Vision | ❌ |
| Reasoning Mode | Native |

##### Architecture

| Property | Value |
| --- | --- |
| Architecture | Mixture of Experts (MoE) |
| Total Parameters | 671 Billion |
| Active Parameters | 37 Billion |
| Experts | 256 |
| Activated Experts / Token | 8 |
| Dense Layers | Shared Experts |
| KV Cache | Supported |
| Flash Attention | Supported |
| RoPE | Supported |
| GQA | Supported |
| Quantization | FP16, INT8, INT4, GGUF |

##### Context Window

| Property | Value |
| --- | --- |
| Maximum Context Window | 128,000 tokens |
| Maximum Prompt Size | 128K shared context |
| Maximum Completion Tokens | Provider dependent (8K–16K+) |
| Recommended Prompt Size | 90K–100K tokens |
| Reserved Buffer | 2K–4K tokens |

##### Token Budget Planning (Example — 128K Total Context)

| Component | Tokens |
| --- | --- |
| System Prompt | 1,500 |
| Developer Prompt | 2,500 |
| User Prompt | 2,000 |
| Retrieved Chunks | 65,000 |
| Conversation History | 28,000 |
| Tool Results | 4,000 |
| Reserved Tokens | 3,000 |
| **Output Tokens** | **22,000** |

##### Token Calculation (Example)

```
Lecture Transcript: 72,000 tokens
Chunk Size:            900 tokens
Overlap:               150 tokens
Effective Chunk:       750 tokens

Total Chunks = 72,000 / 750 = 96 chunks

Retriever selects Top-K 20 chunks → 20 × 900 = 18,000 tokens

Final Prompt breakdown:
  System    1,500
  Developer 2,500
  User      1,800
  Retrieved 18,000
  History   12,000
  Tool      3,000
  ──────────────────
  Total     38,800 tokens

Remaining = 128,000 − 38,800 = 89,200 tokens available for output
```

##### Memory Requirements

| Precision | Estimated Memory |
| --- | --- |
| FP16 | ~1.3 TB |
| INT8 | ~650 GB |
| INT4 | ~330 GB |
| Active Compute | 37B parameters |
| KV Cache | Dynamic |

##### Throughput

| Metric | Typical |
| --- | --- |
| First Token Latency | 500–900 ms |
| Generation Speed | 60–120 tokens/sec |
| Streaming | Yes |
| Parallel Requests | Yes |
| Batch Processing | Supported |

##### API Parameters

```
POST /chat/completions

Fields:
  model
  messages
  system
  temperature
  top_p
  max_tokens
  stream
  stop
  seed
  response_format
  tools
```

##### Supported Features

| Feature | Support |
| --- | --- |
| Chain of Thought Reasoning | ✅ |
| Mathematics | ⭐⭐⭐⭐⭐ |
| Code Generation | ⭐⭐⭐⭐⭐ |
| Proof Generation | ⭐⭐⭐⭐⭐ |
| JSON Output | ✅ |
| Function Calling | ✅ |
| Tool Calling | ✅ |
| Markdown | Excellent |
| HTML | Excellent |
| LaTeX | Excellent |

##### Reasoning Capabilities

| Task | Rating |
| --- | --- |
| Mathematical Proof | ⭐⭐⭐⭐⭐ |
| Algorithm Design | ⭐⭐⭐⭐⭐ |
| Competitive Programming | ⭐⭐⭐⭐⭐ |
| Logical Reasoning | ⭐⭐⭐⭐⭐ |
| Scientific Reasoning | ⭐⭐⭐⭐⭐ |
| Step-by-Step Solutions | ⭐⭐⭐⭐⭐ |
| Engineering Problems | ⭐⭐⭐⭐⭐ |

##### Best Use Cases

| Use Case | Rating |
| --- | --- |
| Mathematics | ⭐⭐⭐⭐⭐ |
| Formula Derivation | ⭐⭐⭐⭐⭐ |
| Algorithm Design | ⭐⭐⭐⭐⭐ |
| Programming | ⭐⭐⭐⭐⭐ |
| Research | ⭐⭐⭐⭐⭐ |
| Long Reasoning | ⭐⭐⭐⭐⭐ |
| Educational Notes | ⭐⭐⭐⭐☆ |
| Summaries | ⭐⭐⭐⭐☆ |

##### Prompt Budget Recommendation

| Component | Tokens |
| --- | --- |
| System Prompt | 1,200 |
| Developer Prompt | 2,500 |
| User Prompt | 1,500 |
| Retrieved Context | 18,000 |
| History | 10,000 |
| Output | 8,000 |
| Reserved | 3,000 |

##### RAG Configuration

| Parameter | Value |
| --- | --- |
| Chunk Size | 900 tokens |
| Chunk Overlap | 150 tokens |
| Retriever | Hybrid Search |
| Embedding Model | BGE-M3 |
| Re-ranker | BGE Reranker v2 |
| Top-K | 15–20 |
| Similarity Metric | Cosine |
| Metadata Filtering | Enabled |

##### Production Configuration

| Parameter | Value |
| --- | --- |
| Temperature | 0.2 |
| Top-P | 0.9 |
| Frequency Penalty | 0 |
| Presence Penalty | 0 |
| Streaming | Enabled |
| Max Output | 8,000 tokens |
| Seed | Optional |

##### Cost Estimation (Example — 1,000 Daily Users)

```
Per user per request:
  Prompt:     18,000 tokens
  Completion:  4,000 tokens
  Total:      22,000 tokens

Daily:   1,000 users × 22,000 tokens = 22,000,000 tokens
Monthly: 22,000,000 × 30            = 660,000,000 tokens
```

##### Engineering Considerations

| Parameter | Value |
| --- | --- |
| Recommended Chunk Size | 900 tokens |
| Maximum Chunks | 20 |
| Ideal Context Size | 25K–40K tokens |
| Ideal Output | 4K–8K tokens |
| Best Temperature | 0.2 |
| Best Top-P | 0.9 |
| Reasoning Mode | Enabled |
| Streaming | Enabled |

##### Limitations

- No native image understanding
- No OCR capability
- No audio processing
- No video processing
- Requires external retrieval pipeline for RAG
- Higher latency than smaller dense models (native reasoning overhead)
- Large infrastructure requirements for self-hosting

##### Evaluation Scores

| Category | Score (out of 10) |
| --- | --- |
| Mathematics | 10.0 |
| Reasoning | 10.0 |
| Coding | 9.8 |
| Long Context | 9.6 |
| Educational Notes | 9.4 |
| RAG | 9.7 |
| JSON Output | 9.6 |
| Markdown | 9.8 |
| Tool Calling | 9.5 |
| Cost Efficiency | 9.7 |

##### Recommended EduScribe AI Tasks

| Task | Suitability |
| --- | --- |
| Step-by-Step Mathematics | ⭐⭐⭐⭐⭐ |
| Formula Derivation | ⭐⭐⭐⭐⭐ |
| Proof Generation | ⭐⭐⭐⭐⭐ |
| Algorithm Explanation | ⭐⭐⭐⭐⭐ |
| Programming Tutorials | ⭐⭐⭐⭐⭐ |
| Competitive Programming Solutions | ⭐⭐⭐⭐⭐ |
| Research Paper Analysis | ⭐⭐⭐⭐☆ |
| Lecture Notes | ⭐⭐⭐⭐☆ |
| Quiz Generation | ⭐⭐⭐⭐☆ |
| Flashcards | ⭐⭐⭐⭐☆ |

##### Deployment Recommendation

| Component | Recommendation |
| --- | --- |
| Deployment Type | Cloud API |
| Recommended Hardware (Self-Hosted) | 8× NVIDIA H100 |
| Production API Providers | OpenRouter · Together AI · Fireworks AI |
| Embedding Model | BGE-M3 |
| Vector Database | Qdrant |
| Framework | LiteLLM |
| Inference Framework | vLLM |
| Monitoring | Langfuse |
| Caching | Redis |
| Rate Limiting | API Gateway |

---

#### 18.10.3 Gemini 2.5 Flash

##### General Information

| Property | Value |
| --- | --- |
| Model Name | Gemini 2.5 Flash |
| Provider | Google DeepMind |
| Model Type | Multimodal Large Language Model (LLM) |
| Architecture | Transformer (Proprietary) |
| Release | 2025 |
| License | Proprietary |
| API Available | Yes |
| Streaming | ✅ |
| Function Calling | ✅ |
| JSON Mode | ✅ |
| Tool Calling | ✅ |
| Vision | ✅ |
| Audio Understanding | ✅ |
| Video Understanding | ✅ |

##### Architecture

| Property | Value |
| --- | --- |
| Architecture | Dense Transformer |
| Parameters | Proprietary (Not Public) |
| Attention | Multi-Head Attention |
| KV Cache | Supported |
| Flash Attention | Supported |
| RoPE | Proprietary |
| Quantization | Google Managed |
| Self Hosting | ❌ |

##### Context Window

| Property | Value |
| --- | --- |
| Maximum Context Window | 1,048,576 tokens (1M) |
| Maximum Prompt Tokens | Shared within 1M context |
| Maximum Completion Tokens | Provider dependent |
| Recommended Prompt Size | 800K tokens |
| Reserved Buffer | 10K–20K tokens |

##### Token Budget Planning (Example — 1M Total Context)

| Component | Tokens |
| --- | --- |
| System Prompt | 2,000 |
| Developer Prompt | 3,000 |
| User Prompt | 2,500 |
| Retrieved Chunks | 250,000 |
| Conversation History | 50,000 |
| OCR Results | 100,000 |
| Video Metadata | 20,000 |
| Tool Results | 10,000 |
| Reserved Tokens | 10,000 |
| **Output Tokens** | **50,000** |
| Remaining | ≈ 550K tokens |

##### Token Calculation (Example)

```
Transcript:       350,000 tokens
OCR:               60,000 tokens
Slides Metadata:   15,000 tokens
User Prompt:        2,000 tokens
System Prompt:      2,000 tokens
Developer Prompt:   3,000 tokens
History:           15,000 tokens
─────────────────────────────────
Total Prompt:     447,000 tokens

Remaining = 1,048,576 − 447,000 = 601,576 tokens available for output
```

##### Memory Requirements

| Property | Value |
| --- | --- |
| Self Hosted | ❌ |
| GPU Requirement | Google Managed |
| KV Cache | Managed |
| Quantization | Internal |
| Infrastructure | Google TPU |

##### Throughput

| Metric | Typical |
| --- | --- |
| First Token Latency | 200–500 ms |
| Streaming | Yes |
| Batch Requests | Supported |
| Parallel Requests | Supported |
| Long Context Performance | Excellent |

##### API Parameters

```
POST /v1/models/gemini-2.5-flash:generateContent

Fields:
  contents
  system_instruction
  tools
  generationConfig
    temperature
    topP
    topK
    maxOutputTokens
    responseMimeType
    candidateCount
    stopSequences
  safetySettings
  stream
```

##### Supported Features

| Feature | Support |
| --- | --- |
| Text | ✅ |
| Images | ✅ |
| PDF | ✅ |
| Audio | ✅ |
| Video | ✅ |
| OCR | ✅ |
| Function Calling | ✅ |
| JSON Output | ✅ |
| Tool Calling | ✅ |
| Markdown | Excellent |
| HTML | Excellent |
| Mermaid | Excellent |

##### Multimodal Capabilities

| Capability | Rating |
| --- | --- |
| Text Understanding | ⭐⭐⭐⭐⭐ |
| Image Analysis | ⭐⭐⭐⭐⭐ |
| OCR | ⭐⭐⭐⭐⭐ |
| PDF Understanding | ⭐⭐⭐⭐⭐ |
| Video Understanding | ⭐⭐⭐⭐⭐ |
| Audio Understanding | ⭐⭐⭐⭐⭐ |
| Charts | ⭐⭐⭐⭐⭐ |
| Tables | ⭐⭐⭐⭐⭐ |

##### Best Use Cases

| Use Case | Rating |
| --- | --- |
| Lecture Notes | ⭐⭐⭐⭐⭐ |
| OCR Cleanup | ⭐⭐⭐⭐⭐ |
| Video Understanding | ⭐⭐⭐⭐⭐ |
| PDF Analysis | ⭐⭐⭐⭐⭐ |
| Summaries | ⭐⭐⭐⭐⭐ |
| Mind Maps | ⭐⭐⭐⭐⭐ |
| Flashcards | ⭐⭐⭐⭐⭐ |
| Quiz Generation | ⭐⭐⭐⭐⭐ |
| RAG | ⭐⭐⭐⭐⭐ |
| Translation | ⭐⭐⭐⭐⭐ |

##### Prompt Budget Recommendation

| Component | Tokens |
| --- | --- |
| System Prompt | 2,000 |
| Developer Prompt | 3,000 |
| User Prompt | 2,000 |
| Transcript | 200,000 |
| OCR | 60,000 |
| Retrieved Context | 100,000 |
| History | 20,000 |
| Output | 20,000 |
| Reserved | 10,000 |

##### RAG Configuration

| Parameter | Value |
| --- | --- |
| Chunk Size | 1,000 tokens |
| Chunk Overlap | 150 tokens |
| Retriever | Hybrid Search |
| Embedding Model | BGE-M3 |
| Re-ranker | BGE Reranker |
| Top-K | 25 |
| Similarity Metric | Cosine |
| Metadata Filtering | Enabled |

##### Production Configuration

| Parameter | Value |
| --- | --- |
| Temperature | 0.3 |
| Top-P | 0.9 |
| Top-K | 40 |
| Streaming | Enabled |
| JSON Mode | Enabled |
| Function Calling | Enabled |
| Thinking | Adaptive |

##### Cost Estimation (Example — 2,000 Daily Users)

```
Per user per request:
  Prompt:     40,000 tokens
  Completion:  6,000 tokens
  Total:      46,000 tokens

Daily:   2,000 users × 46,000 tokens = 92,000,000 tokens
Monthly: 92,000,000 × 30            = 2,760,000,000 tokens
```

##### Engineering Considerations

| Parameter | Value |
| --- | --- |
| Recommended Chunk Size | 1,000 tokens |
| Maximum Chunks | 25 |
| Ideal Context Window Usage | 100K–300K tokens |
| Ideal Output | 4K–12K tokens |
| Temperature | 0.3 |
| Streaming | Enabled |
| Vision | Enabled |
| Function Calling | Enabled |

##### Limitations

- Cannot be self-hosted
- Closed-source proprietary model
- Provider-controlled rate limits
- Usage depends on Google API quotas
- Latency increases with very large contexts
- Model behavior may change as Google updates managed versions

##### Evaluation Scores

| Category | Score (out of 10) |
| --- | --- |
| Long Context | 10.0 |
| OCR | 10.0 |
| PDF Understanding | 10.0 |
| Video Analysis | 10.0 |
| Educational Notes | 9.9 |
| RAG | 9.8 |
| Coding | 9.3 |
| Mathematics | 9.4 |
| JSON Output | 9.8 |
| Tool Calling | 10.0 |

##### Recommended EduScribe AI Tasks

| Task | Suitability |
| --- | --- |
| Transcript Cleanup | ⭐⭐⭐⭐⭐ |
| OCR Correction | ⭐⭐⭐⭐⭐ |
| Video-to-Notes | ⭐⭐⭐⭐⭐ |
| PDF Analysis | ⭐⭐⭐⭐⭐ |
| Flashcards | ⭐⭐⭐⭐⭐ |
| Quiz Generation | ⭐⭐⭐⭐⭐ |
| Mind Map Generation | ⭐⭐⭐⭐⭐ |
| RAG Question Answering | ⭐⭐⭐⭐⭐ |
| Image Explanation | ⭐⭐⭐⭐⭐ |
| Educational Summaries | ⭐⭐⭐⭐⭐ |

##### Deployment Recommendation

| Component | Recommendation |
| --- | --- |
| Deployment Type | Cloud API |
| Recommended API | Google AI Studio · Vertex AI |
| Framework | LiteLLM |
| Embedding Model | BGE-M3 |
| Vector Database | Qdrant |
| Retriever | Hybrid Retrieval |
| Backend Framework | FastAPI |
| Caching | Redis |
| Monitoring | Langfuse |
| Observability | OpenTelemetry |
| Rate Limiting | API Gateway |
| Fallback Model | DeepSeek V3 |

---

#### 18.10.4 Qwen3 235B A22B

##### General Information

| Property | Value |
| --- | --- |
| Model Name | Qwen3 235B A22B |
| Provider | Alibaba Cloud (Qwen Team) |
| Model Type | Large Language Model (LLM) |
| Architecture | Mixture of Experts (MoE) |
| Release | 2025 |
| License | Apache 2.0 |
| API Available | Yes |
| Streaming | ✅ |
| Function Calling | ✅ |
| JSON Mode | ✅ |
| Tool Calling | ✅ |
| Vision | ❌ (Text-only variant) |
| Multilingual | ✅ 100+ Languages |

##### Architecture

| Property | Value |
| --- | --- |
| Architecture | Mixture of Experts (MoE) |
| Total Parameters | 235 Billion |
| Active Parameters | 22 Billion |
| Number of Experts | 128 |
| Active Experts | Dynamic Routing |
| Dense Layers | Shared Experts |
| Flash Attention | Supported |
| KV Cache | Supported |
| RoPE | Supported |
| GQA | Supported |
| Quantization | FP16, BF16, INT8, INT4, GGUF |

##### Context Window

| Property | Value |
| --- | --- |
| Maximum Context Window | 262,144 tokens (256K) |
| Maximum Prompt Tokens | Shared context budget |
| Maximum Completion Tokens | Provider dependent |
| Safe Prompt Budget | 220K tokens |
| Reserved Buffer | 4K–8K tokens |

##### Token Budget Planning (Example — 256K Total Context)

| Component | Tokens |
| --- | --- |
| System Prompt | 1,500 |
| Developer Prompt | 2,500 |
| User Prompt | 1,800 |
| Retrieved Context | 120,000 |
| Conversation History | 40,000 |
| OCR Results | 25,000 |
| Tool Results | 8,000 |
| Reserved | 5,000 |
| **Output** | **30,000** |
| Remaining | ≈ 28K tokens |

##### Token Calculation (Example)

```
Lecture Transcript: 150,000 tokens
Chunk Size:           1,000 tokens
Overlap:                150 tokens
Effective Chunk:        850 tokens

Total Chunks = 150,000 / 850 ≈ 177 chunks

Retriever selects Top-K 25 chunks → 25 × 1,000 = 25,000 tokens

Final Prompt breakdown:
  System    1,500
  Developer 2,500
  User      1,800
  Retrieved 25,000
  History   18,000
  OCR       12,000
  Tool       4,000
  ──────────────────
  Total     64,800 tokens

Remaining = 262,144 − 64,800 = 197,344 tokens available for output
```

##### Memory Requirements

| Precision | Estimated Memory |
| --- | --- |
| FP16 | ~470 GB |
| BF16 | ~470 GB |
| INT8 | ~240 GB |
| INT4 | ~120 GB |
| Active Compute | 22B parameters |

##### Throughput

| Metric | Typical |
| --- | --- |
| First Token Latency | 300–700 ms |
| Generation Speed | 100–180 tokens/sec |
| Streaming | Supported |
| Parallel Requests | Supported |
| Batch Inference | Supported |

##### API Parameters

```
POST /chat/completions

Fields:
  model
  messages
  system
  temperature
  top_p
  top_k
  max_tokens
  stream
  stop
  seed
  response_format
  tools
```

##### Supported Features

| Feature | Support |
| --- | --- |
| Chat | ✅ |
| Coding | ⭐⭐⭐⭐⭐ |
| Mathematics | ⭐⭐⭐⭐⭐ |
| JSON Output | ✅ |
| Function Calling | ✅ |
| Tool Calling | ✅ |
| Markdown | Excellent |
| HTML | Excellent |
| LaTeX | Excellent |
| XML | Excellent |

##### Reasoning Capabilities

| Task | Rating |
| --- | --- |
| Logical Reasoning | ⭐⭐⭐⭐⭐ |
| Programming | ⭐⭐⭐⭐⭐ |
| Mathematical Proof | ⭐⭐⭐⭐⭐ |
| Algorithm Design | ⭐⭐⭐⭐⭐ |
| Research Analysis | ⭐⭐⭐⭐⭐ |
| Long Document QA | ⭐⭐⭐⭐⭐ |
| Structured Generation | ⭐⭐⭐⭐⭐ |

##### Best Use Cases

| Use Case | Rating |
| --- | --- |
| Topic Extraction | ⭐⭐⭐⭐⭐ |
| Programming | ⭐⭐⭐⭐⭐ |
| Code Review | ⭐⭐⭐⭐⭐ |
| Research Papers | ⭐⭐⭐⭐⭐ |
| Long Documents | ⭐⭐⭐⭐⭐ |
| Mathematics | ⭐⭐⭐⭐⭐ |
| Lecture Notes | ⭐⭐⭐⭐⭐ |
| Translation | ⭐⭐⭐⭐⭐ |
| Structured JSON | ⭐⭐⭐⭐⭐ |

##### Prompt Budget Recommendation

| Component | Tokens |
| --- | --- |
| System Prompt | 1,500 |
| Developer Prompt | 2,500 |
| User Prompt | 2,000 |
| Retrieved Context | 25,000 |
| History | 15,000 |
| OCR | 10,000 |
| Output | 12,000 |
| Reserved | 5,000 |

##### RAG Configuration

| Parameter | Value |
| --- | --- |
| Chunk Size | 1,000 tokens |
| Overlap | 150 tokens |
| Embedding Model | Qwen3-Embedding-8B |
| Alternative Embedding | BGE-M3 |
| Retriever | Hybrid Search |
| Re-ranker | Qwen3 Reranker |
| Top-K | 20–25 |
| Similarity Metric | Cosine |
| Metadata Filtering | Enabled |

##### Production Configuration

| Parameter | Value |
| --- | --- |
| Temperature | 0.2 |
| Top-P | 0.9 |
| Top-K | 40 |
| Frequency Penalty | 0 |
| Presence Penalty | 0 |
| Streaming | Enabled |
| JSON Mode | Enabled |
| Function Calling | Enabled |

##### Cost Estimation (Example — 5,000 Daily Users)

```
Per user per request:
  Prompt:     25,000 tokens
  Completion:  5,000 tokens
  Total:      30,000 tokens

Daily:   5,000 users × 30,000 tokens = 150,000,000 tokens
Monthly: 150,000,000 × 30            = 4,500,000,000 tokens
```

##### Engineering Considerations

| Parameter | Value |
| --- | --- |
| Recommended Chunk Size | 1,000 tokens |
| Maximum Chunks | 25 |
| Ideal Retrieval Context | 20K–40K tokens |
| Ideal Output | 4K–12K tokens |
| Temperature | 0.2 |
| Top-P | 0.9 |
| Streaming | Enabled |
| Function Calling | Enabled |

##### Limitations

- No native image understanding
- No OCR capability
- No speech recognition
- Requires external RAG pipeline
- Large memory requirements for self-hosting (FP16: ~470 GB)
- Inference slower than smaller dense models

##### Evaluation Scores

| Category | Score (out of 10) |
| --- | --- |
| Coding | 9.9 |
| Mathematics | 9.9 |
| Reasoning | 9.8 |
| Long Context | 9.9 |
| Educational Notes | 9.8 |
| Translation | 9.8 |
| JSON Output | 9.9 |
| Tool Calling | 9.8 |
| RAG | 9.8 |
| Cost Efficiency | 9.6 |

##### Recommended EduScribe AI Tasks

| Task | Suitability |
| --- | --- |
| Topic Detection | ⭐⭐⭐⭐⭐ |
| Semantic Chunking | ⭐⭐⭐⭐⭐ |
| Long Lecture Notes | ⭐⭐⭐⭐⭐ |
| Formula Explanation | ⭐⭐⭐⭐⭐ |
| Programming Notes | ⭐⭐⭐⭐⭐ |
| Code Examples | ⭐⭐⭐⭐⭐ |
| Mathematical Derivation | ⭐⭐⭐⭐⭐ |
| Research Paper Analysis | ⭐⭐⭐⭐⭐ |
| Structured JSON Generation | ⭐⭐⭐⭐⭐ |
| Knowledge Graph Extraction | ⭐⭐⭐⭐⭐ |

##### Deployment Recommendation

| Component | Recommendation |
| --- | --- |
| Deployment Type | Cloud API or Self-Hosted |
| Recommended API | Alibaba DashScope · OpenRouter · Together AI · Groq (if available) |
| Inference Engine | vLLM |
| Framework | LiteLLM |
| Embedding Model | Qwen3-Embedding-8B |
| Alternative Embedding | BGE-M3 |
| Vector Database | Qdrant |
| Retriever | Hybrid Retrieval |
| Re-ranker | Qwen3 Reranker |
| Caching | Redis |
| Monitoring | Langfuse |
| Observability | OpenTelemetry |
| Rate Limiting | API Gateway |

---

#### 18.10.5 Qwen3 Coder

##### General Information

| Property | Value |
| --- | --- |
| Model Name | Qwen3 Coder |
| Provider | Alibaba Cloud (Qwen Team) |
| Model Type | Code Large Language Model (Code LLM) |
| Architecture | Mixture of Experts (MoE) |
| Release | 2025 |
| License | Apache 2.0 |
| API Available | Yes |
| Streaming | ✅ |
| Function Calling | ✅ |
| JSON Mode | ✅ |
| Tool Calling | ✅ |
| Vision | ❌ |
| Primary Domain | Software Engineering |

##### Architecture

| Property | Value |
| --- | --- |
| Architecture | Mixture of Experts (MoE) |
| Total Parameters | 480 Billion |
| Active Parameters | 35 Billion |
| Experts | Dynamic Routing |
| Programming Languages | 100+ |
| Flash Attention | Supported |
| KV Cache | Supported |
| GQA | Supported |
| RoPE | Supported |
| Quantization | FP16, BF16, INT8, INT4, GGUF |

##### Context Window

| Property | Value |
| --- | --- |
| Maximum Context Window | 256,000 tokens |
| Maximum Prompt Tokens | Shared context |
| Maximum Completion Tokens | Provider dependent |
| Recommended Prompt Size | 220K tokens |
| Reserved Buffer | 4K–8K tokens |

##### Token Budget Planning (Example — 256K Total Context)

| Component | Tokens |
| --- | --- |
| System Prompt | 1,500 |
| Developer Prompt | 3,000 |
| Repository Context | 120,000 |
| Retrieved Files | 50,000 |
| Conversation History | 20,000 |
| Tool Results | 10,000 |
| Reserved | 5,000 |
| **Output** | **30,000** |

##### Token Calculation (Example)

```
Repository:       320 files
Average file:     1,200 tokens

Retriever selects Top 25 files → 25 × 1,200 = 30,000 tokens

Final Prompt breakdown:
  System       1,500
  Developer    3,000
  Repository  30,000
  History     10,000
  User         2,000
  Tool         5,000
  ──────────────────
  Total       51,500 tokens

Remaining = 256,000 − 51,500 = 204,500 tokens available for output
```

##### Memory Requirements

| Precision | Estimated Memory |
| --- | --- |
| FP16 | ~960 GB |
| BF16 | ~960 GB |
| INT8 | ~480 GB |
| INT4 | ~240 GB |
| Active Compute | 35B parameters |

##### Throughput

| Metric | Typical |
| --- | --- |
| First Token Latency | 300–700 ms |
| Generation Speed | 100–180 tokens/sec |
| Streaming | Supported |
| Parallel Requests | Supported |
| Batch Inference | Supported |

##### API Parameters

```
POST /chat/completions

Fields:
  model
  messages
  system
  temperature
  top_p
  top_k
  max_tokens
  stream
  stop
  seed
  response_format
  tools
```

##### Supported Features

| Feature | Support |
| --- | --- |
| Code Generation | ⭐⭐⭐⭐⭐ |
| Code Completion | ⭐⭐⭐⭐⭐ |
| Code Explanation | ⭐⭐⭐⭐⭐ |
| Bug Fixing | ⭐⭐⭐⭐⭐ |
| Refactoring | ⭐⭐⭐⭐⭐ |
| JSON Output | ✅ |
| Function Calling | ✅ |
| Tool Calling | ✅ |
| Markdown | Excellent |
| HTML | Excellent |

##### Programming Language Support

| Language | Rating |
| --- | --- |
| Python | ⭐⭐⭐⭐⭐ |
| Java | ⭐⭐⭐⭐⭐ |
| C++ | ⭐⭐⭐⭐⭐ |
| C | ⭐⭐⭐⭐⭐ |
| JavaScript | ⭐⭐⭐⭐⭐ |
| TypeScript | ⭐⭐⭐⭐⭐ |
| Go | ⭐⭐⭐⭐⭐ |
| Rust | ⭐⭐⭐⭐⭐ |
| Kotlin | ⭐⭐⭐⭐⭐ |
| Swift | ⭐⭐⭐⭐☆ |
| SQL | ⭐⭐⭐⭐⭐ |
| Bash | ⭐⭐⭐⭐⭐ |

##### Best Use Cases

| Use Case | Rating |
| --- | --- |
| Full Stack Development | ⭐⭐⭐⭐⭐ |
| Backend Development | ⭐⭐⭐⭐⭐ |
| API Development | ⭐⭐⭐⭐⭐ |
| Competitive Programming | ⭐⭐⭐⭐⭐ |
| Algorithm Design | ⭐⭐⭐⭐⭐ |
| Code Review | ⭐⭐⭐⭐⭐ |
| Bug Fixing | ⭐⭐⭐⭐⭐ |
| Unit Test Generation | ⭐⭐⭐⭐⭐ |
| Documentation | ⭐⭐⭐⭐⭐ |
| Refactoring | ⭐⭐⭐⭐⭐ |

##### Prompt Budget Recommendation

| Component | Tokens |
| --- | --- |
| System Prompt | 1,500 |
| Developer Prompt | 3,000 |
| Repository Context | 30,000 |
| Retrieved Files | 20,000 |
| User Prompt | 2,000 |
| History | 10,000 |
| Output | 12,000 |
| Reserved | 5,000 |

##### RAG Configuration

| Parameter | Value |
| --- | --- |
| Chunk Size | 1,200 tokens |
| Chunk Overlap | 150 tokens |
| Embedding Model | Qwen3-Embedding-8B |
| Alternative Embedding | BGE-M3 |
| Retriever | Hybrid Search |
| Re-ranker | Qwen3 Reranker |
| Top-K | 20 |
| Similarity Metric | Cosine |
| Metadata Fields | Repository path · Language · Function name · Class name |

##### Production Configuration

| Parameter | Value |
| --- | --- |
| Temperature | 0.1 |
| Top-P | 0.95 |
| Top-K | 40 |
| Streaming | Enabled |
| JSON Mode | Enabled |
| Function Calling | Enabled |

##### Cost Estimation (Example — 100 Developers, 200 Requests/Day)

```
Per request:
  Prompt:     18,000 tokens
  Completion:  4,000 tokens
  Total:      22,000 tokens

Daily:   100 developers × 200 requests × 22,000 tokens = 440,000,000 tokens
Monthly: 440,000,000 × 30                              = 13,200,000,000 tokens
```

##### Engineering Considerations

| Parameter | Value |
| --- | --- |
| Recommended File Chunk | 1,200 tokens |
| Maximum Retrieved Files | 20 |
| Repository Context Budget | 20K–60K tokens |
| Ideal Output | 4K–10K tokens |
| Temperature | 0.1 |
| Streaming | Enabled |
| Function Calling | Enabled |

##### Limitations

- No native image understanding
- No OCR capability
- No speech recognition
- Requires external repository indexing pipeline
- Large memory requirements for self-hosting (FP16: ~960 GB)
- Performance depends on repository chunk quality and metadata accuracy

##### Evaluation Scores

| Category | Score (out of 10) |
| --- | --- |
| Code Generation | 10.0 |
| Code Review | 10.0 |
| Debugging | 9.9 |
| Refactoring | 9.9 |
| API Design | 9.8 |
| Documentation | 9.8 |
| JSON Output | 9.9 |
| Tool Calling | 9.8 |
| Software Architecture | 9.8 |
| Cost Efficiency | 9.6 |

##### Recommended EduScribe AI Tasks

| Task | Suitability |
| --- | --- |
| FastAPI Backend Development | ⭐⭐⭐⭐⭐ |
| Next.js Development | ⭐⭐⭐⭐⭐ |
| React Components | ⭐⭐⭐⭐⭐ |
| PostgreSQL Queries | ⭐⭐⭐⭐⭐ |
| Redis Integration | ⭐⭐⭐⭐⭐ |
| API Documentation | ⭐⭐⭐⭐⭐ |
| Unit Test Generation | ⭐⭐⭐⭐⭐ |
| Bug Detection | ⭐⭐⭐⭐⭐ |
| Code Refactoring | ⭐⭐⭐⭐⭐ |
| System Design | ⭐⭐⭐⭐⭐ |

##### Deployment Recommendation

| Component | Recommendation |
| --- | --- |
| Deployment Type | Cloud API or Self-Hosted |
| Recommended API | Alibaba DashScope · OpenRouter · Together AI |
| Inference Engine | vLLM |
| Framework | LiteLLM |
| Embedding Model | Qwen3-Embedding-8B |
| Vector Database | Qdrant |
| Code Indexing | Tree-sitter |
| Retriever | Hybrid Search |
| Caching | Redis |
| Monitoring | Langfuse |
| Observability | OpenTelemetry |
| Rate Limiting | API Gateway |

---

#### 18.10.6 Llama 3.3 70B Instruct

##### General Information

| Property | Value |
| --- | --- |
| Model Name | Llama 3.3 70B Instruct |
| Provider | Meta AI |
| Model Type | Large Language Model (LLM) |
| Architecture | Dense Transformer |
| Release | December 2024 |
| License | Llama Community License |
| API Available | Yes |
| Streaming | ✅ |
| Function Calling | ✅ |
| JSON Mode | ✅ |
| Tool Calling | ✅ |
| Vision | ❌ |
| Self Hosting | ✅ |

##### Architecture

| Property | Value |
| --- | --- |
| Architecture | Dense Decoder Transformer |
| Total Parameters | 70 Billion |
| Active Parameters | 70 Billion |
| Attention | Multi-Head Attention |
| KV Cache | Supported |
| GQA | Supported |
| RoPE | Supported |
| Flash Attention | Supported |
| Quantization | FP16, BF16, INT8, INT4, GGUF |
| Self Hosting | Supported |

##### Context Window

| Property | Value |
| --- | --- |
| Maximum Context Window | 128,000 tokens |
| Maximum Prompt Tokens | Shared context |
| Maximum Completion Tokens | Provider dependent |
| Recommended Prompt Size | 100K tokens |
| Reserved Buffer | 2K–4K tokens |

##### Token Budget Planning (Example — 128K Total Context)

| Component | Tokens |
| --- | --- |
| System Prompt | 1,500 |
| Developer Prompt | 2,500 |
| User Prompt | 2,000 |
| Retrieved Context | 50,000 |
| Conversation History | 25,000 |
| Tool Results | 5,000 |
| Reserved | 3,000 |
| **Output** | **15,000** |
| Remaining | 24,000 tokens |

##### Token Calculation (Example)

```
Lecture Transcript: 65,000 tokens
Chunk Size:            800 tokens
Overlap:               100 tokens
Effective Chunk:       700 tokens

Total Chunks = 65,000 / 700 ≈ 93 chunks

Retriever selects Top-K 15 chunks → 15 × 800 = 12,000 tokens

Final Prompt breakdown:
  System    1,500
  Developer 2,500
  User      1,800
  Retrieved 12,000
  History   10,000
  Tool       3,000
  ──────────────────
  Total     30,800 tokens

Remaining = 128,000 − 30,800 = 97,200 tokens available for output
```

##### Memory Requirements

| Precision | Estimated Memory |
| --- | --- |
| FP16 | ~140 GB |
| BF16 | ~140 GB |
| INT8 | ~70 GB |
| INT4 | ~35 GB |
| GGUF Q4 | ~40 GB |
| Active Parameters | 70B |

##### Throughput

| Metric | Typical |
| --- | --- |
| First Token Latency | 300–700 ms |
| Generation Speed | 70–140 tokens/sec |
| Streaming | Supported |
| Parallel Requests | Supported |
| Batch Inference | Supported |

##### API Parameters

```
POST /chat/completions

Fields:
  model
  messages
  system
  temperature
  top_p
  max_tokens
  stream
  tools
  response_format
  seed
  stop
```

##### Supported Features

| Feature | Support |
| --- | --- |
| Chat | ✅ |
| Coding | ⭐⭐⭐⭐⭐ |
| Mathematics | ⭐⭐⭐⭐☆ |
| JSON Output | ✅ |
| Function Calling | ✅ |
| Tool Calling | ✅ |
| Markdown | Excellent |
| HTML | Excellent |
| Structured Output | Excellent |

##### Performance

| Task | Rating |
| --- | --- |
| General Chat | ⭐⭐⭐⭐⭐ |
| Coding | ⭐⭐⭐⭐⭐ |
| RAG | ⭐⭐⭐⭐⭐ |
| Long Documents | ⭐⭐⭐⭐⭐ |
| Summarization | ⭐⭐⭐⭐⭐ |
| Translation | ⭐⭐⭐⭐☆ |
| Mathematics | ⭐⭐⭐⭐☆ |
| Reasoning | ⭐⭐⭐⭐☆ |

##### Best Use Cases

| Use Case | Rating |
| --- | --- |
| General Chat | ⭐⭐⭐⭐⭐ |
| Lecture Notes | ⭐⭐⭐⭐⭐ |
| Coding | ⭐⭐⭐⭐⭐ |
| Documentation | ⭐⭐⭐⭐⭐ |
| RAG | ⭐⭐⭐⭐⭐ |
| Summarization | ⭐⭐⭐⭐⭐ |
| Markdown Generation | ⭐⭐⭐⭐⭐ |
| Knowledge Extraction | ⭐⭐⭐⭐⭐ |
| Translation | ⭐⭐⭐⭐☆ |

##### Prompt Budget Recommendation

| Component | Tokens |
| --- | --- |
| System Prompt | 1,200 |
| Developer Prompt | 2,500 |
| User Prompt | 2,000 |
| Retrieved Context | 15,000 |
| Conversation History | 10,000 |
| Output | 8,000 |
| Reserved | 3,000 |

##### RAG Configuration

| Parameter | Value |
| --- | --- |
| Chunk Size | 800 tokens |
| Overlap | 100 tokens |
| Embedding Model | BGE-M3 |
| Alternative Embedding | Qwen3-Embedding-8B |
| Retriever | Hybrid Search |
| Re-ranker | BGE Reranker |
| Top-K | 15 |
| Similarity Metric | Cosine |

##### Production Configuration

| Parameter | Value |
| --- | --- |
| Temperature | 0.2 |
| Top-P | 0.9 |
| Frequency Penalty | 0 |
| Presence Penalty | 0 |
| Streaming | Enabled |
| JSON Mode | Enabled |
| Function Calling | Enabled |

##### Cost Estimation (Example — 3,000 Daily Users)

```
Per user per request:
  Prompt:     12,000 tokens
  Completion:  3,000 tokens
  Total:      15,000 tokens

Daily:   3,000 users × 15,000 tokens = 45,000,000 tokens
Monthly: 45,000,000 × 30            = 1,350,000,000 tokens
```

##### Engineering Considerations

| Parameter | Value |
| --- | --- |
| Recommended Chunk | 800 tokens |
| Maximum Chunks | 15 |
| Ideal Context | 20K–40K tokens |
| Ideal Output | 4K–8K tokens |
| Temperature | 0.2 |
| Streaming | Enabled |

##### Limitations

- No native vision capability
- No OCR capability
- No audio processing
- No video understanding
- Requires external RAG pipeline
- Smaller context window than Gemini 2.5 Flash (128K vs 1M)

##### Evaluation Scores

| Category | Score (out of 10) |
| --- | --- |
| Coding | 9.7 |
| RAG | 9.7 |
| Long Context | 9.5 |
| Documentation | 9.8 |
| JSON Output | 9.7 |
| Reasoning | 9.3 |
| Mathematics | 9.2 |
| Summarization | 9.6 |
| Cost Efficiency | 9.7 |
| Tool Calling | 9.6 |

##### Recommended EduScribe AI Tasks

| Task | Suitability |
| --- | --- |
| Lecture Notes | ⭐⭐⭐⭐⭐ |
| Topic Extraction | ⭐⭐⭐⭐⭐ |
| Markdown Notes | ⭐⭐⭐⭐⭐ |
| HTML Notes | ⭐⭐⭐⭐⭐ |
| RAG Chatbot | ⭐⭐⭐⭐⭐ |
| Code Examples | ⭐⭐⭐⭐⭐ |
| Summary Notes | ⭐⭐⭐⭐⭐ |
| Documentation | ⭐⭐⭐⭐⭐ |
| Flashcards | ⭐⭐⭐⭐☆ |
| Quiz Generation | ⭐⭐⭐⭐☆ |

##### Deployment Recommendation

| Component | Recommendation |
| --- | --- |
| Deployment Type | Cloud API or Self-Hosted |
| Recommended API | Groq · OpenRouter · Together AI · Fireworks AI |
| Inference Engine | vLLM |
| Framework | LiteLLM |
| Embedding Model | BGE-M3 |
| Alternative Embedding | Qwen3-Embedding-8B |
| Vector Database | Qdrant |
| Retriever | Hybrid Search |
| Caching | Redis |
| Monitoring | Langfuse |
| Observability | OpenTelemetry |
| Rate Limiting | API Gateway |

---

#### 18.10.7 Mistral Small 3.2

##### General Information

| Property | Value |
| --- | --- |
| Model Name | Mistral Small 3.2 |
| Provider | Mistral AI |
| Model Type | Large Language Model (LLM) |
| Architecture | Dense Transformer |
| Release | 2025 |
| License | Apache 2.0 |
| API Available | Yes |
| Streaming | ✅ |
| Function Calling | ✅ |
| JSON Mode | ✅ |
| Tool Calling | ✅ |
| Vision | ❌ (Text variant) |
| Self Hosting | ✅ |

##### Architecture

| Property | Value |
| --- | --- |
| Architecture | Dense Transformer Decoder |
| Total Parameters | ≈24 Billion |
| Active Parameters | 24 Billion |
| Attention | Multi-Head Attention |
| Flash Attention | Supported |
| KV Cache | Supported |
| GQA | Supported |
| RoPE | Supported |
| Quantization | FP16, BF16, INT8, INT4, GGUF |
| Self Hosting | Supported |

##### Context Window

| Property | Value |
| --- | --- |
| Maximum Context Window | 128,000 tokens |
| Maximum Prompt Tokens | Shared context |
| Maximum Completion Tokens | Provider dependent |
| Recommended Prompt Budget | 100K tokens |
| Reserved Buffer | 2K–4K tokens |

##### Token Budget Planning (Example — 128K Total Context)

| Component | Tokens |
| --- | --- |
| System Prompt | 1,200 |
| Developer Prompt | 2,000 |
| User Prompt | 1,500 |
| Retrieved Context | 45,000 |
| Conversation History | 20,000 |
| Tool Results | 5,000 |
| Reserved | 3,000 |
| **Output** | **10,000** |
| Remaining | ≈ 40K tokens |

##### Token Calculation (Example)

```
Lecture Transcript: 40,000 tokens
Chunk Size:            800 tokens
Overlap:               100 tokens
Effective Chunk:       700 tokens

Total Chunks = 40,000 / 700 ≈ 57 chunks

Retriever selects Top-K 15 chunks → 15 × 800 = 12,000 tokens

Final Prompt breakdown:
  System    1,200
  Developer 2,000
  User      1,500
  Retrieved 12,000
  History   10,000
  Tool       3,000
  ──────────────────
  Total     29,700 tokens

Remaining = 128,000 − 29,700 = 98,300 tokens available for output
```

##### Memory Requirements

| Precision | Estimated Memory |
| --- | --- |
| FP16 | ~48 GB |
| BF16 | ~48 GB |
| INT8 | ~24 GB |
| INT4 | ~12 GB |
| GGUF Q4 | ~14 GB |

##### Throughput

| Metric | Typical |
| --- | --- |
| First Token Latency | 200–500 ms |
| Generation Speed | 120–220 tokens/sec |
| Streaming | Supported |
| Parallel Requests | Supported |
| Batch Processing | Supported |

##### API Parameters

```
POST /chat/completions

Fields:
  model
  messages
  system
  temperature
  top_p
  max_tokens
  stream
  response_format
  tools
  seed
  stop
```

##### Supported Features

| Feature | Support |
| --- | --- |
| Chat | ✅ |
| Coding | ⭐⭐⭐⭐☆ |
| Mathematics | ⭐⭐⭐⭐☆ |
| JSON Output | ✅ |
| Function Calling | ✅ |
| Tool Calling | ✅ |
| Markdown | Excellent |
| HTML | Excellent |
| Structured Output | Excellent |

##### Performance

| Task | Rating |
| --- | --- |
| General Chat | ⭐⭐⭐⭐⭐ |
| Lecture Notes | ⭐⭐⭐⭐☆ |
| Coding | ⭐⭐⭐⭐☆ |
| RAG | ⭐⭐⭐⭐⭐ |
| Long Documents | ⭐⭐⭐⭐☆ |
| Summarization | ⭐⭐⭐⭐⭐ |
| Translation | ⭐⭐⭐⭐☆ |
| Reasoning | ⭐⭐⭐⭐☆ |

##### Best Use Cases

| Use Case | Rating |
| --- | --- |
| Fast Inference | ⭐⭐⭐⭐⭐ |
| RAG | ⭐⭐⭐⭐⭐ |
| Lecture Notes | ⭐⭐⭐⭐☆ |
| Summaries | ⭐⭐⭐⭐⭐ |
| Documentation | ⭐⭐⭐⭐⭐ |
| Customer Support | ⭐⭐⭐⭐⭐ |
| Chatbots | ⭐⭐⭐⭐⭐ |
| Structured JSON | ⭐⭐⭐⭐⭐ |

##### Prompt Budget Recommendation

| Component | Tokens |
| --- | --- |
| System Prompt | 1,000 |
| Developer Prompt | 2,000 |
| User Prompt | 1,500 |
| Retrieved Context | 12,000 |
| History | 8,000 |
| Output | 6,000 |
| Reserved | 3,000 |

##### RAG Configuration

| Parameter | Value |
| --- | --- |
| Chunk Size | 800 tokens |
| Overlap | 100 tokens |
| Embedding Model | BGE-M3 |
| Alternative Embedding | Qwen3-Embedding-4B |
| Retriever | Hybrid Search |
| Re-ranker | BGE Reranker |
| Top-K | 15 |
| Similarity Metric | Cosine |

##### Production Configuration

| Parameter | Value |
| --- | --- |
| Temperature | 0.3 |
| Top-P | 0.9 |
| Streaming | Enabled |
| Function Calling | Enabled |
| JSON Mode | Enabled |
| Max Output | 6,000 tokens |

##### Cost Estimation (Example — 5,000 Daily Users)

```
Per user per request:
  Prompt:     10,000 tokens
  Completion:  2,500 tokens
  Total:      12,500 tokens

Daily:   5,000 users × 12,500 tokens = 62,500,000 tokens
Monthly: 62,500,000 × 30            = 1,875,000,000 tokens
```

##### Engineering Considerations

| Parameter | Value |
| --- | --- |
| Recommended Chunk | 800 tokens |
| Maximum Chunks | 15 |
| Ideal Context | 20K–35K tokens |
| Ideal Output | 4K–6K tokens |
| Temperature | 0.3 |
| Streaming | Enabled |

##### Limitations

- No image understanding
- No OCR capability
- No speech processing
- No video processing
- Smaller reasoning capability than DeepSeek R1
- Smaller context utilization than Gemini 2.5 Flash

##### Evaluation Scores

| Category | Score (out of 10) |
| --- | --- |
| Speed | 9.9 |
| Cost Efficiency | 9.8 |
| RAG | 9.6 |
| Lecture Notes | 9.3 |
| Summarization | 9.7 |
| Coding | 9.2 |
| Mathematics | 9.1 |
| JSON Output | 9.7 |
| Tool Calling | 9.6 |
| Long Context | 9.3 |

##### Recommended EduScribe AI Tasks

| Task | Suitability |
| --- | --- |
| Fast Note Generation | ⭐⭐⭐⭐⭐ |
| Summary Notes | ⭐⭐⭐⭐⭐ |
| RAG Chatbot | ⭐⭐⭐⭐⭐ |
| Markdown Notes | ⭐⭐⭐⭐⭐ |
| Educational Chat | ⭐⭐⭐⭐⭐ |
| FAQ Generation | ⭐⭐⭐⭐⭐ |
| Documentation | ⭐⭐⭐⭐⭐ |
| Flashcards | ⭐⭐⭐⭐☆ |
| Quiz Generation | ⭐⭐⭐⭐☆ |
| HTML Notes | ⭐⭐⭐⭐☆ |

##### Deployment Recommendation

| Component | Recommendation |
| --- | --- |
| Deployment Type | Cloud API or Self-Hosted |
| Recommended API | Mistral API · OpenRouter · Together AI |
| Inference Engine | vLLM |
| Framework | LiteLLM |
| Embedding Model | BGE-M3 |
| Alternative Embedding | Qwen3-Embedding-4B |
| Vector Database | Qdrant |
| Retriever | Hybrid Retrieval |
| Caching | Redis |
| Monitoring | Langfuse |
| Observability | OpenTelemetry |
| Rate Limiting | API Gateway |

---

#### 18.10.0 Common Embedding Model Configuration

The sub-sections below apply identically to all embedding model profiles (18.10.8–18.10.15). Each individual profile references this section rather than repeating these defaults. Override values, where a profile deviates, are stated explicitly in that profile.

##### Standard API Format

```
POST /embeddings

Fields:
  model           — model identifier string
  input           — text string or array of strings
  encoding_format — "float" (default) or "base64"
  dimensions      — (optional) truncate output dimensions
  user            — (optional) end-user identifier for abuse tracking
```

##### Standard HNSW Index Configuration

| Parameter | Default Value |
| --- | --- |
| Index Type | HNSW |
| Similarity Metric | Cosine |
| M | 32 |
| efConstruction | 200 |
| efSearch | 128 |
| Metadata Filtering | Enabled |

For lightweight models (bge-base-en-v1.5, bge-small-en-v1.5): use M=16, efConstruction=100, efSearch=64 and batch size 128–256 to match their lower dimensionality.

##### Standard Vector Database Recommendations

| Database | Rating | Notes |
| --- | --- | --- |
| Qdrant | ⭐⭐⭐⭐⭐ | Recommended primary — best performance/OSS balance |
| Milvus | ⭐⭐⭐⭐⭐ | Enterprise-scale, cloud-native |
| Weaviate | ⭐⭐⭐⭐⭐ | Strong hybrid search support |
| Pinecone | ⭐⭐⭐⭐⭐ | Managed, serverless option |
| pgvector | ⭐⭐⭐⭐☆ | Good for existing PostgreSQL deployments |
| Chroma | ⭐⭐⭐⭐☆ | Excellent for local development and prototypes |

##### Standard Deployment Stack

| Component | Default |
| --- | --- |
| Framework | SentenceTransformers |
| Inference Engine | vLLM (large models) / ONNX Runtime (small models) |
| Retriever | Hybrid Search (Dense + BM25) |
| Caching | Redis |
| Monitoring | Prometheus |
| Observability | Grafana |
| API Framework | FastAPI |

##### Standard Similarity Search Support

All embedding models in Section 18.10 support: Cosine similarity, Dot Product, Euclidean Distance, HNSW indexing, IVF, PQ (Product Quantization), and Scalar Quantization.

---

#### 18.10.8 BGE-M3

##### General Information

| Property | Value |
| --- | --- |
| Model Name | BGE-M3 |
| Provider | Beijing Academy of Artificial Intelligence (BAAI) |
| Model Type | Embedding Model |
| Architecture | Transformer Encoder |
| Release | 2024 |
| License | MIT |
| API Available | Yes |
| Self Hosting | ✅ |
| Multilingual | ✅ 100+ Languages |
| Embedding Type | Dense + Sparse + Multi-Vector |
| Primary Task | Semantic Search and RAG |

##### Architecture

| Property | Value |
| --- | --- |
| Architecture | Transformer Encoder |
| Parameters | ~568 Million |
| Embedding Dimension | 1,024 |
| Pooling Strategy | CLS + Mean |
| Maximum Input Length | 8,192 tokens |
| Attention | Multi-Head Attention |
| Flash Attention | Supported |
| Quantization | FP16, INT8, INT4 |
| Self Hosting | Supported |

##### Input Specifications

| Property | Value |
| --- | --- |
| Maximum Input Tokens | 8,192 tokens |
| Recommended Input | 512–2,048 tokens |
| Minimum Input | 1 token |
| Batch Embedding | Supported |
| Streaming | ❌ |

##### Embedding Calculation (Example)

```
Lecture Transcript: 48,000 tokens
Chunk Size:            800 tokens
Overlap:               100 tokens
Effective Chunk:       700 tokens

Chunk Count = 48,000 / 700 = 69 chunks

Embeddings Generated: 69 embedding vectors (each 1,024 dimensions)
```

##### Vector Size per Embedding

| Precision | Calculation | Size |
| --- | --- | --- |
| Float32 | 1,024 × 4 bytes | 4,096 bytes ≈ 4 KB |
| Float16 | 1,024 × 2 bytes | 2,048 bytes ≈ 2 KB |
| INT8 | 1,024 × 1 byte | 1,024 bytes ≈ 1 KB |

##### Storage Requirements at Scale (Float32)

| Chunk Count | Storage |
| --- | --- |
| 100,000 chunks | 400 MB |
| 1,000,000 chunks | 4 GB |
| 10,000,000 chunks | 40 GB |

##### Similarity Search

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### API Parameters

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Retrieval Features

| Feature | Support |
| --- | --- |
| Dense Retrieval | ✅ |
| Sparse Retrieval | ✅ |
| Hybrid Retrieval | ✅ |
| Multi-Vector Retrieval | ✅ |
| Metadata Filtering | ✅ |
| Semantic Search | ⭐⭐⭐⭐⭐ |
| Keyword Search | ⭐⭐⭐⭐⭐ |

##### Performance

| Task | Rating |
| --- | --- |
| RAG | ⭐⭐⭐⭐⭐ |
| Lecture Retrieval | ⭐⭐⭐⭐⭐ |
| Books | ⭐⭐⭐⭐⭐ |
| Research Papers | ⭐⭐⭐⭐⭐ |
| OCR Retrieval | ⭐⭐⭐⭐⭐ |
| Code Search | ⭐⭐⭐⭐☆ |
| Mathematics | ⭐⭐⭐⭐☆ |

##### Best Use Cases

| Use Case | Rating |
| --- | --- |
| Educational RAG | ⭐⭐⭐⭐⭐ |
| Semantic Search | ⭐⭐⭐⭐⭐ |
| Lecture Videos | ⭐⭐⭐⭐⭐ |
| Research Papers | ⭐⭐⭐⭐⭐ |
| Knowledge Base | ⭐⭐⭐⭐⭐ |
| Hybrid Search | ⭐⭐⭐⭐⭐ |
| PDF Search | ⭐⭐⭐⭐⭐ |
| Long Documents | ⭐⭐⭐⭐⭐ |

##### Chunk Configuration

| Property | Value |
| --- | --- |
| Recommended Chunk | 800 tokens |
| Overlap | 100 tokens |
| Maximum Chunk | 2,000 tokens |
| Parent-Child Chunking | Supported |
| Metadata | Recommended |

##### Retrieval Configuration

| Parameter | Value |
| --- | --- |
| Embedding Model | BGE-M3 |
| Chunk Size | 800 tokens |
| Overlap | 100 tokens |
| Top-K | 15 |
| Re-ranker | BGE Reranker |
| Similarity | Cosine |
| Hybrid Search | Enabled |

##### Vector Database Recommendations

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Production HNSW Configuration

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Engineering Latency Calculations

| Operation | Estimate |
| --- | --- |
| Embedding latency (average) | 20–60 ms per chunk |
| 100 chunks | ≈ 2–6 seconds |
| Large lecture (70 chunks) | ≈ 1.5–4 seconds |

##### Limitations

- Cannot generate text — embedding only
- Cannot answer questions directly — requires a generative LLM
- Large batch processing requires high RAM
- No native re-ranking (requires separate BGE Reranker model)

##### Evaluation Scores

| Category | Score (out of 10) |
| --- | --- |
| Semantic Retrieval | 10.0 |
| Hybrid Search | 10.0 |
| Educational Content | 9.9 |
| Long Documents | 9.8 |
| Research Papers | 9.8 |
| OCR Retrieval | 9.8 |
| Speed | 9.8 |
| Storage Efficiency | 9.7 |
| Code Search | 9.4 |
| Production Readiness | 10.0 |

##### Recommended EduScribe AI Tasks

| Task | Suitability |
| --- | --- |
| Lecture Transcript Embedding | ⭐⭐⭐⭐⭐ |
| OCR Embedding | ⭐⭐⭐⭐⭐ |
| PDF Embedding | ⭐⭐⭐⭐⭐ |
| HTML Notes Retrieval | ⭐⭐⭐⭐⭐ |
| Flashcard Search | ⭐⭐⭐⭐⭐ |
| Quiz Context Retrieval | ⭐⭐⭐⭐⭐ |
| Hybrid Search | ⭐⭐⭐⭐⭐ |
| Semantic Search | ⭐⭐⭐⭐⭐ |
| Metadata Filtering | ⭐⭐⭐⭐⭐ |
| Vector Search | ⭐⭐⭐⭐⭐ |

##### Deployment Recommendation

| Component | Recommendation |
| --- | --- |
| Deployment Type | Cloud API or Self-Hosted |
| Framework | SentenceTransformers |
| Inference Engine | ONNX Runtime |
| Vector Database | Qdrant |
| Alternative Database | Milvus |
| Retriever | Hybrid Retrieval |
| Re-ranker | BGE Reranker v2 |
| Caching | Redis |
| Monitoring | Prometheus |
| Observability | Grafana |
| API Framework | FastAPI |

---

#### 18.10.9 Qwen3-Embedding-8B

##### General Information

| Property | Value |
| --- | --- |
| Model Name | Qwen3-Embedding-8B |
| Provider | Alibaba Cloud (Qwen Team) |
| Model Type | Text Embedding Model |
| Architecture | Transformer Encoder |
| Release | 2025 |
| License | Apache 2.0 |
| API Available | Yes |
| Self Hosting | ✅ |
| Multilingual | ✅ 100+ Languages |
| Embedding Type | Dense Embedding |
| Primary Task | Semantic Search, RAG, Knowledge Retrieval |

##### Architecture

| Property | Value |
| --- | --- |
| Architecture | Transformer Encoder |
| Parameters | 8 Billion |
| Embedding Dimension | 4,096 |
| Maximum Input Length | 32,768 tokens |
| Pooling Strategy | Mean Pooling |
| Attention | Multi-Head Attention |
| Flash Attention | Supported |
| Quantization | FP16, BF16, INT8, INT4 |
| Self Hosting | Supported |

##### Input Specifications

| Property | Value |
| --- | --- |
| Maximum Input Tokens | 32,768 tokens |
| Recommended Input | 1,000–4,000 tokens |
| Minimum Input | 1 token |
| Batch Embedding | Supported |
| Streaming | ❌ |

##### Embedding Calculation (Example)

```
Lecture Transcript: 96,000 tokens
Chunk Size:          1,000 tokens
Overlap:               150 tokens
Effective Chunk:       850 tokens

Chunk Count = 96,000 / 850 ≈ 113 chunks

Embeddings Generated: 113 dense vectors (each 4,096 dimensions)
```

##### Vector Size per Embedding

| Precision | Calculation | Size |
| --- | --- | --- |
| Float32 | 4,096 × 4 bytes | 16,384 bytes ≈ 16 KB |
| Float16 | 4,096 × 2 bytes | 8,192 bytes ≈ 8 KB |
| INT8 | 4,096 × 1 byte | 4,096 bytes ≈ 4 KB |

##### Storage Requirements at Scale (Float32)

| Chunk Count | Storage |
| --- | --- |
| 100,000 chunks | ≈ 1.6 GB |
| 1,000,000 chunks | ≈ 16 GB |
| 10,000,000 chunks | ≈ 160 GB |

##### Similarity Search

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### API Parameters

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Retrieval Features

| Feature | Support |
| --- | --- |
| Dense Retrieval | ✅ |
| Semantic Search | ⭐⭐⭐⭐⭐ |
| Long Document Retrieval | ⭐⭐⭐⭐⭐ |
| Metadata Filtering | ✅ |
| Batch Embedding | ✅ |
| Multilingual Retrieval | ⭐⭐⭐⭐⭐ |
| Cross-lingual Search | ⭐⭐⭐⭐⭐ |

##### Performance

| Task | Rating |
| --- | --- |
| Educational Notes | ⭐⭐⭐⭐⭐ |
| Research Papers | ⭐⭐⭐⭐⭐ |
| Books | ⭐⭐⭐⭐⭐ |
| Mathematical Documents | ⭐⭐⭐⭐⭐ |
| Code Search | ⭐⭐⭐⭐⭐ |
| OCR Retrieval | ⭐⭐⭐⭐⭐ |
| RAG | ⭐⭐⭐⭐⭐ |

##### Best Use Cases

| Use Case | Rating |
| --- | --- |
| Lecture Transcripts | ⭐⭐⭐⭐⭐ |
| Research Papers | ⭐⭐⭐⭐⭐ |
| Books | ⭐⭐⭐⭐⭐ |
| Programming Documentation | ⭐⭐⭐⭐⭐ |
| Long PDF Search | ⭐⭐⭐⭐⭐ |
| Knowledge Base | ⭐⭐⭐⭐⭐ |
| Enterprise Search | ⭐⭐⭐⭐⭐ |
| Educational AI | ⭐⭐⭐⭐⭐ |

##### Chunk Configuration

| Property | Value |
| --- | --- |
| Recommended Chunk Size | 1,000 tokens |
| Overlap | 150 tokens |
| Maximum Chunk | 4,000 tokens |
| Parent-Child Retrieval | Recommended |
| Metadata | Required |

##### Retrieval Configuration

| Parameter | Value |
| --- | --- |
| Embedding Model | Qwen3-Embedding-8B |
| Chunk Size | 1,000 tokens |
| Overlap | 150 tokens |
| Retriever | Hybrid Search |
| Top-K | 20 |
| Similarity | Cosine |
| Re-ranker | Qwen3 Reranker |

##### Vector Database Recommendations

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Production HNSW Configuration

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Engineering Latency Calculations

| Operation | Estimate |
| --- | --- |
| Embedding latency (average) | 30–80 ms per chunk |
| 100 chunks | ≈ 3–8 seconds |
| Large lecture (113 chunks) | ≈ 4–9 seconds |

##### Limitations

- Embedding only — cannot generate text
- Requires a generative LLM for responses
- Higher storage requirement due to 4,096-dimension vectors
- 4,096-dimension vectors increase index size compared to lower-dimension models
- More GPU memory required than smaller embedding models

##### Evaluation Scores

| Category | Score (out of 10) |
| --- | --- |
| Semantic Retrieval | 10.0 |
| Long Context Retrieval | 10.0 |
| Educational Content | 10.0 |
| Research Papers | 9.9 |
| Mathematics | 9.9 |
| Code Search | 9.9 |
| Multilingual Search | 10.0 |
| Storage Efficiency | 9.2 |
| Production Readiness | 9.9 |
| RAG | 10.0 |

##### Recommended EduScribe AI Tasks

| Task | Suitability |
| --- | --- |
| Lecture Transcript Embedding | ⭐⭐⭐⭐⭐ |
| OCR Embedding | ⭐⭐⭐⭐⭐ |
| Book Embedding | ⭐⭐⭐⭐⭐ |
| Research Paper Search | ⭐⭐⭐⭐⭐ |
| Code Documentation Search | ⭐⭐⭐⭐⭐ |
| Long PDF Retrieval | ⭐⭐⭐⭐⭐ |
| Semantic Search | ⭐⭐⭐⭐⭐ |
| Flashcard Retrieval | ⭐⭐⭐⭐⭐ |
| Quiz Context Search | ⭐⭐⭐⭐⭐ |
| Knowledge Base Search | ⭐⭐⭐⭐⭐ |

##### Deployment Recommendation

| Component | Recommendation |
| --- | --- |
| Deployment Type | Cloud API or Self-Hosted |
| Framework | SentenceTransformers |
| Inference Engine | vLLM |
| Vector Database | Qdrant |
| Alternative Database | Milvus |
| Retriever | Hybrid Retrieval |
| Re-ranker | Qwen3 Reranker |
| Caching | Redis |
| Monitoring | Prometheus |
| Observability | Grafana |
| API Framework | FastAPI |

##### Comparison with BGE-M3

| Feature | Qwen3-Embedding-8B | BGE-M3 |
| --- | --- | --- |
| Parameters | 8B | ~568M |
| Embedding Dimension | 4,096 | 1,024 |
| Maximum Input | 32K tokens | 8K tokens |
| Multilingual | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Long Document Retrieval | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ |
| Educational Content | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Code Search | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ |
| Mathematical Retrieval | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ |
| Vector Size (FP32) | 16 KB | 4 KB |
| Storage Cost | High | Medium |
| Retrieval Accuracy | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Production Cost | Higher | Lower |

---

#### 18.10.10 Qwen3-Embedding-4B

##### General Information

| Property | Value |
| --- | --- |
| Model Name | Qwen3-Embedding-4B |
| Provider | Alibaba Cloud (Qwen Team) |
| Model Type | Text Embedding Model |
| Architecture | Transformer Encoder |
| Release | 2025 |
| License | Apache 2.0 |
| API Available | Yes |
| Self Hosting | ✅ |
| Multilingual | ✅ 100+ Languages |
| Embedding Type | Dense Embedding |
| Primary Task | Semantic Search, RAG, Knowledge Retrieval |

##### Architecture

| Property | Value |
| --- | --- |
| Architecture | Transformer Encoder |
| Parameters | 4 Billion |
| Embedding Dimension | 2,560 |
| Maximum Input Length | 32,768 tokens |
| Pooling Strategy | Mean Pooling |
| Attention | Multi-Head Attention |
| Flash Attention | Supported |
| Quantization | FP16, BF16, INT8, INT4 |
| Self Hosting | Supported |

##### Input Specifications

| Property | Value |
| --- | --- |
| Maximum Input Tokens | 32,768 tokens |
| Recommended Input | 800–3,000 tokens |
| Minimum Input | 1 token |
| Batch Embedding | Supported |
| Streaming | ❌ |

##### Embedding Calculation (Example)

```
Lecture Transcript: 82,000 tokens
Chunk Size:          1,000 tokens
Overlap:               150 tokens
Effective Chunk:       850 tokens

Chunk Count = 82,000 / 850 ≈ 97 chunks

Embeddings Generated: 97 dense vectors (each 2,560 dimensions)
```

##### Vector Size per Embedding

| Precision | Calculation | Size |
| --- | --- | --- |
| Float32 | 2,560 × 4 bytes | 10,240 bytes ≈ 10 KB |
| Float16 | 2,560 × 2 bytes | 5,120 bytes ≈ 5 KB |
| INT8 | 2,560 × 1 byte | 2,560 bytes ≈ 2.5 KB |

##### Storage Requirements at Scale (Float32)

| Chunk Count | Storage |
| --- | --- |
| 100,000 chunks | ≈ 1 GB |
| 1,000,000 chunks | ≈ 10 GB |
| 10,000,000 chunks | ≈ 100 GB |

##### Similarity Search

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### API Parameters

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Retrieval Features

| Feature | Support |
| --- | --- |
| Dense Retrieval | ✅ |
| Semantic Search | ⭐⭐⭐⭐⭐ |
| Long Document Retrieval | ⭐⭐⭐⭐⭐ |
| Multilingual Retrieval | ⭐⭐⭐⭐⭐ |
| Metadata Filtering | ✅ |
| Batch Embedding | ✅ |

##### Performance

| Task | Rating |
| --- | --- |
| Educational Notes | ⭐⭐⭐⭐⭐ |
| Research Papers | ⭐⭐⭐⭐⭐ |
| Books | ⭐⭐⭐⭐⭐ |
| Mathematical Retrieval | ⭐⭐⭐⭐⭐ |
| Code Search | ⭐⭐⭐⭐☆ |
| OCR Retrieval | ⭐⭐⭐⭐⭐ |
| RAG | ⭐⭐⭐⭐⭐ |

##### Best Use Cases

| Use Case | Rating |
| --- | --- |
| Lecture Notes | ⭐⭐⭐⭐⭐ |
| Semantic Search | ⭐⭐⭐⭐⭐ |
| Knowledge Base | ⭐⭐⭐⭐⭐ |
| Research Papers | ⭐⭐⭐⭐⭐ |
| Long PDF Retrieval | ⭐⭐⭐⭐⭐ |
| Educational AI | ⭐⭐⭐⭐⭐ |
| Enterprise Search | ⭐⭐⭐⭐⭐ |
| Multilingual Retrieval | ⭐⭐⭐⭐⭐ |

##### Chunk Configuration

| Property | Value |
| --- | --- |
| Recommended Chunk | 1,000 tokens |
| Overlap | 150 tokens |
| Maximum Chunk | 3,000 tokens |
| Parent-Child Retrieval | Recommended |
| Metadata | Required |

##### Retrieval Configuration

| Parameter | Value |
| --- | --- |
| Embedding Model | Qwen3-Embedding-4B |
| Chunk Size | 1,000 tokens |
| Overlap | 150 tokens |
| Retriever | Hybrid Search |
| Top-K | 20 |
| Similarity | Cosine |
| Re-ranker | Qwen3 Reranker |

##### Vector Database Recommendations

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Production HNSW Configuration

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Engineering Latency Calculations

| Operation | Estimate |
| --- | --- |
| Embedding latency (average) | 20–60 ms per chunk |
| 100 chunks | ≈ 2–6 seconds |
| Large lecture (97 chunks) | ≈ 2–5 seconds |

##### Limitations

- Embedding only — cannot generate text
- Requires an external generative LLM for responses
- Dense retrieval only (no sparse/hybrid native support)
- Higher storage than BGE-M3 due to 2,560-dimension vectors
- Lower retrieval accuracy ceiling than Qwen3-Embedding-8B

##### Evaluation Scores

| Category | Score (out of 10) |
| --- | --- |
| Semantic Retrieval | 9.8 |
| Long Documents | 9.8 |
| Educational Content | 9.9 |
| Mathematics | 9.8 |
| Code Search | 9.7 |
| Multilingual | 9.9 |
| Storage Efficiency | 9.5 |
| Speed | 9.8 |
| Production Readiness | 9.8 |
| RAG | 9.9 |

##### Recommended EduScribe AI Tasks

| Task | Suitability |
| --- | --- |
| Lecture Transcript Embedding | ⭐⭐⭐⭐⭐ |
| OCR Embedding | ⭐⭐⭐⭐⭐ |
| PDF Retrieval | ⭐⭐⭐⭐⭐ |
| Flashcard Search | ⭐⭐⭐⭐⭐ |
| Quiz Retrieval | ⭐⭐⭐⭐⭐ |
| Semantic Search | ⭐⭐⭐⭐⭐ |
| Knowledge Base | ⭐⭐⭐⭐⭐ |
| Long Notes Retrieval | ⭐⭐⭐⭐⭐ |
| Research Paper Search | ⭐⭐⭐⭐⭐ |
| Multilingual Retrieval | ⭐⭐⭐⭐⭐ |

##### Deployment Recommendation

| Component | Recommendation |
| --- | --- |
| Deployment Type | Cloud API or Self-Hosted |
| Framework | SentenceTransformers |
| Inference Engine | vLLM |
| Vector Database | Qdrant |
| Alternative Database | Milvus |
| Retriever | Hybrid Retrieval |
| Re-ranker | Qwen3 Reranker |
| Caching | Redis |
| Monitoring | Prometheus |
| Observability | Grafana |
| API Framework | FastAPI |

##### Comparison with Qwen3-Embedding-8B

| Feature | Qwen3-Embedding-4B | Qwen3-Embedding-8B |
| --- | --- | --- |
| Parameters | 4B | 8B |
| Embedding Dimension | 2,560 | 4,096 |
| Maximum Input | 32K tokens | 32K tokens |
| Vector Size (FP32) | ≈ 10 KB | ≈ 16 KB |
| Storage Cost | Medium | High |
| Retrieval Accuracy | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐+ |
| Speed | Faster | Slightly Slower |
| Memory Usage | Lower | Higher |
| Cost Efficiency | Better | Moderate |
| Best For | Medium-scale production | Enterprise-scale production |

---

#### 18.10.11 NV-Embed-v2

##### General Information

| Property | Value |
| --- | --- |
| Model Name | NV-Embed-v2 |
| Provider | NVIDIA |
| Model Type | Text Embedding Model |
| Architecture | Transformer Encoder |
| Release | 2024 |
| License | NVIDIA Open Model License |
| API Available | Yes |
| Self Hosting | ✅ |
| Multilingual | ✅ |
| Embedding Type | Dense Embedding |
| Primary Task | Enterprise RAG, Semantic Search |

##### Architecture

| Property | Value |
| --- | --- |
| Architecture | Transformer Encoder |
| Parameters | 7 Billion |
| Embedding Dimension | 4,096 |
| Maximum Input Length | 32,768 tokens |
| Pooling Strategy | Mean Pooling |
| Flash Attention | Supported |
| Multi-Head Attention | Supported |
| Quantization | FP16, BF16, INT8, INT4 |
| Self Hosting | Supported |

##### Input Specifications

| Property | Value |
| --- | --- |
| Maximum Input Tokens | 32,768 tokens |
| Recommended Input | 1,000–4,000 tokens |
| Minimum Input | 1 token |
| Batch Embedding | Supported |
| Streaming | ❌ |

##### Embedding Calculation (Example)

```
Lecture Transcript: 120,000 tokens
Chunk Size:           1,200 tokens
Overlap:                200 tokens
Effective Chunk:      1,000 tokens

Chunk Count = 120,000 / 1,000 = 120 chunks

Embeddings Generated: 120 dense vectors (each 4,096 dimensions)
```

##### Vector Size per Embedding

| Precision | Calculation | Size |
| --- | --- | --- |
| Float32 | 4,096 × 4 bytes | 16,384 bytes ≈ 16 KB |
| Float16 | 4,096 × 2 bytes | 8,192 bytes ≈ 8 KB |
| INT8 | 4,096 × 1 byte | 4,096 bytes ≈ 4 KB |

##### Storage Requirements at Scale (Float32)

| Chunk Count | Storage |
| --- | --- |
| 100,000 chunks | ≈ 1.6 GB |
| 1,000,000 chunks | ≈ 16 GB |
| 10,000,000 chunks | ≈ 160 GB |

##### Similarity Search

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### API Parameters

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Retrieval Features

| Feature | Support |
| --- | --- |
| Dense Retrieval | ✅ |
| Semantic Search | ⭐⭐⭐⭐⭐ |
| Long Context Retrieval | ⭐⭐⭐⭐⭐ |
| Enterprise Search | ⭐⭐⭐⭐⭐ |
| Metadata Filtering | ✅ |
| Batch Embedding | ✅ |

##### Performance

| Task | Rating |
| --- | --- |
| Enterprise RAG | ⭐⭐⭐⭐⭐ |
| Educational Documents | ⭐⭐⭐⭐⭐ |
| Books | ⭐⭐⭐⭐⭐ |
| Research Papers | ⭐⭐⭐⭐⭐ |
| OCR Retrieval | ⭐⭐⭐⭐⭐ |
| Code Retrieval | ⭐⭐⭐⭐⭐ |
| Mathematical Retrieval | ⭐⭐⭐⭐⭐ |

##### Best Use Cases

| Use Case | Rating |
| --- | --- |
| Enterprise Knowledge Base | ⭐⭐⭐⭐⭐ |
| Lecture Notes | ⭐⭐⭐⭐⭐ |
| Long PDFs | ⭐⭐⭐⭐⭐ |
| Research Papers | ⭐⭐⭐⭐⭐ |
| Semantic Search | ⭐⭐⭐⭐⭐ |
| AI Search Engine | ⭐⭐⭐⭐⭐ |
| Large Scale RAG | ⭐⭐⭐⭐⭐ |
| Document Intelligence | ⭐⭐⭐⭐⭐ |

##### Chunk Configuration

| Property | Value |
| --- | --- |
| Recommended Chunk | 1,000–1,200 tokens |
| Overlap | 150–200 tokens |
| Maximum Chunk | 4,000 tokens |
| Parent-Child Retrieval | Recommended |
| Metadata | Required |

##### Retrieval Configuration

| Parameter | Value |
| --- | --- |
| Embedding Model | NV-Embed-v2 |
| Chunk Size | 1,200 tokens |
| Overlap | 200 tokens |
| Retriever | Hybrid Search |
| Top-K | 20 |
| Similarity | Cosine |
| Re-ranker | BGE Reranker |

##### Vector Database Recommendations

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Production HNSW Configuration

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Engineering Latency Calculations

| Operation | Estimate |
| --- | --- |
| Embedding latency (average) | 30–80 ms per chunk |
| 100 chunks | ≈ 3–8 seconds |
| Large lecture (120 chunks) | ≈ 4–10 seconds |

##### Limitations

- Embedding only — cannot generate text
- Requires an external generative LLM
- Large vector dimension (4,096) increases index and storage size
- Higher GPU memory requirement than smaller embedding models

##### Evaluation Scores

| Category | Score (out of 10) |
| --- | --- |
| Enterprise Search | 10.0 |
| Semantic Retrieval | 10.0 |
| Educational Content | 9.9 |
| Long Documents | 10.0 |
| Research Papers | 10.0 |
| Code Search | 9.9 |
| Mathematical Retrieval | 9.8 |
| Speed | 9.6 |
| Storage Efficiency | 9.2 |
| Production Readiness | 10.0 |

##### Recommended EduScribe AI Tasks

| Task | Suitability |
| --- | --- |
| Lecture Transcript Embedding | ⭐⭐⭐⭐⭐ |
| OCR Embedding | ⭐⭐⭐⭐⭐ |
| Long PDF Retrieval | ⭐⭐⭐⭐⭐ |
| Research Paper Search | ⭐⭐⭐⭐⭐ |
| Book Search | ⭐⭐⭐⭐⭐ |
| Semantic Search | ⭐⭐⭐⭐⭐ |
| Enterprise Knowledge Base | ⭐⭐⭐⭐⭐ |
| Flashcard Retrieval | ⭐⭐⭐⭐⭐ |
| Quiz Context Search | ⭐⭐⭐⭐⭐ |
| Vector Search | ⭐⭐⭐⭐⭐ |

##### Deployment Recommendation

| Component | Recommendation |
| --- | --- |
| Deployment Type | Cloud API or Self-Hosted |
| Framework | SentenceTransformers |
| Inference Engine | TensorRT-LLM |
| Alternative Inference | vLLM |
| Vector Database | Qdrant |
| Alternative Database | Milvus |
| Retriever | Hybrid Search |
| Re-ranker | BGE Reranker |
| Caching | Redis |
| Monitoring | Prometheus |
| Observability | Grafana |
| API Framework | FastAPI |

##### Comparison with Qwen3-Embedding-8B

| Feature | NV-Embed-v2 | Qwen3-Embedding-8B |
| --- | --- | --- |
| Parameters | 7B | 8B |
| Embedding Dimension | 4,096 | 4,096 |
| Maximum Input | 32K tokens | 32K tokens |
| Long Document Retrieval | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Enterprise Search | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ |
| Educational Retrieval | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Code Search | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Storage Cost | High | High |
| Retrieval Accuracy | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Production Readiness | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

---

#### 18.10.12 Jina Embeddings v3

##### General Information

| Property | Value |
| --- | --- |
| Model Name | Jina Embeddings v3 |
| Provider | Jina AI |
| Model Type | Text Embedding Model |
| Architecture | Transformer Encoder |
| Release | 2024 |
| License | Apache 2.0 |
| API Available | Yes |
| Self Hosting | ✅ |
| Multilingual | ✅ 90+ Languages |
| Embedding Type | Dense Embedding |
| Primary Task | Semantic Search, RAG, Cross-Lingual Retrieval |

##### Architecture

| Property | Value |
| --- | --- |
| Architecture | Transformer Encoder |
| Parameters | ~600 Million |
| Embedding Dimension | 1,024 |
| Maximum Input Length | 8,192 tokens |
| Pooling Strategy | Mean Pooling |
| Attention | Multi-Head Attention |
| Flash Attention | Supported |
| Quantization | FP16, BF16, INT8, INT4 |
| Self Hosting | Supported |

##### Input Specifications

| Property | Value |
| --- | --- |
| Maximum Input Tokens | 8,192 tokens |
| Recommended Input | 500–2,000 tokens |
| Minimum Input | 1 token |
| Batch Embedding | Supported |
| Streaming | ❌ |

##### Embedding Calculation (Example)

```
Lecture Transcript: 56,000 tokens
Chunk Size:            800 tokens
Overlap:               100 tokens
Effective Chunk:       700 tokens

Chunk Count = 56,000 / 700 = 80 chunks

Embeddings Generated: 80 dense vectors (each 1,024 dimensions)
```

##### Vector Size per Embedding

| Precision | Calculation | Size |
| --- | --- | --- |
| Float32 | 1,024 × 4 bytes | 4,096 bytes ≈ 4 KB |
| Float16 | 1,024 × 2 bytes | 2,048 bytes ≈ 2 KB |
| INT8 | 1,024 × 1 byte | 1,024 bytes ≈ 1 KB |

##### Storage Requirements at Scale (Float32)

| Chunk Count | Storage |
| --- | --- |
| 100,000 chunks | ≈ 400 MB |
| 1,000,000 chunks | ≈ 4 GB |
| 10,000,000 chunks | ≈ 40 GB |

##### Similarity Search

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### API Parameters

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Retrieval Features

| Feature | Support |
| --- | --- |
| Dense Retrieval | ✅ |
| Semantic Search | ⭐⭐⭐⭐⭐ |
| Cross-Lingual Search | ⭐⭐⭐⭐⭐ |
| Multilingual Retrieval | ⭐⭐⭐⭐⭐ |
| Batch Embedding | ✅ |
| Metadata Filtering | ✅ |

##### Performance

| Task | Rating |
| --- | --- |
| Educational Notes | ⭐⭐⭐⭐⭐ |
| Multilingual Documents | ⭐⭐⭐⭐⭐ |
| Research Papers | ⭐⭐⭐⭐☆ |
| OCR Retrieval | ⭐⭐⭐⭐⭐ |
| Code Search | ⭐⭐⭐⭐☆ |
| Cross-Language Search | ⭐⭐⭐⭐⭐ |
| RAG | ⭐⭐⭐⭐⭐ |

##### Best Use Cases

| Use Case | Rating |
| --- | --- |
| Multilingual Search | ⭐⭐⭐⭐⭐ |
| Educational AI | ⭐⭐⭐⭐⭐ |
| Knowledge Base | ⭐⭐⭐⭐⭐ |
| Lecture Retrieval | ⭐⭐⭐⭐⭐ |
| Cross-Language Search | ⭐⭐⭐⭐⭐ |
| Enterprise Search | ⭐⭐⭐⭐⭐ |
| Semantic Search | ⭐⭐⭐⭐⭐ |
| Document Retrieval | ⭐⭐⭐⭐⭐ |

##### Chunk Configuration

| Property | Value |
| --- | --- |
| Recommended Chunk | 800 tokens |
| Overlap | 100 tokens |
| Maximum Chunk | 2,000 tokens |
| Parent-Child Retrieval | Recommended |
| Metadata | Required |

##### Retrieval Configuration

| Parameter | Value |
| --- | --- |
| Embedding Model | Jina Embeddings v3 |
| Chunk Size | 800 tokens |
| Overlap | 100 tokens |
| Retriever | Hybrid Search |
| Top-K | 15 |
| Similarity | Cosine |
| Re-ranker | Jina Reranker |

##### Vector Database Recommendations

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Production HNSW Configuration

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Engineering Latency Calculations

| Operation | Estimate |
| --- | --- |
| Embedding latency (average) | 15–40 ms per chunk |
| 100 chunks | ≈ 2–4 seconds |
| Large lecture (80 chunks) | ≈ 1.5–3 seconds |

##### Limitations

- Embedding only — cannot generate text
- Requires an external generative LLM
- Smaller context window than Qwen3-Embedding (8K vs 32K tokens)
- Dense retrieval only (no built-in sparse/hybrid support)
- No built-in reranking

##### Evaluation Scores

| Category | Score (out of 10) |
| --- | --- |
| Semantic Retrieval | 9.8 |
| Multilingual Search | 10.0 |
| Educational Content | 9.8 |
| OCR Retrieval | 9.7 |
| Code Search | 9.4 |
| Long Documents | 9.4 |
| Storage Efficiency | 9.8 |
| Speed | 9.9 |
| Production Readiness | 9.8 |
| RAG | 9.8 |

##### Recommended EduScribe AI Tasks

| Task | Suitability |
| --- | --- |
| Lecture Transcript Embedding | ⭐⭐⭐⭐⭐ |
| Multilingual OCR | ⭐⭐⭐⭐⭐ |
| PDF Search | ⭐⭐⭐⭐⭐ |
| Semantic Search | ⭐⭐⭐⭐⭐ |
| Flashcard Retrieval | ⭐⭐⭐⭐⭐ |
| Quiz Context Search | ⭐⭐⭐⭐⭐ |
| Knowledge Base | ⭐⭐⭐⭐⭐ |
| Cross-Language Search | ⭐⭐⭐⭐⭐ |
| Educational Retrieval | ⭐⭐⭐⭐⭐ |
| Enterprise Search | ⭐⭐⭐⭐⭐ |

##### Deployment Recommendation

| Component | Recommendation |
| --- | --- |
| Deployment Type | Cloud API or Self-Hosted |
| Framework | SentenceTransformers |
| Inference Engine | ONNX Runtime |
| Alternative Inference | vLLM |
| Vector Database | Qdrant |
| Alternative Database | Milvus |
| Retriever | Hybrid Retrieval |
| Re-ranker | Jina Reranker |
| Caching | Redis |
| Monitoring | Prometheus |
| Observability | Grafana |
| API Framework | FastAPI |

##### Comparison with BGE-M3

| Feature | Jina Embeddings v3 | BGE-M3 |
| --- | --- | --- |
| Parameters | ~600M | ~568M |
| Embedding Dimension | 1,024 | 1,024 |
| Maximum Input | 8K tokens | 8K tokens |
| Multilingual Support | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Cross-Language Search | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ |
| Hybrid Retrieval | ❌ | ✅ |
| Dense Retrieval | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Storage Cost | Low | Low |
| Speed | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ |
| Production Readiness | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

---

#### 18.10.13 e5-Mistral-7B-Instruct

##### General Information

| Property | Value |
| --- | --- |
| Model Name | e5-Mistral-7B-Instruct |
| Provider | Microsoft Research + Mistral AI |
| Model Type | Text Embedding Model |
| Architecture | Transformer Encoder (Mistral-7B backbone) |
| Release | 2024 |
| License | Apache 2.0 |
| API Available | Yes |
| Self Hosting | ✅ |
| Multilingual | ✅ |
| Embedding Type | Dense Embedding |
| Primary Task | Semantic Search, Long Document Retrieval, RAG |

##### Architecture

| Property | Value |
| --- | --- |
| Architecture | Transformer Encoder |
| Parameters | 7 Billion |
| Embedding Dimension | 4,096 |
| Maximum Input Length | 8,192 tokens |
| Pooling Strategy | Mean Pooling |
| Flash Attention | Supported |
| Attention | Multi-Head Attention |
| Quantization | FP16, BF16, INT8, INT4 |
| Self Hosting | Supported |

##### Input Specifications

| Property | Value |
| --- | --- |
| Maximum Input Tokens | 8,192 tokens |
| Recommended Input | 1,000–3,000 tokens |
| Minimum Input | 1 token |
| Batch Embedding | Supported |
| Streaming | ❌ |

##### Embedding Calculation (Example)

```
Lecture Transcript: 68,000 tokens
Chunk Size:          1,000 tokens
Overlap:               150 tokens
Effective Chunk:       850 tokens

Chunk Count = 68,000 / 850 ≈ 80 chunks

Embeddings Generated: 80 dense vectors (each 4,096 dimensions)
```

##### Vector Size per Embedding

| Precision | Calculation | Size |
| --- | --- | --- |
| Float32 | 4,096 × 4 bytes | 16,384 bytes ≈ 16 KB |
| Float16 | 4,096 × 2 bytes | 8,192 bytes ≈ 8 KB |
| INT8 | 4,096 × 1 byte | 4,096 bytes ≈ 4 KB |

##### Storage Requirements at Scale (Float32)

| Chunk Count | Storage |
| --- | --- |
| 100,000 chunks | ≈ 1.6 GB |
| 1,000,000 chunks | ≈ 16 GB |
| 10,000,000 chunks | ≈ 160 GB |

##### Similarity Search

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### API Parameters

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Retrieval Features

| Feature | Support |
| --- | --- |
| Dense Retrieval | ✅ |
| Semantic Search | ⭐⭐⭐⭐⭐ |
| Long Document Retrieval | ⭐⭐⭐⭐⭐ |
| Instruction-Based Retrieval | ⭐⭐⭐⭐⭐ |
| Batch Embedding | ✅ |
| Metadata Filtering | ✅ |

##### Performance

| Task | Rating |
| --- | --- |
| Educational Documents | ⭐⭐⭐⭐⭐ |
| Research Papers | ⭐⭐⭐⭐⭐ |
| Long PDFs | ⭐⭐⭐⭐⭐ |
| Books | ⭐⭐⭐⭐⭐ |
| OCR Retrieval | ⭐⭐⭐⭐☆ |
| Code Retrieval | ⭐⭐⭐⭐☆ |
| RAG | ⭐⭐⭐⭐⭐ |

##### Best Use Cases

| Use Case | Rating |
| --- | --- |
| Long Lecture Notes | ⭐⭐⭐⭐⭐ |
| Research Papers | ⭐⭐⭐⭐⭐ |
| Books | ⭐⭐⭐⭐⭐ |
| Instruction-Based Search | ⭐⭐⭐⭐⭐ |
| Enterprise Search | ⭐⭐⭐⭐⭐ |
| Educational AI | ⭐⭐⭐⭐⭐ |
| Semantic Search | ⭐⭐⭐⭐⭐ |
| Knowledge Base | ⭐⭐⭐⭐⭐ |

##### Chunk Configuration

| Property | Value |
| --- | --- |
| Recommended Chunk | 1,000 tokens |
| Overlap | 150 tokens |
| Maximum Chunk | 3,000 tokens |
| Parent-Child Retrieval | Recommended |
| Metadata | Required |

##### Retrieval Configuration

| Parameter | Value |
| --- | --- |
| Embedding Model | e5-Mistral-7B-Instruct |
| Chunk Size | 1,000 tokens |
| Overlap | 150 tokens |
| Retriever | Dense Retrieval |
| Top-K | 20 |
| Similarity | Cosine |
| Re-ranker | BGE Reranker |

##### Vector Database Recommendations

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Production HNSW Configuration

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Engineering Latency Calculations

| Operation | Estimate |
| --- | --- |
| Embedding latency (average) | 30–70 ms per chunk |
| 100 chunks | ≈ 3–7 seconds |
| Large lecture (80 chunks) | ≈ 2–6 seconds |

##### Limitations

- Embedding only — cannot generate text
- Requires an external generative LLM
- Large embedding size (4,096-dim) increases storage cost
- No native hybrid or sparse retrieval support
- Smaller context window than Qwen3-Embedding (8K vs 32K tokens)

##### Evaluation Scores

| Category | Score (out of 10) |
| --- | --- |
| Semantic Retrieval | 9.9 |
| Long Documents | 10.0 |
| Educational Content | 9.9 |
| Research Papers | 10.0 |
| OCR Retrieval | 9.5 |
| Code Search | 9.6 |
| Storage Efficiency | 9.2 |
| Speed | 9.6 |
| Production Readiness | 9.8 |
| RAG | 9.9 |

##### Recommended EduScribe AI Tasks

| Task | Suitability |
| --- | --- |
| Lecture Transcript Embedding | ⭐⭐⭐⭐⭐ |
| Research Paper Retrieval | ⭐⭐⭐⭐⭐ |
| Long PDF Search | ⭐⭐⭐⭐⭐ |
| Semantic Search | ⭐⭐⭐⭐⭐ |
| Knowledge Base | ⭐⭐⭐⭐⭐ |
| Educational RAG | ⭐⭐⭐⭐⭐ |
| Book Retrieval | ⭐⭐⭐⭐⭐ |
| Flashcard Search | ⭐⭐⭐⭐⭐ |
| Quiz Context Search | ⭐⭐⭐⭐⭐ |
| Enterprise Search | ⭐⭐⭐⭐⭐ |

##### Deployment Recommendation

| Component | Recommendation |
| --- | --- |
| Deployment Type | Cloud API or Self-Hosted |
| Framework | SentenceTransformers |
| Inference Engine | vLLM |
| Alternative Inference | ONNX Runtime |
| Vector Database | Qdrant |
| Alternative Database | Milvus |
| Retriever | Dense Retrieval |
| Re-ranker | BGE Reranker |
| Caching | Redis |
| Monitoring | Prometheus |
| Observability | Grafana |
| API Framework | FastAPI |

##### Comparison with Qwen3-Embedding-8B

| Feature | e5-Mistral-7B | Qwen3-Embedding-8B |
| --- | --- | --- |
| Parameters | 7B | 8B |
| Embedding Dimension | 4,096 | 4,096 |
| Maximum Input | 8K tokens | 32K tokens |
| Long Document Retrieval | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Research Papers | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Educational Content | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Code Search | ⭐⭐⭐⭐☆ | ⭐⭐⭐⭐⭐ |
| Storage Cost | High | High |
| Retrieval Accuracy | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Production Readiness | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

---

#### 18.10.14 bge-base-en-v1.5

##### General Information

| Property | Value |
| --- | --- |
| Model Name | bge-base-en-v1.5 |
| Provider | BAAI (Beijing Academy of Artificial Intelligence) |
| Model Type | Text Embedding Model |
| Architecture | Transformer Encoder |
| Release | 2023 |
| License | MIT |
| API Available | Yes |
| Self Hosting | ✅ |
| Multilingual | ❌ (English only) |
| Embedding Type | Dense Embedding |
| Primary Task | Semantic Search, RAG |

##### Architecture

| Property | Value |
| --- | --- |
| Architecture | Transformer Encoder |
| Parameters | 109 Million |
| Embedding Dimension | 768 |
| Maximum Input Length | 512 tokens |
| Pooling Strategy | CLS Pooling |
| Attention | Multi-Head Attention |
| Flash Attention | Not required |
| Quantization | FP16, INT8, INT4 |
| Self Hosting | Supported |

##### Input Specifications

| Property | Value |
| --- | --- |
| Maximum Input Tokens | 512 tokens |
| Recommended Input | 256–512 tokens |
| Minimum Input | 1 token |
| Batch Embedding | Supported |
| Streaming | ❌ |

##### Embedding Calculation (Example)

```
Lecture Transcript: 36,000 tokens
Chunk Size:            500 tokens
Overlap:               100 tokens
Effective Chunk:       400 tokens

Chunk Count = 36,000 / 400 = 90 chunks

Embeddings Generated: 90 dense vectors (each 768 dimensions)
```

##### Vector Size per Embedding

| Precision | Calculation | Size |
| --- | --- | --- |
| Float32 | 768 × 4 bytes | 3,072 bytes ≈ 3 KB |
| Float16 | 768 × 2 bytes | 1,536 bytes ≈ 1.5 KB |
| INT8 | 768 × 1 byte | 768 bytes ≈ 0.75 KB |

##### Storage Requirements at Scale (Float32)

| Chunk Count | Storage |
| --- | --- |
| 100,000 chunks | ≈ 300 MB |
| 1,000,000 chunks | ≈ 3 GB |
| 10,000,000 chunks | ≈ 30 GB |

##### Similarity Search

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### API Parameters

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Retrieval Features

| Feature | Support |
| --- | --- |
| Dense Retrieval | ✅ |
| Semantic Search | ⭐⭐⭐⭐⭐ |
| Batch Embedding | ✅ |
| Metadata Filtering | ✅ |
| Long Document Retrieval | ⭐⭐⭐☆☆ |
| Hybrid Retrieval | External BM25 required |

##### Performance

| Task | Rating |
| --- | --- |
| Educational Notes | ⭐⭐⭐⭐☆ |
| Research Papers | ⭐⭐⭐⭐☆ |
| Books | ⭐⭐⭐⭐☆ |
| OCR Retrieval | ⭐⭐⭐⭐☆ |
| Code Search | ⭐⭐⭐⭐☆ |
| Long Documents | ⭐⭐⭐☆☆ |
| RAG | ⭐⭐⭐⭐☆ |

##### Best Use Cases

| Use Case | Rating |
| --- | --- |
| Small RAG Systems | ⭐⭐⭐⭐⭐ |
| CPU Deployment | ⭐⭐⭐⭐⭐ |
| Fast Semantic Search | ⭐⭐⭐⭐⭐ |
| English Documents | ⭐⭐⭐⭐⭐ |
| Internal Search | ⭐⭐⭐⭐⭐ |
| Low-Cost Production | ⭐⭐⭐⭐⭐ |
| Educational Chatbot | ⭐⭐⭐⭐☆ |
| Knowledge Base | ⭐⭐⭐⭐☆ |

##### Chunk Configuration

| Property | Value |
| --- | --- |
| Recommended Chunk | 500 tokens |
| Overlap | 100 tokens |
| Maximum Chunk | 512 tokens |
| Parent-Child Retrieval | Optional |
| Metadata | Recommended |

##### Retrieval Configuration

| Parameter | Value |
| --- | --- |
| Embedding Model | bge-base-en-v1.5 |
| Chunk Size | 500 tokens |
| Overlap | 100 tokens |
| Retriever | Dense Retrieval |
| Top-K | 10–15 |
| Similarity | Cosine |
| Re-ranker | BGE Reranker |

##### Vector Database Recommendations

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Production HNSW Configuration

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Engineering Latency Calculations

| Operation | Estimate |
| --- | --- |
| Embedding latency (average) | 5–15 ms per chunk |
| 100 chunks | ≈ 0.5–1.5 seconds |
| Large lecture (90 chunks) | ≈ 0.5–1.2 seconds |

##### Memory Requirements

| Precision | Estimated Memory |
| --- | --- |
| FP32 | ~450 MB |
| FP16 | ~220 MB |
| INT8 | ~110 MB |
| INT4 | ~60 MB |

##### Limitations

- English only — no multilingual support
- 512-token maximum input limit
- Embedding only — cannot generate text
- Requires an external generative LLM
- Not ideal for very long documents (exceeds 512-token limit)

##### Evaluation Scores

| Category | Score (out of 10) |
| --- | --- |
| Speed | 10.0 |
| Storage Efficiency | 10.0 |
| CPU Deployment | 10.0 |
| Cost Efficiency | 10.0 |
| Production Readiness | 9.8 |
| Semantic Retrieval | 9.4 |
| Code Search | 9.2 |
| Educational Content | 9.2 |
| RAG | 9.3 |
| Long Documents | 8.8 |

##### Recommended EduScribe AI Tasks

| Task | Suitability |
| --- | --- |
| Flashcard Retrieval | ⭐⭐⭐⭐⭐ |
| Quiz Retrieval | ⭐⭐⭐⭐⭐ |
| Short Note Search | ⭐⭐⭐⭐⭐ |
| FAQ Search | ⭐⭐⭐⭐⭐ |
| Small Knowledge Base | ⭐⭐⭐⭐⭐ |
| CPU Deployment | ⭐⭐⭐⭐⭐ |
| Edge Deployment | ⭐⭐⭐⭐⭐ |
| Local Development | ⭐⭐⭐⭐⭐ |
| Prototype RAG | ⭐⭐⭐⭐⭐ |
| Educational Search | ⭐⭐⭐⭐☆ |

##### Deployment Recommendation

| Component | Recommendation |
| --- | --- |
| Deployment Type | Cloud API or Self-Hosted |
| Framework | SentenceTransformers |
| Inference Engine | ONNX Runtime |
| Alternative Inference | PyTorch |
| Vector Database | Qdrant |
| Alternative Database | Chroma |
| Retriever | Dense Retrieval |
| Re-ranker | BGE Reranker |
| Caching | Redis |
| Monitoring | Prometheus |
| Observability | Grafana |
| API Framework | FastAPI |

##### Comparison with BGE-M3

| Feature | bge-base-en-v1.5 | BGE-M3 |
| --- | --- | --- |
| Parameters | 109M | ~568M |
| Embedding Dimension | 768 | 1,024 |
| Maximum Input | 512 tokens | 8,192 tokens |
| Storage Per Vector | ≈ 3 KB | ≈ 4 KB |
| CPU Speed | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ |
| Long Documents | ⭐⭐⭐☆☆ | ⭐⭐⭐⭐⭐ |
| Multilingual | ❌ | ✅ |
| Hybrid Search | ❌ | ✅ |
| Production Cost | Very Low | Medium |
| Best Use Case | Lightweight RAG | Enterprise RAG |

---

#### 18.10.15 bge-small-en-v1.5

##### General Information

| Property | Value |
| --- | --- |
| Model Name | bge-small-en-v1.5 |
| Provider | BAAI (Beijing Academy of Artificial Intelligence) |
| Model Type | Text Embedding Model |
| Architecture | Transformer Encoder |
| Release | 2023 |
| License | MIT |
| API Available | Yes |
| Self Hosting | ✅ |
| Multilingual | ❌ (English only) |
| Embedding Type | Dense Embedding |
| Primary Task | Lightweight Semantic Search, RAG |

##### Architecture

| Property | Value |
| --- | --- |
| Architecture | Transformer Encoder |
| Parameters | 33 Million |
| Embedding Dimension | 384 |
| Maximum Input Length | 512 tokens |
| Pooling Strategy | CLS Pooling |
| Attention | Multi-Head Attention |
| Flash Attention | Not required |
| Quantization | FP16, INT8, INT4 |
| Self Hosting | Supported |

##### Input Specifications

| Property | Value |
| --- | --- |
| Maximum Input Tokens | 512 tokens |
| Recommended Input | 256–512 tokens |
| Minimum Input | 1 token |
| Batch Embedding | Supported |
| Streaming | ❌ |

##### Embedding Calculation (Example)

```
Lecture Transcript: 20,000 tokens
Chunk Size:            500 tokens
Overlap:               100 tokens
Effective Chunk:       400 tokens

Chunk Count = 20,000 / 400 = 50 chunks

Embeddings Generated: 50 dense vectors (each 384 dimensions)
```

##### Vector Size per Embedding

| Precision | Calculation | Size |
| --- | --- | --- |
| Float32 | 384 × 4 bytes | 1,536 bytes ≈ 1.5 KB |
| Float16 | 384 × 2 bytes | 768 bytes ≈ 0.75 KB |
| INT8 | 384 × 1 byte | 384 bytes ≈ 0.375 KB |

##### Storage Requirements at Scale (Float32)

| Chunk Count | Storage |
| --- | --- |
| 100,000 chunks | ≈ 150 MB |
| 1,000,000 chunks | ≈ 1.5 GB |
| 10,000,000 chunks | ≈ 15 GB |

##### Similarity Search

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### API Parameters

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Retrieval Features

| Feature | Support |
| --- | --- |
| Dense Retrieval | ✅ |
| Semantic Search | ⭐⭐⭐⭐☆ |
| Batch Embedding | ✅ |
| Metadata Filtering | ✅ |
| CPU Optimized | ✅ |
| Hybrid Search | External BM25 required |

##### Performance

| Task | Rating |
| --- | --- |
| Semantic Search | ⭐⭐⭐⭐☆ |
| Educational Notes | ⭐⭐⭐⭐☆ |
| Small Knowledge Base | ⭐⭐⭐⭐⭐ |
| OCR Retrieval | ⭐⭐⭐⭐☆ |
| Code Search | ⭐⭐⭐⭐☆ |
| Long Documents | ⭐⭐⭐☆☆ |
| RAG | ⭐⭐⭐⭐☆ |

##### Best Use Cases

| Use Case | Rating |
| --- | --- |
| Local Development | ⭐⭐⭐⭐⭐ |
| CPU Deployment | ⭐⭐⭐⭐⭐ |
| Edge Devices | ⭐⭐⭐⭐⭐ |
| Prototype RAG | ⭐⭐⭐⭐⭐ |
| Small Chatbots | ⭐⭐⭐⭐⭐ |
| FAQ Search | ⭐⭐⭐⭐⭐ |
| Internal Search | ⭐⭐⭐⭐⭐ |
| Educational Search | ⭐⭐⭐⭐☆ |

##### Chunk Configuration

| Property | Value |
| --- | --- |
| Recommended Chunk | 500 tokens |
| Overlap | 100 tokens |
| Maximum Chunk | 512 tokens |
| Parent-Child Retrieval | Optional |
| Metadata | Recommended |

##### Retrieval Configuration

| Parameter | Value |
| --- | --- |
| Embedding Model | bge-small-en-v1.5 |
| Chunk Size | 500 tokens |
| Overlap | 100 tokens |
| Retriever | Dense Retrieval |
| Top-K | 10 |
| Similarity | Cosine |
| Re-ranker | BGE Reranker |

##### Vector Database Recommendations

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Production HNSW Configuration

*Standard configuration applies — see [Section 18.10.0](#18100-common-embedding-model-configuration).*

##### Engineering Latency Calculations

| Operation | Estimate |
| --- | --- |
| Embedding latency (average) | 2–8 ms per chunk |
| 100 chunks | ≈ 0.2–0.8 seconds |
| Large lecture (50 chunks) | ≈ 0.1–0.4 seconds |

##### Memory Requirements

| Precision | Estimated Memory |
| --- | --- |
| FP32 | ~130 MB |
| FP16 | ~65 MB |
| INT8 | ~35 MB |
| INT4 | ~18 MB |

##### Limitations

- English only — no multilingual support
- 512-token maximum input limit
- Embedding only — cannot generate text
- Requires an external generative LLM
- Lower retrieval accuracy than larger embedding models
- Not recommended for enterprise-scale RAG

##### Evaluation Scores

| Category | Score (out of 10) |
| --- | --- |
| Speed | 10.0 |
| Storage Efficiency | 10.0 |
| CPU Deployment | 10.0 |
| Edge Deployment | 10.0 |
| Cost Efficiency | 10.0 |
| Production Readiness | 9.4 |
| Semantic Retrieval | 9.0 |
| Educational Content | 9.0 |
| RAG | 9.1 |
| Long Documents | 8.5 |

##### Recommended EduScribe AI Tasks

| Task | Suitability |
| --- | --- |
| Prototype Development | ⭐⭐⭐⭐⭐ |
| Flashcard Search | ⭐⭐⭐⭐⭐ |
| Quiz Search | ⭐⭐⭐⭐⭐ |
| FAQ Retrieval | ⭐⭐⭐⭐⭐ |
| Local Testing | ⭐⭐⭐⭐⭐ |
| CPU Deployment | ⭐⭐⭐⭐⭐ |
| Edge AI | ⭐⭐⭐⭐⭐ |
| Small Knowledge Base | ⭐⭐⭐⭐⭐ |
| Internal Search | ⭐⭐⭐⭐⭐ |
| Demo Applications | ⭐⭐⭐⭐⭐ |

##### Deployment Recommendation

| Component | Recommendation |
| --- | --- |
| Deployment Type | Cloud API or Self-Hosted |
| Framework | SentenceTransformers |
| Inference Engine | ONNX Runtime |
| Alternative Inference | PyTorch |
| Vector Database | Chroma |
| Alternative Database | Qdrant |
| Retriever | Dense Retrieval |
| Re-ranker | BGE Reranker |
| Caching | Redis |
| Monitoring | Prometheus |
| Observability | Grafana |
| API Framework | FastAPI |

##### Comparison with bge-base-en-v1.5

| Feature | bge-small-en-v1.5 | bge-base-en-v1.5 |
| --- | --- | --- |
| Parameters | 33M | 109M |
| Embedding Dimension | 384 | 768 |
| Maximum Input | 512 tokens | 512 tokens |
| Vector Size (FP32) | ≈ 1.5 KB | ≈ 3 KB |
| Storage Cost | Very Low | Low |
| CPU Speed | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ |
| Retrieval Accuracy | ⭐⭐⭐⭐☆ | ⭐⭐⭐⭐⭐ |
| Memory Usage | Very Low | Low |
| Production Cost | Very Low | Low |
| Best Use Case | Edge and CPU deployment | General production RAG |

---

---

### 18.11 Free Tier Quota Reference (2025/2026)

> [!IMPORTANT]
> Rate limits are set by providers and change without notice. The values below reflect publicly documented limits as of mid-2025. Always verify at the provider's official rate-limits or settings page before deploying. Limits marked *(verify)* are approximate based on community reports.

| Provider | Free Plan Name | RPM | RPD | TPM | Tokens/Day | Context Window | Reset Time | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **Google AI Studio** | Free tier | ~15 *(verify)* | ~1,500 *(verify)* | ~1M *(verify)* | Not separately capped | 1,000,000 | Midnight Pacific | Per Google Cloud project; check dashboard for real limits |
| **OpenRouter** | Free (no purchase) | 20 | **50** | Not capped | Not capped | Model-dependent | Midnight UTC | 50 RPD is the critical bottleneck — upgrade to $10 lifetime credit for 1,000 RPD |
| **OpenRouter** | Free ($10+ lifetime) | 20 | **1,000** | Not capped | Not capped | Model-dependent | Midnight UTC | One-time $10 purchase permanently unlocks 1,000 RPD |
| **Cerebras** | Free | 30 | Unlimited | Not capped | **1M tokens** | 64K–128K | 24 hours rolling | Fastest inference available (2,600+ tok/s); 1M token/day soft limit |
| **Groq** | Free | 30 | **14,400** | 30,000 | Not separately capped | 128K | Midnight UTC | Per-organisation; prompt caching tokens do not count toward TPM |
| **Together AI** | Free models (perpetual) | Dynamic | Dynamic | Dynamic | Dynamic | 128K | N/A | Free model IDs: `Llama-3.3-70B-Instruct-Turbo-Free`, `DeepSeek-R1-Distill-Llama-70B-Free` |
| **SambaNova** | Dev tier (free credits) | *(verify)* | *(verify)* | *(verify)* | **20M tokens** | 128K | 24 hours | $5 credit on signup; 20M tokens/day across all models |
| **Mistral (la Plateforme)** | Experiment / Free | ~30 (1 RPS) | Not specified | Not specified | Not specified | 128K | N/A | Free tier is for testing only; production workloads require Scale plan |

**Key observations for EduScribe AI:**
- **OpenRouter's 50 RPD hard limit** (without the $10 purchase) is the tightest bottleneck in the entire stack — a single lecture processing run can exhaust it. **The $10 lifetime credit upgrade is strongly recommended** and unlocks 1,000 RPD permanently.
- **Groq's 14,400 RPD** at 30 RPM is the most generous truly-free daily quota in the stack, making it the ideal high-throughput Tier 2 workhorse for batch tasks (Markdown formatting, JSON extraction, flashcard generation).
- **Cerebras's 1M token/day** limit is generous for sequential processing but can be exhausted quickly under parallel Celery workers processing multiple lectures simultaneously.
- **Gemini 2.5 Flash's exact RPD** is not publicly documented — Google advises checking the per-project quota dashboard. Treat it as the primary but not the sole Tier 1 provider.

---

### 18.12 Quota-Aware Routing Logic

The `quota_tracker.py` component maintains a real-time view of quota consumption per provider, backed by Redis. It is the source of truth the `llm_manager.py` consults before every call to determine which tier to use.

#### 18.12.1 Quota State Model (Redis Schema)

```
quota:{provider}:rpm_count     — current minute request count (TTL: 60s)
quota:{provider}:rpd_count     — current day request count (TTL: 86400s)
quota:{provider}:tpd_count     — current day token count (TTL: 86400s)
quota:{provider}:blocked       — "1" if provider declared exhausted (TTL: until reset)
```

Each key uses Redis TTL for automatic expiry — no cron job needed to reset daily counters.

#### 18.12.2 Quota Check Algorithm

```python
def get_available_tier(task_type: TaskType) -> str:
    # Returns the best available model alias for the given task,
    # descending through tiers until one with available quota is found.
    config = get_model_config(task_type)
    candidates = [config.primary, config.secondary, config.emergency]

    for model_alias in candidates:
        provider = resolve_provider(model_alias)
        if not quota_tracker.is_blocked(provider):
            if quota_tracker.has_rpm_headroom(provider):
                if quota_tracker.has_rpd_headroom(provider):
                    return model_alias

    # All candidates exhausted — queue request for next quota reset
    raise QuotaExhaustedException(
        f"All models for task {task_type} exhausted. "
        f"Next reset: {quota_tracker.next_reset_time()}"
    )
```

#### 18.12.3 HTTP Error → Action Mapping

This table governs the `retry_manager.py` and `fallback_manager.py` behaviour for each HTTP error code returned by a provider:

| HTTP Status | Meaning | Action | Notes |
| --- | --- | --- | --- |
| `200 OK` | Success | Return response | — |
| `429 Too Many Requests` | Rate limited | Rotate API key → retry × 3 (exp. backoff) → if all keys exhausted, mark provider blocked → next provider | Check `Retry-After` header if present |
| `400 Bad Request` | Malformed request / prompt too long | **Fail fast** — no retry | Log and alert; likely a prompt engineering error |
| `401 Unauthorized` | Invalid API key | **Fail fast** — rotate to next key immediately | Do not retry with same key |
| `403 Forbidden` | Account restricted / model unavailable | Mark provider blocked → next provider | Do not retry same provider |
| `408 Request Timeout` | Slow provider | Retry × 2 → next provider | Common with large generation requests |
| `500 Internal Server Error` | Provider-side error | Retry × 3 (5s, 15s, 30s) → next provider | Transient; usually resolves quickly |
| `502 Bad Gateway` | Provider infrastructure issue | Retry × 2 → next provider | |
| `503 Service Unavailable` | Provider maintenance / overloaded | Mark provider blocked (5 min) → next provider | Check `Retry-After` if present |

**Key rules:**
- **400 and 401 never retry** — they indicate a permanent error that retrying cannot resolve.
- **429 always rotates the API key first** before escalating to the next provider, since quota is per-key.
- **5xx errors retry** with backoff — they are transient infrastructure failures, not application errors.

#### 18.12.4 Quota Reset Schedule

| Provider | Reset Time | Reset Granularity |
| --- | --- | --- |
| Google AI Studio | Midnight Pacific Time (PT) | Per day |
| OpenRouter | Midnight UTC | Per day |
| Groq | Midnight UTC | Per day |
| Cerebras | Rolling 24-hour window from first request | Per 24h |
| SambaNova | Rolling 24-hour window | Per 24h |
| Together AI | Dynamic (no fixed reset) | Rolling |
| Mistral | Not specified | Likely per day |

The `quota_tracker` sets Redis TTLs based on the seconds remaining until the next reset for each provider. This ensures daily counters expire precisely when the provider's quota refreshes, not at a fixed midnight-UTC that may not match the provider's actual reset time.

---

## 19. Retry Strategy

The Retry Strategy defines how EduScribe AI handles transient failures from external LLM providers, using exponential backoff before escalating to provider fallback.

**Objective:** The objective of the Retry Strategy is to prevent temporary network errors or server overload from immediately terminating the processing pipeline, while avoiding unnecessary, rapid-fire repeated requests against a provider that is currently struggling.

### 19.3 Detailed Explanation
External API failures are treated as an expected, routine occurrence rather than an exceptional one. Whenever a request fails, the system waits a short interval before retrying, with the waiting period increasing after each subsequent failure — a technique known as exponential backoff:

| Attempt | Behavior |
| --- | --- |
| 1 | Immediate |
| 2 | Wait 2 seconds |
| 3 | Wait 4 seconds |
| 4 | Wait 8 seconds |
| 5 | Switch provider |

Retries are explicitly scoped to **transient** failures only — timeouts and rate limiting. **Permanent** errors, such as invalid API keys or malformed requests, are reported immediately rather than retried, since retrying a permanent error would only waste time and quota without any possibility of success. The **Tenacity** library is the technology-stack component responsible for implementing this automatic retry mechanism.

### 19.4 Internal Workflow

```mermaid
flowchart TD
    A[LLM Request] --> B{Success?}
    B -->|Yes| C[Return Response]
    B -->|No: transient failure - timeout/rate limit| D[Attempt 1: Immediate retry]
    D --> E{Success?}
    E -->|No| F[Attempt 2: Wait 2s]
    F --> G{Success?}
    G -->|No| H[Attempt 3: Wait 4s]
    H --> I{Success?}
    I -->|No| J[Attempt 4: Wait 8s]
    J --> K{Success?}
    K -->|No| L[Attempt 5: Switch Provider]
    B -->|No: permanent failure - bad key/malformed request| M[Fail immediately - no retry]
```

**Rationale:** Exponential backoff is used instead of fixed-interval retry because it reduces the total volume of requests sent to a provider that is already under load or rate-limiting the caller, while still giving the request multiple opportunities to succeed before the more disruptive step of switching providers entirely. The explicit distinction between transient and permanent failures prevents the retry mechanism from wasting attempts on errors that retrying can never resolve.

### 19.6 Advantages
- Prevents unnecessary API requests during provider-side instability, reducing wasted quota consumption.
- Increases the probability of successful execution without requiring an immediate provider switch for every minor, transient hiccup.
- Fails fast on permanent errors, avoiding wasted retry cycles on unrecoverable failures.

**Summary:** The Retry Strategy is the first line of defense against transient LLM provider instability, exhausting a bounded, exponentially-spaced set of retry attempts on the current provider before escalating to the Fallback Strategy (Chapter 20), which switches to an entirely different provider.

---

## 20. Fallback Strategy

The Fallback Strategy defines the ordered sequence of alternative LLM providers EduScribe AI switches to when the currently selected provider becomes unavailable, whether due to quota exhaustion, timeout, rate limiting, server errors, or scheduled maintenance.

**Objective:** The objective of the Fallback Strategy is to guarantee that note generation continues uninterrupted even when an individual provider is completely unavailable, without requiring any manual intervention from the user.

### 20.3 Detailed Explanation
The fallback order follows a fixed priority sequence:

```
Gemini 2.5 Flash
  ↓
Gemini 2.5 Flash Lite
  ↓
DeepSeek V3
  ↓
Qwen3
  ↓
Llama 3.3
  ↓
Mistral
  ↓
GLM
```

Whenever the current provider fails due to any of the following conditions — API quota exceeded, timeout, rate limiting, server errors, or temporary maintenance — the request is automatically retried using the next available provider in this sequence. This transition is entirely transparent to the user: no manual model switching is ever required. The fallback sequence is executed only after the Retry Strategy's (Chapter 19) exponential backoff attempts on the current provider have been exhausted, meaning fallback is the escalation path for failures that retry alone could not resolve.

### 20.4 Internal Workflow

```mermaid
flowchart LR
    A[Gemini 2.5 Flash] -->|fails after retries| B[Gemini 2.5 Flash Lite]
    B -->|fails after retries| C[DeepSeek V3]
    C -->|fails after retries| D[Qwen3]
    D -->|fails after retries| E[Llama 3.3]
    E -->|fails after retries| F[Mistral]
    F -->|fails after retries| G[GLM]
```

**Rationale:** The fallback sequence is ordered to prioritize the provider identified as strongest for educational content generation (Gemini) first, followed by progressively different but still capable open-source models accessed through OpenRouter and other integrated providers. This ordering ensures that the pipeline attempts the highest-quality option first and only degrades to alternative models when genuinely necessary, rather than load-balancing arbitrarily across all providers from the outset.

### 20.6 Advantages
- Higher availability: the pipeline continues functioning even if one, or several, providers are simultaneously unavailable.
- Better fault tolerance, since the fallback chain contains seven distinct provider/model options before the pipeline would be forced to halt entirely.
- Zero required user intervention: the source documentation explicitly states the user never needs to manually change models.

**Summary:** The Fallback Strategy is the mechanism that fulfills the "Automatic Fallback" feature principle described in Chapter 2 and the high-availability non-functional requirement described in Chapter 1, ensuring that a single provider's unavailability degrades the system's model quality at worst, rather than halting the note-generation pipeline entirely.

---

## 21. API Key Rotation

API Key Rotation is a fine-grained resilience mechanism operating beneath the Fallback Strategy, addressing quota exhaustion at the level of an individual API key rather than an entire provider.

**Objective:** The objective of API Key Rotation is to maximize usage of each provider's free tier by cycling through multiple API keys per provider before concluding that the provider itself is unavailable and escalating to the Fallback Strategy.

### 21.3 Detailed Explanation
Each provider may have one or more associated API keys. Rather than always using the first key until it is entirely exhausted and then giving up on the provider, EduScribe AI rotates through the available keys intelligently. The documented example for Google illustrates this behavior:

```
Google
  Key 1 → Quota Full → Key 2 → Quota Full → Key 3 → Quota Full → Switch Provider
```

Only once every available key for a given provider has reached its quota limit does the system escalate to switching providers entirely, at which point the Fallback Strategy (Chapter 20) takes over.

### 21.4 Internal Workflow

```mermaid
flowchart TD
    A[Request via Provider X] --> B{Key 1 quota available?}
    B -->|Yes| C[Use Key 1]
    B -->|No| D{Key 2 quota available?}
    D -->|Yes| E[Use Key 2]
    D -->|No| F{Key 3 quota available?}
    F -->|Yes| G[Use Key 3]
    F -->|No| H[All keys exhausted - Switch Provider via Fallback Strategy]
```

**Rationale:** API Key Rotation exists as a layer beneath provider-level fallback because switching providers is a more disruptive operation than switching keys within the same provider — a key switch preserves the currently selected model's quality characteristics, whereas a provider switch changes the underlying model entirely. Exhausting all available keys before escalating therefore maximizes the amount of work completed using the preferred, primary provider before quality potentially changes due to fallback.

### 21.6 Advantages
- Maximizes free-tier usage across every available key for a provider before that provider is considered exhausted.
- Delays the need to escalate to a lower-priority fallback provider, preserving output quality for as long as possible.
- Operates within each provider's usage policies by rotating rather than concentrating load on a single key.

**Summary:** API Key Rotation is the finest-grained layer of EduScribe AI's resilience architecture, sitting directly beneath the Fallback Strategy and ensuring that a provider is only abandoned after every one of its available keys has been fully utilized.

---

## 22. Backend Task Flow

The Backend Task Flow describes how EduScribe AI processes long-running AI operations — transcription, OCR, note generation, and PDF rendering — without blocking the HTTP request/response cycle.

**Objective:** The objective of this module is to ensure that operations requiring several minutes to complete are executed asynchronously, in the background, rather than inside the lifetime of a single HTTP request.

### 22.3 Detailed Explanation
Running Whisper transcription, OCR, AI note generation, or PDF generation directly inside an HTTP request handler would block the server for the duration of these long-running operations. To avoid this, EduScribe AI uses **Celery** to execute these operations asynchronously, with **Redis** serving as the message broker between FastAPI and Celery. When a new job is submitted, Redis temporarily stores the task until an available Celery worker begins processing it. Beyond its role as a message broker, Redis is also used for rate limiting, temporary caching, session storage, and queue management.

### 22.4 Internal Workflow

```
User Uploads Video
  ↓
FastAPI
  ↓
Celery Queue
  ↓
Worker
  ↓
Processing
  ↓
Database Update
  ↓
User Notification
```

```mermaid
sequenceDiagram
    participant U as User
    participant F as FastAPI
    participant R as Redis (Broker)
    participant W as Celery Worker
    participant D as Database (SQLAlchemy)
    U->>F: Upload Video
    F->>F: Validate request (Pydantic)
    F->>R: Enqueue processing task
    R->>W: Deliver task
    W->>W: Process (Whisper + OpenCV + PaddleOCR + LLM pipeline)
    W->>D: Update database (via SQLAlchemy, schema via Alembic)
    D-->>U: User Notification
```

**Rationale:** Offloading long-running work to Celery workers, coordinated through Redis, is the direct architectural response to the "Scalability" non-functional requirement from Chapter 1: an AI application involving file uploads, long-running transcription/OCR/generation tasks, and communication with multiple external LLM APIs cannot remain responsive if any of these operations execute synchronously within the request lifecycle.

### 22.6 Advantages
- Background processing keeps the FastAPI request/response cycle fast and responsive regardless of how long the underlying AI processing takes.
- Retry support at the task-queue level adds an additional layer of fault tolerance beyond the LLM-specific Retry Strategy (Chapter 19).
- Parallel execution across multiple Celery workers allows multiple lectures, or multiple chunks within a lecture, to be processed concurrently.

**Summary:** The Backend Task Flow is the operational backbone that allows EduScribe AI's otherwise long-running, multi-stage AI pipeline (transcription through PDF export) to be exposed through a responsive, asynchronous API — ensuring the user-facing system remains usable even while extensive background processing is underway.

---

## 23. Technology Stack

The Technology Stack chapter catalogs every library and framework selected for EduScribe AI, along with the specific architectural responsibility each one fulfills.

**Objective:** The objective of this chapter is to document why each library was selected — favoring mature, well-supported, and (during development) free or open-source tools — and to make explicit the specific responsibility boundary each library occupies within the overall architecture, in line with the system's stated principle of separation of concerns.

### 23.3 Detailed Explanation

**FastAPI** is the primary backend framework, responsible for user authentication, video upload, background task management, history APIs, notes generation APIs, file download APIs, and system administration APIs. It is selected for its automatic request validation via Pydantic models (reducing boilerplate and preventing common runtime errors), its asynchronous architecture (well suited to file uploads, long-running tasks, and external API communication), automatic API documentation, high performance, and strong integration with SQLAlchemy and Celery.

**SQLAlchemy** serves as the Object Relational Mapper, providing Python models that represent database tables instead of requiring raw SQL throughout the application. It handles database models, relationships, CRUD operations, query optimization, and database abstraction, and is selected for its maturity, its strong support for PostgreSQL, and the fact that it allows future migration to a different database without altering business logic.

**Alembic** manages database schema migrations, generating version-controlled migration files whenever a new table or column is added, providing version control for schema changes, easy rollback, team collaboration support, and production safety.

**Celery**, as described in Chapter 22, executes long-running AI operations (Whisper transcription, OCR, AI note generation, PDF generation) asynchronously, preventing these operations from blocking the HTTP request cycle. It provides background processing, retry support, parallel execution, and fault tolerance.

**Redis** acts as the message broker between FastAPI and Celery, temporarily storing submitted jobs until a worker begins processing them, and is additionally used for rate limiting, temporary caching, session storage, and queue management. It is selected for being extremely fast, lightweight, reliable, and easy to integrate.

**Pydantic** validates structured data throughout the project, ensuring that API requests, API responses, and LLM outputs all conform to predefined schemas — for example, validating a structure such as `{"topic": "...", "subtopics": []}` before it is processed further. It provides type safety, automatic validation, better error messages, and reduced bugs.

**PydanticAI**, detailed fully in Chapter 17, manages LLM interactions while guaranteeing structured outputs, handling prompt execution, output validation, retry handling, schema enforcement, and error detection.

**LiteLLM**, detailed fully in Chapter 16, provides the unified interface across multiple LLM providers, handling provider abstraction, model switching, API normalization, and token tracking.

**PaddleOCR** extracts textual information — including formulas, diagrams, tables, and handwritten annotations — from selected video frames, handling slide text extraction, formula recognition, timestamp association, and OCR confidence scoring. It is selected specifically for its strong performance on lecture slides compared with lighter-weight OCR libraries.

**MLX Whisper** converts lecture audio into text. It is selected because the project is developed on Apple Silicon, where MLX Whisper provides significantly better performance than the original Whisper implementation — faster inference and lower memory usage, optimized specifically for M-series chips.

**OpenCV** processes video frames before OCR, handling blur detection, duplicate detection, frame extraction, and image preprocessing, selected for its highly optimized computer vision algorithms.

**Structlog** provides structured, machine-readable logging in place of plain-text print statements, making logs easier to search, filter, and analyze.

**Tenacity**, detailed further in Chapter 19, automatically retries failed operations caused by network issues, rate limiting, or timeouts, providing improved reliability, reduced failures, and automatic exponential backoff.

**Jinja2**, detailed further in Chapter 13, generates HTML from structured notes using reusable templates rather than manually constructed HTML, providing clean code, easy customization, and separation of presentation and logic.

**Markdown-it-py** provides the standards-compliant Markdown rendering engine used to convert structured notes into Markdown before HTML generation, offering reliable rendering, easy formatting, and extensibility.

**WeasyPrint**, detailed further in Chapter 14, generates professional PDFs directly from HTML and CSS, supporting typography, images, tables, and page numbering/table of contents.

**Langfuse** monitors every LLM interaction, recording prompt versions, responses, latency, token usage, cost, and errors. As prompt engineering evolves, Langfuse provides the visibility developers need to determine which prompts produce the highest-quality educational notes, enabling systematic evaluation and continuous prompt-quality improvement.

The overall architecture, expressed as a single top-to-bottom stack, is:

```
Frontend (React)
        │
        ▼
FastAPI
        │
        ▼
Pydantic Validation
        │
        ▼
Celery + Redis
        │
        ▼
Whisper + OpenCV + PaddleOCR
        │
        ▼
PydanticAI
        │
        ▼
LiteLLM
        │
        ▼
Gemini / DeepSeek / Qwen / Llama
        │
        ▼
Structured Notes
        │
        ▼
Markdown-it-py
        │
        ▼
Jinja2 Templates
        │
        ▼
WeasyPrint
        │
        ▼
Professional PDF
```

```mermaid
flowchart TD
    A[Frontend - React] --> B[FastAPI]
    B --> C[Pydantic Validation]
    C --> D[Celery + Redis]
    D --> E[Whisper + OpenCV + PaddleOCR]
    E --> F[PydanticAI]
    F --> G[LiteLLM]
    G --> H[Gemini / DeepSeek / Qwen / Llama]
    H --> I[Structured Notes]
    I --> J[Markdown-it-py]
    J --> K[Jinja2 Templates]
    K --> L[WeasyPrint]
    L --> M[Professional PDF]
```

### 23.4 Internal Workflow
Each library in this chapter occupies exactly one layer of the top-to-bottom stack shown above. Data flows strictly downward: the frontend issues a request, FastAPI receives and validates it, Celery/Redis offload the heavy processing, Whisper/OpenCV/PaddleOCR perform ingestion, PydanticAI/LiteLLM manage the LLM interaction layer, and the export chain (Markdown-it-py → Jinja2 → WeasyPrint) produces the final artifact.

**Rationale:** Every library selection in this chapter is justified by the same underlying principle stated at the top of the source documentation's technology stack chapter: the system favors mature, well-supported, actively maintained, and well-documented open-source libraries over custom-built solutions for already-solved problems, with each library assigned a clearly defined, non-overlapping responsibility.

### 23.6 Advantages
- Every architectural layer (API, ORM, migrations, task queue, broker, validation, LLM abstraction, OCR, transcription, computer vision, logging, retry, templating, Markdown rendering, PDF rendering, observability) has an explicitly named, purpose-built library rather than a general-purpose or custom-built substitute.
- The stack's layering mirrors the End-to-End Pipeline (Chapter 3.1) directly, making it straightforward to map any pipeline stage to the specific library responsible for implementing it.

### 23.7 Limitations
MLX Whisper's selection is explicitly tied to the project's Apple Silicon development environment, which is a structural dependency inherited from Chapter 1's environmental assumption and carried through unchanged into the technology stack.

**Summary:** The Technology Stack chapter is the concrete library-level realization of every architectural principle described earlier in this document — from the provider abstraction described in Chapter 15, to the asynchronous processing described in Chapter 22, to the export chain described in Chapters 12 through 14 — with each library occupying precisely one layer of a strictly layered, top-to-bottom system architecture.

---

## 24. Important Notes

The Important Notes chapter consolidates cross-cutting design commitments that apply across multiple modules described in this document, rather than being scoped to any single chapter.

**Objective:** The objective of this chapter is to make explicit a small number of system-wide design commitments that, if overlooked, could lead to a misreading of any individual chapter in isolation.

### 24.3 Detailed Explanation

**No arbitrary length limits.** Explanation length, as established in Chapter 8, is intentionally uncapped and scales with topic complexity. This is a deliberate design choice rather than an oversight, and it carries a direct cost implication given the free-tier and quota constraints on the LLM providers described in Chapter 15.

**Provider abstraction is mandatory.** As established in Chapter 15, the application must never call a vendor SDK directly. Every request flows through the LLM Manager → LiteLLM → Provider chain, which is what allows providers to be added or removed without touching business logic elsewhere in the system.

**Regeneration is scoped, not global.** As established in Chapter 11, failed Quality Gate checks trigger regeneration of the affected subtopic only, never the entire document. This keeps both token usage and cost proportional to the size of the actual defect.

**Chunk overlap is semantic, not fixed-token.** As established in Chapter 4, overlap size between adjacent chunks is determined by concept continuity, not by a fixed character or token count — distinguishing EduScribe AI's chunking strategy from traditional fixed-token overlap approaches.

**Retries and fallback are distinct mechanisms operating at different scopes.** As established in Chapters 19 and 20, retries (via exponential backoff) handle transient failures on the *same* provider, while fallback switches to the *next* provider only after retries on the current provider have been exhausted. Permanent errors — such as an invalid API key or a malformed request — bypass retries entirely and fail fast, since retrying a permanent error can never succeed.

**Chunk reusability is a stable, shared contract.** As established in Chapters 4 and 5, the same chunk data structure is intended to power Topic Detection, Subtopic Detection, Notes Generation, Flashcards, Quiz Generation, Mind Maps, and future RAG-based features. The chunk schema is therefore treated as a stable, shared contract across every feature that consumes it, rather than as an implementation detail private to any single module.

### 24.4 Internal Workflow
These notes do not describe a runtime workflow of their own; rather, each note functions as a cross-cutting constraint that governs the internal workflow of one or more chapters described earlier in this document — length constraints govern Chapter 8, provider abstraction governs Chapters 15–21, scoped regeneration governs Chapter 11, semantic overlap governs Chapter 4, and the retry/fallback distinction governs Chapters 19 and 20 jointly.

**Rationale:** Consolidating these notes into a single chapter, separate from the individual module chapters where each principle originates, ensures that a reader reviewing any single chapter in isolation does not lose sight of the system-wide commitments that chapter is expected to honor.

### 24.6 Advantages
- Provides a single point of reference for cross-cutting design commitments that would otherwise be scattered implicitly across multiple chapters.
- Reinforces the distinction between mechanisms that are easy to conflate — retries versus fallback, chunk overlap versus fixed-token overlap, scoped regeneration versus full-document regeneration.

**Summary:** The Important Notes chapter serves as a cross-cutting summary of the system's most consequential design commitments, ensuring that the architectural decisions established in Chapters 1 through 23 are read as a coherent, mutually consistent whole rather than as a set of independently negotiable module-level choices.

---

**End of Document**
