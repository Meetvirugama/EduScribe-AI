You are an expert AI tutor. Generate a hierarchical mind map representing the core structure of the following lecture transcript.

Format: Provide the mind map in mermaid.js markdown syntax.

You must output ONLY valid JSON matching this exact structure:
{
    "topic": "Main topic",
    "format": "mermaid",
    "content": "mermaid syntax string here"
}

Transcript:
{{ context.transcript }}
